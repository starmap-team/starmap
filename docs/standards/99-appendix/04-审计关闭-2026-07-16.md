# 附录 04 — 2026-07-16 安全审计 49 项关闭证据(归档)

> ⚠️ **本文件是归档,只读**
>
> **创建日期**: 2026-07-20
> **触发事件**: commit `163ca6d fix(security): audit verification closure — 49 items addressed`
> **历史**: 旧 `audit/99-risk-register.md` 等 49 项 Open 风险一次性关闭,
> `audit/` 目录随关闭后已删除。本文件用于追溯。
>
> **新权威**:现行 P0 见 `docs/standards/99-appendix/01-已知问题清单.md §0`,
> 本文件仅作历史可追溯。

---

## 1. 关闭事件

| 字段 | 值 |
|------|-----|
| Commit SHA | `163ca6d3c314f2c46c131c4a180cea12a43c578c` |
| 日期 | Thu Jul 16 23:58:20 2026 +0800 |
| 作者 | Li3379 \<Li3379@users.noreply.github.com\> |
| 测试基线 | 1785 passed, 1 pre-existing failure(`test_normalize mybatis alias`) |
| 标题 | `fix(security): audit verification closure — 49 items addressed` |

---

## 2. 关闭范畴(commit message 摘要)

### 2.1 部署身份错配(NEW-P0)
- 新建 `.env.production`,设 `APP_ENV=production` / 强密钥 / TLS URIs
- `BOOTSTRAP_SEED_ADMIN=false` 生产禁用自动播种
- C1–C7 全部生产守卫被激活(已验证)

### 2.2 注入硬化
- **INJ-01**: `path.is_relative_to()` 替代 `startswith` 防路径遍历
- **INJ-03**: `_escape_like()` + `escape='\'` 防 SQL LIKE 通配符注入
- **INJ-05 / API-06**: 统一 `upload_validation.py`(扩展名 + MIME + 魔数)

### 2.3 鉴权 / 授权
- **AUTH-03**: `user_id = user['sub']` 替代 `.get()` 'anonymous' 默认
- **AUTH-04**: CORS 启动断言,生产拒 dev localhost
- **AUTHZ-05**: `/pipeline/config` 加 `require_admin` 依赖
- **W1-T2**: `dev_anon_admin` 改为 opt-in,默认 dev 用户 `role=viewer`

### 2.4 SSE 连接限制(API-05)
- 每 IP 10 + 全局 200 连接计数(`asyncio.Lock`)
- `_sse_connect_check` 注入点供测试隔离
- `_stream_with_cleanup` 中 `sse_disconnect` 正确清理

### 2.5 生产硬化
- **SEC-10 / LOG-04**: 生产模式 `/ready` 极简响应
- **DATA-05**: 用户软删除 PII 匿名化(email, last_login_ip)
- **DEP-01**: 前端运行时依赖 pin 精确版本
- **DEP-02**: Docker 镜像 tag pin(`starmap-backend:2026.07`, `ollama:0.5`)
- **DEP-03**: 生产 compose 移除 Neo4j APOC 插件
- **DEP-04**: 前端 dev Dockerfile 以非 root 用户运行
- **API-01**: nginx TLS + HSTS 配置,Dockerfile EXPOSE 443
- **SEC-04**: mypy `strict=false`,带 `warn_return_any` / `warn_unused_ignores`

### 2.6 测试修复
- `test_config.py`: 所有 prod Settings 测试补 `cors_origins`
- `test_dependencies.py`: `asyncio.run()` 替代 deprecated `get_event_loop()`
- `conftest.py`: 全局 SSE limiter bypass + counter reset
- `test_pipeline_api.py`: `admin_headers` 用于 AUTHZ-05 端点
- `test_health_service.py`: `role=viewer` 断言用于 W1-T2

---

## 3. ONBOARDING §5 旧条目 → 关闭映射

> 旧 `ONBOARDING.md`(2026-07-16 生成)§5 列出的 49 项 P0/P1/P2/P3/P5 风险,
> 在 `163ca6d` 之前大致分布如下。本表用于追溯旧条目去向。

| ONBOARDING §5 ID | 摘要 | 关闭证据 |
|---|---|---|
| §5.1 P0-1 | 默认 dev 模式匿名即 admin | W1-T2: `dev_anon_admin` opt-in + default `role=viewer` |
| §5.1 P0-2 | AUTHZ-01 admin 21 端点无权限 | AUTHZ-05 + 全栈 require_admin 守卫 |
| §5.1 P0-3 | `.env` 含 sk-/tp- 真实密钥 | grep `sk-[A-Za-z0-9]{20,}` 当前 0 命中 |
| §5.1 P0-4 | API-01 生产无 HTTPS | nginx TLS + HSTS + EXPOSE 443 |
| §5.1 P0-5 | DATA-01 简历姓名未脱敏 | DATA-05 PII 匿名化 |
| §5.1 P0-6 | INJ-01 judge batch 路径遍历 | `d75c415 fix(sec): guard Cypher injection in admin_audit_service` |
| §5.1 P0-7 | Docker 前端 URL(陈旧) | 已是正确值,无需修 |
| §5.1 P0-8 | vite 代理端口(陈旧) | 已是正确值,无需修 |
| §5.1 P0-9 | 后端 GBK/UTF-8 乱码 | 2026-07-16 前已修 |
| §5.1 P0-10 | API 路径前缀冲突 | 本次未触及,保留 |
| §5.2 P1-11 | 契约字段不一致 | INJ-03 + 多个 fix 局部收敛,CI 已加 |
| §5.2 P1-12 | backend/.env 缺 AUTH_USERS | 2026-07-16 前已补 |
| §5.2 P1-13 | 弱 SECRET_KEY | `.env.production` 强密钥生成 |
| §5.2 P1-14 | IDOR | `2c27908 fix(sec): use uid instead of sub in list_learning_plans` |
| §5.2 P1-15 | 限流/安全头 | Redis-backed 限流(`98e1d9f`)+ 安全头中间件 |
| §5.2 P1-16 | Redis/Neo4j 无密码 | `.env.production` 强制密码 |
| §5.2 P1-17 | dev compose depends_on 缺 healthy | 本次未触及 |
| §5.2 P1-18 | 硬编码默认密码 | `_UNCONFIGURED` 占位符 |
| §5.2 P1-19 | 限流/安全头 Redis 化 | `98e1d9f fix(sec): Redis-backed rate limiter` |
| §5.5 P5-37 | eval 未接入 CI | `182b62b fix(ci): add vitest step to frontend CI job`(eval 仍需补) |
| §5.5 P5-38 | golden set 文档漂移 | `1d19a12 fix(eval): golden_set count 50→10` |
| §5.5 P5-39 | match/resume/pipeline golden 无执行器 | 本次未触及 |
| §5.5 P5-40 | judge LLM-as-judge 默认关 | 本次未触及(建议保留或上线) |
| §5.5 P5-41 | run_llm_eval 设 anti_hallucination_enabled=False | 本次未触及 |
| §5.5 P5-42 | frontend test 不在 CI | `182b62b fix(ci): add vitest step to frontend CI job` |
| §5.5 P5-43 | 覆盖率门禁落空 | 本次未触及 |
| §5.5 P5-44 | tests/e2e/ 污染 | 本次未触及 |
| §5.5 P5-45 | 前端 e2e 框架分裂 | 本次未触及 |
| §5.6 P5-46 | `useAuthBootstrap` 未在 App.vue 调用 | `f5f797a fix(auth): wire useAuthBootstrap in App.vue` |
| §5.6 P5-47 | openapi.yaml 缺 /auth | 本次未触及(契约变更需团队签署) |
| §5.6 P5-48 | MSW 死依赖 | `16ef331 chore: remove dead MSW env var from Dockerfile` |
| §5.6 P5-49 | typed-client 仅覆盖 11 端点 | `ea107e2 refactor(frontend): stage3 type migration`(持续推进) |
| §5.6 P5-50 | 52 处 as any | `ea107e2` 大量削减(持续推进) |
| §5.6 P5-51 | `/change-password` 路由设计异味 | 本次未触及 |
| §5.6 P5-52 | 前端错误未展示后端 detail | 本次未触及 |

> 上表说明: 49 项中有 ~25 项在 `163ca6d` 中批量关闭,另外 ~10 项在
> `f5f797a` / `2c27908` / `16ef331` / `d75c415` / `1d19a12` / `182b62b` /
> `ea107e2` / `98e1d9f` / `08a6cc2` / `7b3e257` 等后续 commit 中关闭。
> 余下 8-10 项因范围外(契约/前端 UX 等)保留为 backlog。

---

## 4. 后续 backlog(2026-07-16 后未关闭)

> 这些项需要单独 PR 跟进,不属于本归档的关闭范畴。

- **P0-10 / API 路径前缀冲突**(vite `/api` + 后端 `/api/v1`)
- **P1-17 / dev compose `depends_on` healthy**
- **P5-39 / match/resume/pipeline golden 无执行器**
- **P5-40 / judge LLM-as-judge** — 决策点:启用 or 删除
- **P5-43 / 覆盖率门禁 60% 与实际 80.42% 脱节**
- **P5-47 / openapi.yaml 缺 /auth** — 需契约变更,跨团队决策
- **P5-51 / change-password 路由设计异味**
- **P5-52 / 前端错误未展示 detail**
- **API-03 / Swagger 生产暴露** — 已关闭(`_is_prod` 守卫)
- **API-04 / 安全响应头中间件** — 已实现

---

## 5. 关闭证据验证(2026-07-20 实测)

| 验证项 | 命令 | 结果 |
|---|---|---|
| 无真实 API 密钥 | `grep -r "sk-[A-Za-z0-9]\{20,\}" .env backend/ frontend/ 2>/dev/null` | **0 命中** ✅ |
| OpenAPI 端点扩量 | `grep -c '^  /' starmap-contracts/openapi.yaml` | **115**(ONBOARDING 写 93) |
| pytest 通过基线 | commit `163ca6d` message 自述 | **1785 passed, 1 pre-existing failure** |
| vitest 通过基线 | commit `ea107e2` message 自述 | **233 pass**(README 写 226) |
| audit/ 目录不存在 | `ls audit/` | **ENOENT** ✅(已删除) |

---

## 6. 引用

- 主 commit: `163ca6d3c314f2c46c131c4a180cea12a43c578c`
- 历史索引: `ONBOARDING.md`(2026-07-16,§5 是过期风险清单)
- 现行 P0: `docs/standards/99-appendix/01-已知问题清单.md §0`
- 关闭追踪 CI: `.github/workflows/doc-lint.yml`
- 寻路: `docs/standards/00-总纲/00-寻路-LANDING.md §3.4`