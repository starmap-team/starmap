# StarMap XH-202621 提交前任务分配表（主线 A + 主线 B）

> 生成日期：2026-08-22 | 提交截止：2026-09-05（剩 14 天）
> 依据：GitHub 远程分支深度核查（ui/upload-ux-polish 139 提交未合入 main）+ 比赛要求文档（科大讯飞 XH-202621）

## ⚠️ 前置事实（必须周知）

1. **`ui/upload-ux-polish` 分支包含全部赛项优化（模块A 新岗位发现、模块B 更新说明、匹配 99.14%、简历 0.938、写入门禁 G1-G4），但 PR #81 CLOSED 未合并**，main 和公网部署均缺这些功能。
2. **所有主线 B 交付物（尤其演示视频）必须在主线 0（合并+重部署）完成后进行**，否则公网没有模块A 可演示。
3. 评测数字存在冲突：8-17 记录三指标全达标（JD 0.951/简历 0.938/匹配 99.14%），8-20 简历报告 F1=0.7569 且 14 条 LLM connection error（假失败）——**必须重配 LLM key 重跑**。

---

## 主线 A：公网项目充实与使用优化

### A 阶段总览

| 阶段 | 目标 | 时限 | 主责 |
|---|---|---|---|
| A0 | 合并 ui 分支赛项优化进 main + 重新部署公网（前置，阻塞 A1-A3） | 第 1-3 天 | Li3379 + 123asdtte + jaimelove67 |
| A1 | 公网数据充实（岗位 550→700+、JD 1285→1500+） | 第 2-8 天 | 123asdtte（主）+ jaimelove67 |
| A2 | 演化数据活化（跑 2-3 轮演化，留真实 changelog 证据） | 第 3-6 天 | xiaoxu-gif |
| A3 | 新岗位候选实战（模块A 端到端验证） | 第 4-6 天 | xiaoxu-gif |
| A4 | 前端使用优化（流畅度/响应式/文案友好化） | 第 3-10 天 | QDYBGS + zeng-yangtao1031 |
| A5 | 每日探活与一致性保障 | 全程 | jaimelove67 |

### A0 合并与重新部署（第 1-3 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| A0.1 | 评估合并策略 | `git fetch origin`；对比 `git log origin/main..origin/ui/upload-ux-polish`（139 提交）与 `git log origin/ui..origin/main`；`git diff --name-only origin/main origin/ui` 看冲突面（约 533 文件） | 列出"必须合入的关键提交清单"（模块A position.py/evolution_service.py/EvolutionDashboard.vue + Phase 8 匹配优化 + build_change_explanation） | Li3379 + 123asdtte |
| A0.2 | 合并冲突解决 | 按 A0.1 清单 cherry-pick 到新分支 `feat/submission-merge`，逐个解决冲突；参考 `backend/tests/` 现有断言 | 本地合并分支可编译、pytest 通过 | jaimelove67 |
| A0.3 | 全量回归 | `cd backend && poetry run pytest`（2590 用例）+ `ruff check` + `mypy`；前端 `cd frontend && npm run build` | 全绿（参考 CI 门禁 .github/workflows/ci.yml） | jaimelove67 |
| A0.4 | 重新部署公网 | push 合并分支 → 服务器 `git pull` → `./scripts/deploy-public.sh <PUBLIC_DOMAIN>` → 探活 `/health` `/ready` | 公网可访问新版本；`POST /api/v1/positions/discover` 返回 candidates 非空 | 123asdtte + 队长确认 |
| A0.5 | 公网版本确认 | 浏览器访问公网 URL，确认 EvolutionDashboard 出现"新兴岗位候选"面板；无公网地址找队长拿 | 截图存档 | 队长 Li3379 |

### A1 公网数据充实（第 2-8 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| A1.1 | 7 适配器多轮抓取 | 逐个调用 `crawler/spiders/` 下 arbeitnow/jobicy/juejin/remoteok/v2ex_remote（含 remotive）/weworkremotely 的 `run_sync(max_count=N)`；每源 2-3 轮 | 每源新增记录，数据源页 total 增长 | 123asdtte |
| A1.2 | 手动导入真实中文 JD | 数据源页"手动导入"（ui 分支已实现 `manual-import` 真实写入 jd_raw，`JdRaw` 有 raw_text 列）；批量粘贴 50-100 条中文 JD | jd_raw 记录 +50~100，pipeline import 后可抽取 | 123asdtte |
| A1.3 | 全链路 pipeline 跑通 | 触发 `POST /api/v1/pipeline/trigger`（或 celery beat 自动）；观察 DAG 各阶段 green；`quality` 页 KPI 岗位 550→700+、JD 1285→1500+ | 三端一致（PG/Neo4j/前端 KPI 对齐） | jaimelove67 |
| A1.4 | 数据质量核对 | 跑 `evaluation/ingestion_consistency.py` + 数据真相页（admin）确认差异 0%；`docs/ingestion-kpi-calibers.md` 对齐口径 | 一致性报告差异 0% | jaimelove67 |

### A2 演化数据活化（第 3-6 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| A2.1 | 跑演化管线 | 触发 `run_evolution_pipeline`（celery task）；或 `POST /api/v1/evolution/run`；确保 skill_timeseries 有数据（先跑 pipeline） | changelog 表新增真实记录 | xiaoxu-gif |
| A2.2 | 更新说明验证 | 确认每条 changelog 有 `build_change_explanation` 生成的自然语言说明（含数据源引用 source_count/mention_count） | EvolutionDashboard changelog 抽屉显示中文说明+数据源 | xiaoxu-gif |
| A2.3 | 截图留证据 | 录制模块B 演示素材：某岗位技能新增/移除/升降级 diff + 更新说明 + 数据源 | 3-5 张截图供 B4 视频/文档引用 | xiaoxu-gif |

### A3 新岗位候选实战（第 4-6 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| A3.1 | 触发岗位发现 | `POST /api/v1/positions/discover`；确认返回 `emerging_positions.candidates`（position/emerging_skills/ratio/definition） | candidates ≥1，definition 含 position_name/required_skills/emerging_required | xiaoxu-gif |
| A3.2 | 前端展示验证 | EvolutionDashboard"新兴岗位候选"面板（ui 分支 `fetchEmergingPositions`）；点击查看定义详情 | 前端可交互展示候选岗位+定义 | xiaoxu-gif + QDYBGS |
| A3.3 | 补充行业场景字段 | `industry_scenario` 当前为 None（注释"P1-5 字段待补"）；从 JD 行业抽取或人工补 1-2 个示例岗位的应用场景 | 候选岗位 definition 含典型应用场景（模块A 完整） | xiaoxu-gif |

### A4 前端使用优化（第 3-10 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| A4.1 | 图谱流畅度 | Graph2D/Graph3D 性能：节点/边规模大时 LOD/聚合；参考 `frontend/src/composables/graph2d|graph3d` | 500+ 节点滚动/缩放无卡顿 | QDYBGS |
| A4.2 | 匹配向导体验 | MatchDiagnosis 5 步流程：上传→选岗→雷达对比→差距分析→学习路径；减少点击步数、结果用通俗中文 | 新用户 3 分钟内完成一次匹配 | zeng-yangtao1031 |
| A4.3 | 文案友好化 | 全站扫专业术语（API 路径/表名/方法名/LLM 产品名）替换为业务化中文；参考 ui 分支 `fd1a4a9`（未分类字面量）、`f3931a2`（KPI 口径标注） | 用户看不懂的术语 0 暴露（浏览器扫 9 路由正则） | QDYBGS |
| A4.4 | 响应式适配 | 移动端/小屏（<768px）布局；参考 ui 分支 `a24b12f`（响应式断点） | 手机浏览器核心页面可用 | zeng-yangtao1031 |

### A5 每日探活（全程）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| A5.1 | 健康检查 | 每日 curl 公网 `/health` `/ready` `/health/detail`；容器 `docker compose -p starmap ps` | 全 healthy | jaimelove67 |
| A5.2 | 双库一致性 | admin 数据真相页 / `reconcile` 端点（`graph_projector.py` 半孤立修复 218f2f9 已生效）；确认 PG=Neo4j（approved 口径） | 岗位/技能/边差异 0%（或 <0.5%） | jaimelove67 |
| A5.3 | LLM key 探活 | 检查 .env.production 的 DASHSCOPE_API_KEY/DEEPSEEK_API_KEY 有效；跑 1 条抽取确认不报 connection error | LLM 通道 100% 可用（B1 依赖此） | jaimelove67 |
| A5.4 | 数据增长日报 | 每日记录岗位/JD/技能/边四数，对比前日 | 数据持续增长曲线（供文档引用） | 123asdtte |

---

## 主线 B：交付物准备

### B 阶段总览

| 阶段 | 目标 | 时限 | 主责 |
|---|---|---|---|
| B1 | 三项指标权威重跑（≥90% 证据） | 第 1-3 天（最优先） | manice2005 + Li3379 |
| B2 | 单元测试覆盖率 ≥60% | 第 3-7 天 | manice2005 |
| B3 | 设计实现方案文档 + PPT | 第 4-10 天 | YYYYYYY-sys + yangle-xgm |
| B4 | 10min 演示视频（须在 A0 后录） | 第 7-11 天 | QDYBGS + yangle-xgm |
| B5 | 测试数据打包（1 新岗位+1 既有岗位） | 第 8-11 天 | 123asdtte |
| B6 | 提交收尾（报名表/云盘/邮件） | 第 12-13 天 | Li3379 |

### B1 三项指标权威重跑（第 1-3 天，最高优先）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| B1.1 | 配好 LLM key | 确认 `.env.production`/本地 env 的 DASHSCOPE_API_KEY 有效；LLM 链路参考 `backend/app/core/llm/`（llm_client.call_llm_with_fallback 降级链） | `curl` 一次真实抽取无 connection error | manice2005 |
| B1.2 | 重跑 JD 解析评测 | `cd backend && poetry run python -m evaluation.run_real_eval`（真实 LLM 管线，110 样本 golden_set.jsonl） | F1 ≥0.90；报告更新 evaluation/baseline_report/ | manice2005 |
| B1.3 | 重跑简历提取评测 | `poetry run python -m evaluation.run_resume_eval`（50 样本 golden_set_resume.jsonl，真实 LLM extract_from_jd 路径） | F1 ≥0.90（修复 8-20 的 0.7569 假失败） | manice2005 |
| B1.4 | 重跑人岗匹配评测 | `poetry run python -m evaluation.run_match_baseline`（432 样本 golden_set_match.jsonl，区间+方向判定口径） | 准确率 ≥90%（当前 97.45%） | manice2005 |
| B1.5 | 汇总权威报告 | 生成一份"三项指标评测总报告"（口径说明：匹配=区间+方向；简历=真实 LLM 管线；附 CI 门禁 accuracy_gate） | docs/competition/ 下最终评测报告（供 B3/B6 引用） | Li3379 |

### B2 覆盖率 ≥60%（第 3-7 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| B2.1 | 重测基线 | 合并 ui 分支后 `cd backend && poetry run pytest --cov`（db7a0f0 已修覆盖率指标）；`backend/pyproject.toml` 门禁 70%（实际目标 ≥60 达标） | 覆盖率数值确认（≥60% 即达标） | manice2005 |
| B2.2 | 补测重点模块 | 用 `backend/coverage.xml` 找低覆盖文件：优先 evolution/emergence_finder/ingestion_gate/graph_projector；参考 `backend/tests/unit/test_discover_positions.py`（ui 分支已有） | 每低覆盖模块 +5 用例；总覆盖率 ≥60% | manice2005 |
| B2.3 | 前端 vitest 覆盖率 | `cd frontend && npx vitest run --coverage`；补关键组件（MatchDiagnosis/PositionList/EvolutionDashboard） | 前端覆盖率 ≥60%（如已达标则记录） | zeng-yangtao1031 |

### B3 设计实现方案文档 + PPT（第 4-10 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| B3.1 | 方案文档结构 | 对照评分四维度组织：完整性（全流程闭环+模块A/B/C/D）、创新性（SimHash 去重/信任度幻觉防控/双库一致性）、体验、实用价值（三指标证据） | 文档目录（参考 `docs/星图-项目设计文档v2.0.md` 已有架构） | YYYYYYY-sys |
| B3.2 | 填充真实数据 | 用 B1 权威评测 + A1 数据规模 + A2/A3 演化截图；引用 `docs/archive/reports/competition-indicators-2026-08-17.md` | 方案文档完成（30-50 页） | YYYYYYY-sys + yangle-xgm |
| B3.3 | PPT 制作 | 10-15 页：背景痛点→系统架构→四大核心功能（含模块A 新岗位发现实测截图）→三指标→公网部署→创新点 | PPT 定稿（配合 B4 视频） | yangle-xgm |

### B4 10min 演示视频（第 7-11 天，须在 A0 合并部署后）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| B4.1 | 录前核对 | 确认公网已部署合并版（有"新兴岗位候选"面板）；准备测试数据（1 份简历 + 若干岗位） | 录制环境就绪 | QDYBGS + 队长 |
| B4.2 | 脚本设计 | 10min 分镜：①图谱全景（2min）②新岗位发现/定义（2.5min，A3.3 的含应用场景）③既有岗位能力更新 diff+说明（2.5min，A2.3 素材）④简历上传匹配诊断+学习路径（2min）⑤创新点收尾（1min） | 分镜脚本（含每段解说词） | yangle-xgm |
| B4.3 | 录制 | 用公网真实地址录屏 + 配音；演示数据用 A1/A2/A3 真实产出 | 10min 视频（mp4） | QDYBGS |
| B4.4 | 剪辑交付 | 剪辑 + 字幕 + 关键指标标注（三指标/数据规模）；导出 ≤10min | 视频文件交 B6 打包 | yangle-xgm |

### B5 测试数据打包（第 8-11 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| B5.1 | 选型 | 从合并后真实数据选 1 个新岗位（A3 候选）+ 1 个既有岗位（A2 有更新记录者） | 2 个岗位确认 | 123asdtte |
| B5.2 | 数据源整理 | 每个岗位：JD 输入（raw_jd 原始 JD 文本）+ 能力图谱输出（required/bonus 技能 + 关系）+ 数据源标注（来源平台/抓取时间） | JSON 示例（输入输出成对） | 123asdtte |
| B5.3 | 打包 | 目录：`submission/test-data/{new-position, existing-position}/` 含 input.json/output.json/README | 压缩包供 B6 | 123asdtte |

### B6 提交收尾（第 12-13 天）

| # | 任务粒度 | 完成办法（引导仓库内容） | 产出/验收 | 负责人 |
|---|---|---|---|---|
| B6.1 | 材料清点 | 对照比赛要求：设计文档 + PPT + 视频 + 源码（GitHub 链接）+ 部署说明 + 测试数据 + 测试用例 + 报名表 | 清单全绿 | Li3379 |
| B6.2 | 云盘上传 | 上传至安全云盘；截图上传时间 | 云盘链接 + 提取码 + 截图 | Li3379 |
| B6.3 | 邮件提交 | 发送至 3094947125@qq.com，压缩包命名"单位—姓名—作品名称—电话" | 邮件送达 + 留存回执 | Li3379 |

---

## 总时间线（14 天）

```
第 1-3 天  ██ A0 合并+重部署  |  B1 指标重跑（并行）
第 2-6 天  ██ A1 数据充实 → A2 演化 → A3 新岗位
第 3-7 天  ██ B2 覆盖率 | A4 前端优化
第 4-10 天 ██ B3 方案文档+PPT
第 7-11 天 ██ B4 演示视频（依赖 A0）
第 8-11 天 ██ B5 测试数据打包
第 12-13 天██ B6 提交收尾
第 14 天   ██ 最终检查 + 预留缓冲
```

## 风险与依赖

| 风险 | 影响 | 缓解 |
|---|---|---|
| 合并冲突（139 提交 vs main） | 阻塞 A0/所有 | A0.1 先出冲突清单，cherry-pick 分批 |
| 评测重跑仍 FAIL | 30 分档位下降 | 8-20 是 connection error 假失败，配好 key 应恢复；备选规则 baseline 口径说明 |
| 公网地址未确认 | 视频/文档无法用真实地址 | A0.5 队长当天确认 |
| 视频录制延迟 | 压缩包缺视频 | B4 严格按 A0 后开始；预留第 13 天缓冲 |
