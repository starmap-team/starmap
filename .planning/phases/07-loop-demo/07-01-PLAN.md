---
phase: 07-loop-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/archive/loop-source-analysis.md
  - frontend/src/stores/loop.ts
  - frontend/src/pages/LoopDemo.vue
  - frontend/src/pages/__tests__/LoopDemo.spec.ts
autonomous: true
requirements: [LOOP-01, LOOP-02]
user_setup: []
must_haves:
  truths:
    - LoopDemo.vue 5 步闭环可正常执行
    - runLoop 请求参数与后端 LoopRunRequest 对齐
    - 步骤状态从后端响应正确映射
  artifacts:
    - LoopDemo.spec.ts 新增 5+ 测试用例
    - loop.ts 错误处理加固（detail 透传）
    - docs/archive/loop-source-analysis.md
---

<objective>
闭环演示模块（LoopDemo）源码分析 + 联调验证 + 测试增强

**Scope:** 前端 LoopDemo.vue (303 行) + loop.ts store + 1 个冒烟测试
</objective>

<tasks>

<task type="tracer">
  <name>Task 1: 源码缺陷分析</name>
  <files>docs/archive/loop-source-analysis.md</files>
  <action>
    Read LoopDemo.vue, stores/loop.ts, backend/app/api/v1/loop.py.
    Document findings:
    - 5 步骤状态机管理（extract → graph_sync → match → learn → reflect）
    - runLoop 请求/响应字段对齐（jd_text/target_position + steps[].duration_seconds）
    - 错误处理（loop.ts 的 catch）
    - 测试覆盖（仅 1 个冒烟测试）
  </action>
  <verify>
    <automated>test -f docs/archive/loop-source-analysis.md</automated>
  </verify>
  <done>分析报告已创建</done>
</task>

<task type="auto">
  <name>Task 2: 联调修复</name>
  <files>frontend/src/stores/loop.ts</files>
  <action>
    - 修复 loop.ts 错误处理：catch 中提取 err.response.data.detail（与 Phase 6 jd.ts 同模式）
    - 验证 URL 正确（/loop/run 应为 POST，无 /admin/ 前缀问题）
  </action>
  <verify>
    <automated>cd frontend && npx eslint --quiet src/stores/loop.ts</automated>
  </verify>
  <done>loop.ts 错误透传修复</done>
</task>

<task type="auto">
  <name>Task 3: 测试增强</name>
  <files>frontend/src/pages/__tests__/LoopDemo.spec.ts</files>
  <action>
    Mock @/api/request。增强 LoopDemo.spec.ts：
    - 渲染测试
    - 空 JD 输入验证（handleRunLoop 应提示）
    - 步骤状态切换（mock /loop/run 响应）
    - 错误处理
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/LoopDemo.spec.ts --no-cov -q 2>&1 | tail -3</automated>
  </verify>
  <done>5+ 测试用例全部通过</done>
</task>

</tasks>

<verification>
1. ruff + eslint 通过
2. 前端 vitest 全量通过
</verification>

<success_criteria>
- [ ] 5+ 新测试用例
- [ ] 错误透传 detail
- [ ] 所有测试通过
</success_criteria>
</output>