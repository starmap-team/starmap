---
wave: 1
depends_on: []
files_modified:
  - backend/app/utils/audit.py (write DB)
  - backend/app/models/audit_models.py (new)
  - backend/alembic/versions/011_*.py (new - audit table)
  - frontend/src/components/ErrorBoundary.vue (new)
  - frontend/src/components/EmptyState.vue (new)
  - frontend/src/components/SkeletonCard.vue (new)
  - frontend/src/plugins/echarts.ts (new - lazy load)
autonomous: true
requirements: [DX-01, DX-02, DX-03, DX-04]
---

# PLAN-01: Observability + DX (Wave 1)

## Tasks

### Task 1.1: Audit log persistence
Write audit events to PostgreSQL `audit_events` table instead of file only.

### Task 1.2: Frontend ErrorBoundary
Vue `onErrorCaptured` global error boundary component. Pages don't white-screen.

### Task 1.3: Shared EmptyState + SkeletonCard
Create reusable components, use in 8+ pages.

### Task 1.4: ECharts lazy loading
Plugin-based lazy registration instead of eager import per page.

## Acceptance Criteria
- Audit events written to DB
- ErrorBoundary catches errors, shows fallback UI
- EmptyState used in 8+ pages
- ECharts lazy loaded, first-screen bundle smaller
