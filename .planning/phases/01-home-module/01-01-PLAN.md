---
phase: 01-home-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/archive/home-source-analysis.md
  - docs/archive/home-optimization-design.md
  - backend/app/api/v1/graph.py
  - frontend/src/stores/graph.ts
  - frontend/src/components/GraphSearchBar.vue
  - frontend/src/pages/__tests__/Home.spec.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - 前端 Home.vue 及其周边组合式函数、Store 的源码缺陷已完整分析并归档
    - 后端 graph/* 端点、graph_service、graph_overview 的源码缺陷已完整分析并归档
    - 基于分析结果产出了 3-5 个具体的用户优化点与需求设计方案
    - /api/v1/graph/overview 返回的数据结构与前端 graph.ts 的 DomainOverviewItem 一致
    - /api/v1/graph/ka/{ka_id}/positions 返回的数据结构与前端 graph.ts 的 fetchKAPositions 预期一致
    - 图谱渲染（2D/3D）正常显示节点和边数据，无渲染异常
    - 搜索功能在已加载数据范围内正常工作（前端搜索），无崩溃或重复结果
    - 所有现有测试通过（pytest 和 vitest）
    - Home.spec.ts 新增冒烟测试覆盖 loading 态、空数据态、搜索交互
  artifacts:
    - docs/archive/home-source-analysis.md
    - docs/archive/home-optimization-design.md
    - backend/app/api/v1/graph.py (fixed)
    - frontend/src/stores/graph.ts (fixed)
    - frontend/src/components/GraphSearchBar.vue (fixed)
    - frontend/src/pages/__tests__/Home.spec.ts (updated)
  key_links:
    - graph.ts store fetchOverview 调用 /api/v1/graph/overview 的响应结构对齐
    - graph.ts store fetchKAPositions 调用 /api/v1/graph/ka/{ka_id}/positions 的响应结构对齐
    - Home.vue onMounted 触发 graphStore.fetchOverview() 初始化数据流
    - GraphSearchBar.vue 的 client-side 搜索依赖 graphStore.domains + graphStore.allNodes
    - Graph2D.vue 渲染依赖 graphStore.visibleNodes / visibleEdges computed 属性
    - Graph3D.vue 渲染依赖 graphStore 的 graph3DNodes / graph3DLinks composable

---

<objective>
对全景图谱模块（Home）进行全链路源码分析、缺陷发现、优化设计、联调修复和闭环验证。

**Purpose:** 确保全景图谱模块的前后端数据流正确、代码无功能性缺陷、用户体验可优化、测试覆盖完整。

**Output:**
1. `docs/archive/home-source-analysis.md` — 前后端源码缺陷分析报告
2. `docs/archive/home-optimization-design.md` — 用户优化点与需求设计方案
3. 修复后的 graph.py / graph.ts / GraphSearchBar.vue 源码
4. 更新后的 Home.spec.ts 测试文件
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@backend/app/api/v1/graph.py
@backend/app/services/graph_overview.py
@backend/app/services/graph_service.py
@backend/app/services/graph_serializers.py
@backend/app/api/v1/evolution.py
@frontend/src/pages/Home.vue
@frontend/src/stores/graph.ts
@frontend/src/composables/home/index.ts
@frontend/src/composables/home/useHomeInteractions.ts
@frontend/src/composables/home/useGraph2DData.ts
@frontend/src/composables/home/useGraph3DData.ts
@frontend/src/composables/home/useEvolutionPanel.ts
@frontend/src/composables/home/useGraphToolbarState.ts
@frontend/src/composables/home/useHomeLayout.ts
@frontend/src/composables/home/useNodeSelection.ts
@frontend/src/composables/useGraph2DLayers.ts
@frontend/src/components/GraphSearchBar.vue
@frontend/src/components/Graph2D.vue
@frontend/src/components/Graph3D.vue
@frontend/src/components/HomeKpiStrip.vue
@frontend/src/components/DetailPanel.vue
@frontend/src/pages/__tests__/Home.spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 全景图谱模块源码缺陷分析（前后端全链路）</name>
  <files>
    docs/archive/home-source-analysis.md
  </files>
  <action>
    逐层阅读并分析全景图谱模块的全部源码，找出功能性缺陷、代码质量问题、潜在 bug。按以下层级组织分析报告，每层列出具体问题和行号引用：

    **第 1 层 — 前端页面与组合式函数 (Home.vue + composables/home/)**
    - Home.vue (frontend/src/pages/Home.vue): 检查 composable 导入与使用是否正确，onMounted 初始化流程，模板中的事件绑定（尤其 onHandleNodeClick 未 await goToPositionLayer 的异步问题），KPI 计算逻辑（totalPositions/totalSkills 的独立计数 vs 领域聚合回退），响应式绑定（autoRotate3D 双向同步问题）
    - useHomeInteractions.ts: 检查 handleNodeClick 在 domain 层调用 goToPositionLayer 未 await 的问题，handleSearchSelect 的 multi-layer 导航逻辑，closeDetail 与 clearSelection 的协作，toggleEvolution 的 loading 状态缺失
    - useGraph2DData.ts: 验证 kaColorMap 计算逻辑在 domain 数据为空时的行为
    - useGraph3DData.ts: 读取并验证 3D 数据转换逻辑
    - useEvolutionPanel.ts: 读取并验证演化面板逻辑
    - useGraphToolbarState.ts: 读取并验证工具栏状态
    - useHomeLayout.ts: 读取并验证布局控制

    **第 2 层 — 前端 Store (graph.ts)**
    - 检查 `fetchOverview` 的 API 响应结构映射：后端返回 `connections` 但前端存为 `domainConnections`，验证字段名对齐
    - 检查 `fetchKAPositions` 的响应处理：`positions`, `position_skill_edges`, `skills` 三个字段是否完整消费
    - 检查 `positionsByKA` 的 Map 响应式：Pinia 对 `Map` 的追踪能力（Map.prototype.set 不会被 Vue 3 自动追踪，需要 `Map` 整体替换或使用 `reactive` Map）
    - 检查 `visibleNodes` / `visibleEdges` computed 逻辑：三层导航的边界条件（domain 层无数据、position 层无缓存、detail 层无 Edges）
    - 检查 `goToPositionLayer` 的 async 调用链：调用方在 Home.vue 中的 await 缺失
    - 检查 `evolutionEdges` 的 `mapEvolutionPath` 函数：原始 API 字段名 `source_position`/`target_position` 到内部 `source_id`/`target_id` 的映射是否正确
    - 检查错误处理：catch 语句中仅 DEV console.error，无用户反馈

    **第 3 层 — 后端 API (graph.py)**
    - 检查 `/graph/overview` 端点：`DomainOverviewResponse` 的结构（domains, connections, independent_positions/skills/edges）与前端预期是否一致
    - 检查 `/graph/ka/{ka_id}/positions` 端点：`KAPositionsResponse` 在 domain/tech_stack/level 三种模式下的数据返回一致性
    - 检查 `/graph/position/{position_id}/skills` 端点：`PositionSkillDetailResponse` 的 position/skills/edges 结构
    - 检查 GraphNode / GraphEdge Pydantic 模型定义与前端 GraphNode / GraphEdge 接口的字段对齐
    - 检查 `group_by` 参数处理：`Literal["domain", "tech_stack", "level"]` 三种模式的 dispatch 逻辑
    - 检查 `get_ka_positions` 中 tech_stack/level 模式下的 Neo4j 查询性能（全量扫描 Position 节点 + 应用层分类过滤）

    **第 4 层 — 后端服务层 (graph_service.py + graph_overview.py + graph_serializers.py)**
    - graph_service.py: 检查 `fetch_overview_by_domain` 的 Neo4j 查询（KA 节点的 OPTIONAL MATCH 链），fallback 逻辑（无 KA 节点时回退到 tech_stack 分类），KA-KA 连接查询的 `SHARES_POSITION` 边权重固定为 0.5
    - graph_overview.py: 检查 `fetch_overview_by_tech_stack` 的 Neo4j 查询模式（三阶段串行：先取所有 Position → 再取 Skill 计数 → 再取共享关系），性能瓶颈分析，`TECH_STACK_KEYWORDS` 关键词匹配的准确性
    - graph_overview.py: 检查 `fetch_overview_by_level` 的查分类逻辑（`_classify_level` 的 name 推断规则可能误判）
    - graph_serializers.py: 检查 `_safe_properties`, `serialize_node`, `serialize_relationship` 的异常处理

    **第 5 层 — 搜索功能 (GraphSearchBar.vue)**
    - 检查搜索范围：仅限 `graphStore.domains` + `graphStore.allNodes`，未加载的节点不可搜索
    - 检查搜索结果排序：精确匹配优先逻辑
    - 检查搜索交互：drop-down 的 blur 延迟隐藏（BUG-007 修复的 timer 泄漏问题验证）
    - 检查搜索与其他组件的协作：`emit("nodeSelected")` → `handleSearchSelect` 的多层导航

    **第 6 层 — 图谱渲染 (Graph2D.vue + Graph3D.vue + useGraph2DLayers.ts)**
    - Graph2D.vue: 检查 composable 生命周期（useG6Lifecycle init/destroy），watch 触发器（何时重新渲染），layer deps getter 闭包捕获
    - Graph3D.vue: 检查 3D 渲染数据流（graph3DNodes/graph3DLinks 来自 composable）
    - useGraph2DLayers.ts: 检查 renderDomainLayer/renderPositionLayer/renderDetailLayer 的节点边构造逻辑，visibleFiltered 过滤条件，layout 配置边界（太多节点时 force layout 性能）

    对每个发现的问题，标注：
    - 所在文件与行号
    - 严重级别: CRITICAL / HIGH / MEDIUM / LOW
    - 问题描述与影响范围
    - 建议修复方向

    将分析结果写入 `docs/archive/home-source-analysis.md`。
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create docs/archive/ directory first. Run: test -f docs/archive/home-source-analysis.md && grep -c 'CRITICAL\|HIGH\|MEDIUM\|LOW' docs/archive/home-source-analysis.md</automated>
  </verify>
  <done>
    docs/archive/home-source-analysis.md 已创建，包含 6 层源码分析、每个问题标注了严重级别、行号和修复方向。
  </done>
</task>

<task type="auto">
  <name>Task 2: 用户优化点调研与需求设计</name>
  <files>
    docs/archive/home-optimization-design.md
  </files>
  <action>
    基于 Task 1 的分析结果，进行用户优化点调研和需求设计。产出 3-5 个具体可执行的优化需求，每个需求包含：

    1. **需求背景**: 当前的问题或用户痛点（引用 Task 1 中的发现）
    2. **优化目标**: 预期的用户体验改善
    3. **设计方案**: 具体的技术实现方案，包括：
       - 需要修改的文件
       - 核心变更逻辑
       - 前后端数据流变化
       - 兼容性考虑（不影响现有功能）
    4. **验收标准**: 可验证的完成条件

    需求候选方向（基于代码分析，但不限于此）：

    **需求 A: 后端搜索 API 支持**
    - 背景: GraphSearchBar.vue 仅搜索已加载的 domains + allNodes，未展开的 KA 下的 Position/Skill 不可搜
    - 设计: 新增 `GET /api/v1/graph/search?q={keyword}&type=all|position|skill|domain` 端点，后端使用 Neo4j 全文索引或子串匹配
    - 影响: graph.py 新增端点，GraphSearchBar.vue 改为混合搜索（本地 + 远程）

    **需求 B: 图谱加载性能优化（分页加载 + 懒展开）**
    - 背景: `fetchKAPositions` 一次性加载 KA 下所有 Position 及其 Skill 边，大量 Position 时渲染卡顿
    - 设计: 后端新增 `limit`/`offset` 参数，前端按需加载更多；Graph2D 渲染增加 LOD 层级
    - 影响: graph.py 的 `/graph/ka/{ka_id}/positions` 新增分页参数，Graph2D.vue 的 LOD 增强

    **需求 C: KPI 口径说明与数据一致性**
    - 背景: totalPositions/totalSkills 有独立计数和领域聚合两个口径，用户可能困惑
    - 设计: 统一 KPI 口径，明确展示独立计数；HomeKpiStrip.vue 的 tooltip 增强
    - 影响: Home.vue 的 KPI 计算逻辑，HomeKpiStrip.vue 的 tooltip 文案

    **需求 D: 错误状态与降级体验**
    - 背景: fetchOverview/fetchKAPositions 出错时仅 DEV console.error，用户无感知
    - 设计: 全局错误提示 + 重试按钮；空数据态文案优化
    - 影响: graph.ts 的 catch 逻辑，Home.vue 的空数据态提示

    **需求 E: 演化图层的交互优化**
    - 背景: 演化路径加载时无 loading 指示，evolutionPathsLoading 状态未在前端展示
    - 设计: 演化加载时显示骨架屏或进度提示；演化边的 source/target 节点高亮
    - 影响: useHomeInteractions.ts 的 toggleEvolution，Graph2D.vue 的 evolution 渲染

    每个需求必须包含可行性评估（基于现有代码架构，指出实现复杂度）。

    将设计方案写入 `docs/archive/home-optimization-design.md`。
  </action>
  <verify>
    <automated>test -f docs/archive/home-optimization-design.md && grep -c '需求' docs/archive/home-optimization-design.md</automated>
  </verify>
  <done>
    docs/archive/home-optimization-design.md 已创建，包含 3-5 个优化需求，每个需求含背景、方案、验收标准。
  </done>
</task>

<task type="auto">
  <name>Task 3: 前后端联调验证与数据流修复</name>
  <files>
    backend/app/api/v1/graph.py
    frontend/src/stores/graph.ts
    frontend/src/components/GraphSearchBar.vue
  </files>
  <action>
    执行前后端联调验证，修复数据流问题。不做的：不做大规模重构，不引入新功能（搜索 API 等新功能在 Task 2 设计但不在本 Task 实现）。

    **Step 1: API 响应结构对齐验证**
    读取 `backend/app/api/v1/graph.py` 中每个端点的响应模型，与 `frontend/src/stores/graph.ts` 中的接口定义和消费代码逐字段对比：

    - `/api/v1/graph/overview` → `DomainOverviewResponse` vs `fetchOverview` 的响应处理
      - 字段: `domains: DomainOverviewItem[]`, `connections: GraphEdge[]`, `independent_positions`, `independent_skills`, `independent_edges`
      - 验证: 前端 fetchOverview 期望的 `data.connections` 赋值给 `domainConnections.value` —— 正确，后端返回 `connections`
      - 验证: 前端 `total_positions`/`total_skills` 未消费 —— 非 bug，KPI 使用 `independentPositions`/`independentSkills`
      - 修复: 如果 `DomainOverviewItem` 缺少 `color` 字段，确保后端返回；如果前端 `GraphNode` 接口的 `color` 属性在 `properties` 内，确认映射正确

    - `/api/v1/graph/ka/{ka_id}/positions` → `KAPositionsResponse` vs `fetchKAPositions` 的响应处理
      - 字段: `positions: GraphNode[]`, `position_skill_edges: GraphEdge[]`, `skills: GraphNode[]`
      - 验证: 前端 `fetchKAPositions` 期望这三个字段 —— 正确，后端返回
      - 修复: 如果 `positionsByKA` 的 Map 响应式有问题（Vue 3 不追踪 Map.prototype.set），改为 `Map → reactive({})` 或整个 Map 替换

    - `/api/v1/graph/position/{position_id}/skills` → `PositionSkillDetailResponse` vs `Home.vue` 中是否调用
      - 验证: Home.vue 不使用此端点（DetailPanel 使用已加载的 allEdges），但确保未来可用

    **Step 2: graph.ts 数据流修复**
    - 修复 `positionsByKA` 的响应式问题：将 `ref(new Map())` 改为 `reactive(new Map())` 或每次 set 时整体替换 `positionsByKA.value = new Map(positionsByKA.value).set(key, val)`
    - 修复 `fetchOverview` 的 catch 逻辑：添加用户友好的错误提示（使用 ElMessage 或 store 中的 error 状态），不仅仅 DEV console.error
    - 修复 `fetchKAPositions` 的 catch 逻辑：同上
    - 确认 `evolutionEdges` 的 `mapEvolutionPath` 中 `source_id`/`target_id` 正确映射 `source_position`/`target_position`

    **Step 3: GraphSearchBar.vue 搜索修复**
    - 修复: 搜索时未处理 `domain.name` 为 undefined 的情况（`d.name.toLowerCase()` 当 `d.name` 为 undefined 时崩溃）
    - 修复: 搜索时 `n.labels[0]` 可能为 undefined，`"Skill"` 等字符串比较可能因类型不一致而失败
    - 增强: 搜索结果去重使用 `id` 作为 key，但 `displayName` 拼接到搜索文本时可能产生重复 —— 使用 Set\<string\> 基于 id 去重
    - 修复: blurHideTimer 在 onUnmounted 中清理，但 `showSearchDropdown` 可能在 unmount 后仍然为 true——添加 `isUnmounted` 守卫

    **Step 4: Home.vue 交互修复**
    - 修复: `onHandleNodeClick` 中 `handleNodeClick(id, selectedNode)` 没有 await —— 在 Home.vue 的 `onHandleNodeClick` 中改为 `async (id: string) => { await handleNodeClick(id, selectedNode) }`
    - 修复: `totalPositions`/`totalSkills` 的 computed 中 `graphStore.independentPositions ?? graphStore.domains.reduce(...)` —— 当 `independentPositions` 为 0 时，`??` 会回退到 domains.reduce，但 0 是有效值应当保留。改为 `graphStore.independentPositions ?? (graphStore.independentPositions !== 0 ? graphStore.independentPositions : 0)` 或使用 `!== undefined && !== null` 判断

    每个修复必须：
    - 只修改目标行，不重构周围代码
    - 添加注释说明修复原因（`// fix: ...` 或 `# fix: ...`）
    - 执行 `cd backend && poetry run ruff check .` 确保无 lint 新增
    - 执行 `cd frontend && npm run lint` 确保无 lint 新增
  </action>
  <verify>
    <automated>cd backend && poetry run ruff check --quiet backend/app/api/v1/graph.py && cd frontend && npx eslint --quiet src/stores/graph.ts src/components/GraphSearchBar.vue</automated>
  </verify>
  <done>
    - graph.py 响应结构与前端 graph.ts 接口定义完全对齐
    - positionsByKA 响应式问题已修复
    - GraphSearchBar.vue 搜索不会因 undefined 属性崩溃
    - Home.vue 的异步 handleNodeClick 已 await
    - totalPositions/totalSkills 的 0 值正确处理
    - lint 检查通过
  </done>
</task>

<task type="auto">
  <name>Task 4: 业务闭环验证与测试增强</name>
  <files>
    frontend/src/pages/__tests__/Home.spec.ts
  </files>
  <action>
    验证全景图谱模块的端到端业务闭环，并增强测试覆盖。

    **Step 1: 数据流溯源验证**
    验证从 Neo4j 到前端渲染的完整数据链路（不修改代码，仅验证）：
    - 链路: Neo4j Position/Skill 节点 → graph_service.py 的 Neo4j 查询 → graph.py API 响应 → graph.ts store 处理 → Home.vue 的 visibleNodes/visibleEdges computed → Graph2D.vue/G6 渲染
    - 验证点: 选择一个实际数据路径，确认每个环节的数据字段一致（如 Position.name 从 Neo4j 属性到前端显示）

    **Step 2: 已有测试确保通过**
    - 运行 `cd backend && poetry run pytest tests/e2e/full_e2e_test.py::test_graph_overview -x --no-header -q` 验证后端图谱概览测试
    - 运行 `cd frontend && npx vitest run src/pages/__tests__/Home.spec.ts --reporter=verbose` 验证前端 Home 测试
    - 如果测试失败，分析失败原因并修复（不修改测试逻辑，只修复源码问题）

    **Step 3: 增强 Home.spec.ts 冒烟测试**
    在现有测试基础上新增以下测试用例（不删除或修改现有测试）：

    - **测试: 图谱加载状态显示** — 设置 `store.loading = true`，验证页面的 `v-loading` 指令生效（检查 loading 类或骨架屏元素）
    - **测试: 空数据状态显示** — 设置 `store.domains = []` 且 `store.loading = false`，验证 `.empty-hint` 元素显示 "图谱数据为空" 文案
    - **测试: 搜索组件交互** — 使用 `mount` 或 `shallowMount` 挂载 GraphSearchBar，设置 `graphStore.domains` 和 `graphStore.allNodes`，模拟搜索输入，验证搜索结果列表出现
    - **测试: 视图模式切换** — 模拟 `@set-view-mode` 事件，验证 `viewMode` 响应式变化
    - **测试: KPI 卡片显示** — 设置 `graphStore.domains` 有数据，验证 KPI 卡片显示正确的总计数值

    每个新增测试采用 `shallowMount` + 完整 mock 的策略（与现有测试一致），避免依赖 Canvas/WebGL。

    **Step 4: 运行完整测试套件**
    - 运行 `cd frontend && npx vitest run --reporter=verbose` 确认所有前端测试通过
    - 运行 `cd backend && poetry run pytest -x --no-header -q` 确认所有后端测试通过
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Home.spec.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 所有现有测试通过（pytest + vitest）
    - Home.spec.ts 新增至少 5 个测试用例（loading 态、空数据态、搜索交互、视图模式、KPI 显示）
    - 数据流溯源验证完成，确认 Neo4j → API → Store → 组件的字段一致性
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → API | Untrusted user input crosses here (search query, node clicks) |
| API → Neo4j | Neo4j query parameters cross here (Cypher injection surface) |
| API → PostgreSQL | Read-only queries, no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | GraphSearchBar.vue → client-side search | low | accept | Client-side search only searches loaded data, no auth needed |
| T-01-02 | Tampering | graph.py `get_ka_positions` ka_id parameter | medium | mitigate | ka_id is URL path param, validated by FastAPI routing. For tech_stack/level modes, ka_id is checked against known prefixes (`ts-`, `lv-`) before use |
| T-01-03 | Information Disclosure | graph.py overview endpoints | low | accept | Graph data is intentionally public within the application |
| T-01-04 | Elevation of Privilege | Neo4j driver injection via unvalidated params | low | mitigate | All Neo4j queries use parameterized syntax (`$param`), no string interpolation except depth int which is clamped to [1,5] |
| T-01-05 | Denial of Service | `get_ka_positions` tech_stack/level mode: MATCH (p:Position) without limit | medium | mitigate | Tech_stack/level mode queries all Positions then filters in-app. Large datasets may cause OOM. Fix: add LIMIT + pagination (covered in Task 2 optimization design) |
| T-01-06 | Denial of Service | Graph2D rendering with +1000 nodes | low | accept | LOD already implemented in Graph2D; 3D rendering may lag but is acceptable |
</threat_model>

<verification>
## Phase Verification

1. **Code correctness**: All modified files pass lint (ruff + ESLint)
2. **Test pass**: `cd backend && poetry run pytest -x --no-header -q` passes
3. **Test pass**: `cd frontend && npx vitest run --reporter=verbose` passes
4. **Analysis quality**: `docs/archive/home-source-analysis.md` covers 6 layers with severity ratings
5. **Design quality**: `docs/archive/home-optimization-design.md` has 3-5 feasible optimization requirements
6. **Search stability**: GraphSearchBar.vue does not crash on undefined/null properties
7. **KPI correctness**: 0 values for independentPositions/independentSkills display correctly
8. **Reactivity**: positionsByKA Map mutation correctly triggers Vue reactivity
</verification>

<success_criteria>
## Success Criteria

1. [ ] docs/archive/home-source-analysis.md 存在，覆盖 6 层源码分析
2. [ ] docs/archive/home-optimization-design.md 存在，包含 3-5 个优化需求
3. [ ] 所有 lint 检查通过 (ruff + ESLint)
4. [ ] 后端 pytest 全部通过
5. [ ] 前端 vitest 全部通过（含新增的 5+ 个测试）
6. [ ] GraphSearchBar.vue 不会因 undefined/null 属性崩溃
7. [ ] Home.vue 的 totalPositions/totalSkills 在 0 值时正确显示
8. [ ] positionsByKA 的 Map 变动正确触发 Vue 响应式更新
9. [ ] onHandleNodeClick 的异步调用已 await
</success_criteria>

<output>
Create `.planning/phases/01-home-module/01-01-SUMMARY.md` when done
</output>