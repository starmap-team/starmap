---
phase: 02-position-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/archive/position-source-analysis.md
  - backend/app/api/v1/position.py
  - backend/tests/integration/test_position_industries_endpoint.py
  - frontend/src/stores/jd.ts
  - frontend/src/pages/PositionList.vue
  - frontend/src/pages/PositionDetail.vue
  - frontend/src/pages/__tests__/PositionList.spec.ts
  - frontend/src/pages/__tests__/PositionDetail.spec.ts
autonomous: true
requirements:
  - POS-01
  - POS-02
  - POS-03

must_haves:
  truths:
    - 源码缺陷分析已重新对齐到 post-Phase 13 / post-Phase 23 的真实代码状态（不重复报告已修复项为 bug）
    - 后端 `GET /api/v1/positions/industries` 端点存在，返回数据库中不重复的 industry 列表（不受当前页/筛选影响）
    - 前端 `el-input`（搜索框）输入变化触发 `fetchPositions()` 重拉（debounced）；`el-tag`（行业 chip）点击更新 `selectedIndustry` 并触发重拉，`page.value` 重置为 1
    - Neo4j fallback 路径（`_list_positions_neo4j`）构造的 `PositionNode` 包含 `review_status=props.get("review_status")`，admin 看到的状态徽标反映 PG/Neo4j 真实审核状态
    - `PositionDetail.vue` 监听 `() => route.params.name`，路由参数变化时重新调用 `jdStore.fetchPositionDetail`（不再依赖 fetchToken 单点竞态）
    - `jdStore.fetchPositionSkills` 标记 `@deprecated`；`/graph/position/{name}/skills` 路由在生产代码中无前端调用方（仅 E2E/契约测试保留）
    - `PROFICIENCY_MAP[s.proficiency]` 不再保留 `?? 0.5` 死分支；后端 required→精通 / preferred→了解 / 其他→熟悉 映射与 Neo4j 路径对齐并锁定
    - `CATEGORY_LABELS` 保留 9 项白名单；未知 category 渲染为「其他」而非原始英文
    - `PositionList.spec.ts` 与 `PositionDetail.spec.ts` 通过 `vi.mock('@/stores/jd', ...)` stub 掉 `useJdStore`，新增的冒烟/交互测试不再触发真实 axios 网络调用
    - 新增 `backend/tests/integration/test_position_industries_endpoint.py` 锁定 `/positions/industries` 响应形状与跨端过滤一致性
    - 既有 Phase 13 契约回归测试 `backend/tests/integration/test_position_conformance.py` 仍 4 passed（P-F1 公开 approved / P-F2 search OR name+industry / `_escape_like`）
    - `pytest` + `vitest` 全绿
  artifacts:
    - docs/archive/position-source-analysis.md (refreshed — flags H2/H3/M1-M5/L2/L3, marks already-fixed items as invariants)
    - backend/app/api/v1/position.py (modified — `/positions/industries` route + Neo4j `review_status` writeback)
    - backend/tests/integration/test_position_industries_endpoint.py (new — locks endpoint shape)
    - frontend/src/stores/jd.ts (modified — `fetchPositionSkills` JSDoc `@deprecated`)
    - frontend/src/pages/PositionList.vue (modified — `el-input` @input handler, industry `el-tag` refetch, `review_status` non-masking)
    - frontend/src/pages/PositionDetail.vue (modified — `watch(route.params.name)`, L2 fallback removed, L3 category fallback label)
    - frontend/src/pages/__tests__/PositionList.spec.ts (modified — `vi.mock('@/stores/jd')` + new test cases)
    - frontend/src/pages/__tests__/PositionDetail.spec.ts (modified — `vi.mock('@/stores/jd')` + new test cases)
  key_links:
    - PositionList.vue el-input 双向绑定的 `searchQuery` → debounced `fetchPositions` → jdStore.fetchPositions → GET /api/v1/positions
    - PositionList.vue el-tag 点击 → set `selectedIndustry` → `onIndustryChange` 重置 page=1 → `fetchPositions`
    - backend/app/api/v1/position.py `list_positions_industries` (new) → `PositionRecord.industry` `select(distinct).order_by(industry.asc())` with admin include_all parity
    - backend/app/api/v1/position.py `_list_positions_neo4j` `PositionNode(... review_status=props.get("review_status"))` — admin status badge in PositionList.vue reflects truth
    - PositionDetail.vue `watch(() => route.params.name, refetch)` → jdStore.fetchPositionDetail → GET /api/v1/positions/{id}
    - PositionList.vue status badge (admin only) → `pos.review_status` from API, NOT `?? 'approved'` masking
    - Frontend spec files: `vi.mock('@/stores/jd', () => ({ useJdStore: () => ({ fetchPositions: vi.fn().mockResolvedValue({...}), fetchPositionDetail: vi.fn().mockResolvedValue({...}), ... }) }))`
    - backend/tests/integration/test_position_industries_endpoint.py mock session asserts DISTINCT industry SQL + ordering + include_all parity

---

<objective>
按 `02-REVIEWS.md` Cycle 1 反馈修订岗位模块的执行计划。**不重做已落地的修复**（`fetchPositions` 搜索/行业参数、PG `proficiency` 映射、`_escape_like` 转义、PG/Neo4j search OR 跨端一致），而是补齐当前真实代码仍存在的差距：搜索/行业控件不触发 refetch（H2）、行业列表受当前页限制（H3）、Neo4j fallback 漏写 `review_status`（M1）、`fetchPositionSkills` 死代码（M2）、`PositionDetail` 路由参数变化不重拉（M3）、测试缺 jdStore mock（M4）、`review_status ?? 'approved'` 掩盖 Neo4j 状态（M5），以及 L2/L3 小清理。同步收缩/重排设计输出（L4）。

**Purpose:** 让 PositionList / PositionDetail 的前后端联调在「用户输入/点击/路由跳转」三个交互维度上都得到真实闭环，并补强测试基础设施（mock pattern + 新端点契约测试），让 `02-VALIDATION.md` 中残留的 OPEN·LOW / OPEN·MEDIUM 全部收敛。

**Output:**
1. 重新对齐 live 源码的 `docs/archive/position-source-analysis.md`（标记已修复项为 invariant，新增 H2/H3/M1-M5/L2-L3 现状记录）
2. 新增后端 `GET /api/v1/positions/industries` 端点（含 admin include_all 变体）
3. 修改 PositionList.vue / PositionDetail.vue / position.py / jd.ts / 两个 spec 文件
4. 新增 `backend/tests/integration/test_position_industries_endpoint.py`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-position-module/02-VALIDATION.md
@.planning/phases/02-position-module/02-REVIEWS.md
@backend/app/api/v1/position.py
@backend/app/api/v1/graph.py
@backend/app/services/graph_service.py
@backend/app/models/extraction_models.py
@backend/tests/integration/test_position_conformance.py
@frontend/src/pages/PositionList.vue
@frontend/src/pages/PositionDetail.vue
@frontend/src/stores/jd.ts
@frontend/src/components/SkillRadar.vue
@frontend/src/utils/proficiency.ts
@frontend/src/router/index.ts
@frontend/src/pages/__tests__/PositionList.spec.ts
@frontend/src/pages/__tests__/PositionDetail.spec.ts
@frontend/src/stores/__tests__/learningPlan.test.ts
@frontend/src/pages/__tests__/AuditLog.spec.ts
@frontend/vitest.config.ts
@frontend/src/api/request.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 重新对齐 live 源码的缺陷分析（覆盖 review cycle findings）</name>
  <files>
    docs/archive/position-source-analysis.md
  </files>
  <read_first>
    - .planning/phases/02-position-module/02-VALIDATION.md
    - .planning/phases/02-position-module/02-REVIEWS.md
    - backend/app/api/v1/position.py
    - frontend/src/pages/PositionList.vue
    - frontend/src/pages/PositionDetail.vue
    - frontend/src/stores/jd.ts
  </read_first>
  <action>
    重新对齐 `docs/archive/position-source-analysis.md` 到 2026-07-28 当前真实代码状态，**禁止把 Phase 13/23 已修复的项再标记为 bug**。按以下结构组织：

    **第 1 层 — 已落地且必须保持的 invariant（明确标 ✅ 不可回退）**
    - `fetchPositions` 已传 `search` + `industry` 到后端（PositionList.vue:111-125，注释 `// fix: 传递搜索关键字和行业筛选到后端...`）—— 标 invariant
    - `search` 后端已 OR 匹配 name+industry（position.py:84-90 + 110-117，注释 `Phase 13 一致性审计：search 同时匹配 name 与 industry`）—— 标 invariant；Neo4j 路径亦对齐（position.py:333-337）—— 标 invariant
    - `_escape_like` 已转义 `%`/`_`（position.py:19-21）—— 标 invariant；由 `test_position_conformance.py:48-50` 锁定
    - 公开默认 `review_status='approved'`（position.py:91-96）；admin `include_all=true` 不得注入 approved 过滤（test_position_conformance.py:65-73）—— 标 invariant
    - PG `proficiency` 映射：required→精通 / preferred→了解 / 其他→熟悉（position.py:201，注释 `# Phase 13 ...`）—— 标 invariant；Neo4j 路径通过 `normalize_proficiency(level)` 落在同一集合 `{精通,熟悉,了解}`（`backend/app/core/extraction/normalize.py:750-760` 定义的 `_EXPERT_TERMS/_BEGINNER_TERMS`）—— 标 invariant
    - `goDetail` 已直接 `router.push('/position/' + id)`，无双重编码（PositionList.vue:165-169，注释 `# 按 UUID 跳转：名称可能含 / 等路径不安全字符...`）—— 标 L1 no-op
    - `fetchPositionSkills` 已被 `fetchPositionDetail` 取代（PositionDetail.vue:80-90 注释「改用列表传入的 id（UUID，路径安全）；silent=true 使真正缺失时不弹全局 404 toast」）；store 仍 export（jd.ts:166）—— 标 M2 dead code
    - `test_position_conformance.py` 4 passed 锁定 P-F1/P-F2—— 标 invariant

    **第 2 层 — Review cycle 新发现的真实缺陷**
    - **H2** — `el-input v-model="searchQuery"` 无 `@input` / `@change` 处理器（PositionList.vue:193-200）；industry `el-tag` 点击仅设 `selectedIndustry = ...`（PositionList.vue:230、243）不触发 `fetchPositions`；`onMounted` / `onPageChange` / `onStatusFilterChange` 是唯一的 refetch 触发点（PositionList.vue:155-163, 180）。影响：用户输入搜索词或切换行业 chip 后，列表仍显示当前页结果（被 `filteredPositions` 客户端过滤遮蔽），`total` 不变，UI 与数据脱节。
    - **H3** — `industries` computed 从 `positions.value.map(p => p.industry)` 派生（PositionList.vue:48-52），`pageSize=24`（:41）意味着仅当前页的行业出现在筛选 chip 中；用户在 page 3 看到的 chip 集与 page 1 不同。影响：缺少 `GET /api/v1/positions/industries` 端点返回数据库级别的 distinct industry。
    - **M1** — `_list_positions_neo4j` 构造 `PositionNode(...)` 缺 `review_status`（position.py:392-400；对比 PG 路径 position.py:151 显式传 `review_status=getattr(r, "review_status", None)`）。影响：admin 通过 Neo4j fallback 看到的卡片 `review_status` 字段为 null，前端 `PositionList.vue:142` 用 `p.review_status ?? 'approved'` 兜底，导致 pending_review 被显示为「已发布」。
    - **M3** — `PositionDetail.vue` 的 `fetchToken` 自增 + `onMounted` 触发器（:82-120）只覆盖首次加载；`router/index.ts:19` 的路由 `name: 'position-detail'` 在 `/position/A` ↔ `/position/B` 切换时复用同一组件实例，`onMounted` 不会重跑。影响：导航到另一岗位时仍显示旧岗位的 skills 与 radar。
    - **M4** — `PositionList.spec.ts:18-36` mount helper 未 `vi.mock('@/stores/jd', ...)`；`onMounted(fetchPositions)` 触发真实 `request.get('/positions', ...)`。jsdom 环境下 axios 走真实网络（无 server 时 `Network Error`），新增断言会因为 unhandled rejection 而 flake。`learningPlan.test.ts:7-20` 与 `AuditLog.spec.ts:9-17` 的 vi.mock 模式可直接复用。
    - **M5** — `PositionList.vue:142` `review_status: p.review_status ?? 'approved'` 把后端未传（Neo4j fallback null）当作 `approved` 渲染徽标；与 M1 同根。
    - **L2** — `PositionDetail.vue:47` `PROFICIENCY_MAP[s.proficiency] ?? 0.5` 是死分支：PG/Neo4j 路径永远返回 `精通/熟悉/了解`，`PROFICIENCY_MAP` 已包含全部 3 键（`frontend/src/utils/proficiency.ts:1-5`）。
    - **L3** — `PositionDetail.vue:52-62` 的 `CATEGORY_LABELS` 白名单 9 项，后端 `SkillRecord.category` 自由字符串（`backend/app/models/extraction_models.py:298` `default="general"`），`graph_serializers.py:191-193` 只在 `category == "Skill"` 时回退到 `source_category`；未知 category 走 `:251` `?? row.category` 显示原始英文。

    **第 3 层 — 测试覆盖现状**
    - `PositionList.spec.ts` 49 行 2 测试；`PositionList.spec.ts:47` `toBeGreaterThanOrEqual(0)` 是恒真断言
    - `PositionDetail.spec.ts` 41 行 1 测试；`SkillRadar` 被 stub，radar 渲染逻辑无覆盖
    - 既无 jdStore mock（M4）；既无 search/industry refetch 行为测试；既无 route-change refetch 测试

    **第 4 层 — 数据流结论**
    - 三端探针（02-VALIDATION.md §1）已锁定 PG ≡ Neo4j 原始计数（Position 56、industry-contains 38、public approved 39）
    - 唯一仍 OPEN 的跨端缺口：Neo4j fallback 漏写 `review_status`（M1，VALIDATION.md §2 残留 OPEN-LOW）
    - 新端点 `/positions/industries` 需保持与 `/positions` 同样的过滤语义（P-F1 公开 approved / P-F2 search OR / `_escape_like`）

    **第 5 层 — 已归档项（仅记录，不再视为 bug）**
    - L1 goDetail 双重编码（H1 中已归档，代码已是 raw UUID）
    - 「空状态文案优化」「错误处理增强」等纯体验项（review §3 L4 评 5/5 项 ROI 低）—— 本 plan 不重做

    对每个发现，标注：
    - 所在文件与行号
    - 严重级别 CRITICAL / HIGH / MEDIUM / LOW
    - 问题描述 + 触发场景 + 用户可见影响
    - 建议修复方向（指向后续 Task 2/3 的具体 action）

    将更新后的分析写入 `docs/archive/position-source-analysis.md`（覆盖旧版本）。
  </action>
  <verify>
    <automated>test -f docs/archive/position-source-analysis.md && INV=$(grep -c 'invariant' docs/archive/position-source-analysis.md) && H2=$(grep -c 'H2' docs/archive/position-source-analysis.md) && H3=$(grep -c 'H3' docs/archive/position-source-analysis.md) && M1=$(grep -c 'M1' docs/archive/position-source-analysis.md) && M2=$(grep -c 'M2' docs/archive/position-source-analysis.md) && M3=$(grep -c 'M3' docs/archive/position-source-analysis.md) && M4=$(grep -c 'M4' docs/archive/position-source-analysis.md) && M5=$(grep -c 'M5' docs/archive/position-source-analysis.md) && L2=$(grep -c 'L2' docs/archive/position-source-analysis.md) && L3=$(grep -c 'L3' docs/archive/position-source-analysis.md) && test "$INV" -ge 5 && test "$H2" -ge 1 && test "$H3" -ge 1 && test "$M1" -ge 1 && test "$M2" -ge 1 && test "$M3" -ge 1 && test "$M4" -ge 1 && test "$M5" -ge 1 && test "$L2" -ge 1 && test "$L3" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - File covers 5 layers (frontend pages / store / backend API / data flow / tests), each item labelled CRITICAL / HIGH / MEDIUM / LOW.
    - Phase 13/23 already-fixed items (`fetchPositions` search/industry params, PG `proficiency`, `_escape_like`, search OR name+industry, public approved default) are listed as `invariant` with a back-reference to `test_position_conformance.py`, NOT as bugs.
    - New review findings H2, H3, M1, M2, M3, M4, M5 each appear with file:line and a one-line repro.
    - L2 (PROFICIENCY_MAP fallback dead) and L3 (CATEGORY_LABELS vs free-form) appear as LOW cleanups.
    - L1 (goDetail double-encode) explicitly labelled as no-op with file:line evidence.
    - L4 (Task 2 Requirements A-E bloat) cited with a one-line rationale for collapsing to direct implementation in Task 2.
  </acceptance_criteria>
  <done>
    docs/archive/position-source-analysis.md 已重写，区分 invariant / 当前真实缺陷 / 已归档项，且明确指向 Task 2/3 的具体修复点。
  </done>
</task>

<task type="auto">
  <name>Task 2: 定向代码修复（H2/H3/M1/M2/M3/M5 + L2/L3 清理）</name>
  <files>
    backend/app/api/v1/position.py
    backend/tests/integration/test_position_industries_endpoint.py
    frontend/src/stores/jd.ts
    frontend/src/pages/PositionList.vue
    frontend/src/pages/PositionDetail.vue
  </files>
  <read_first>
    - backend/app/api/v1/position.py:53-154 (list_positions), :157-251 (get_position), :312-409 (_list_positions_neo4j), :33-43 (PositionNode model), :19-21 (_escape_like)
    - frontend/src/pages/PositionList.vue:42 (pageSize), :48-52 (industries), :53-67 (filteredPositions), :108-153 (fetchPositions), :155-163 (onPageChange/onStatusFilterChange), :165-169 (goDetail), :193-200 (el-input), :230/243 (industry el-tag clicks)
    - frontend/src/pages/PositionDetail.vue:5-7 (imports), :17 (useRoute), :44-50 (radarData), :52-62 (CATEGORY_LABELS), :82-120 (onMounted fetchToken), :251 (category fallback), :259-266 (proficiency tag)
    - frontend/src/stores/jd.ts:93-101 (fetchPositionSkills/fetchPositionDetail), :109-119 (fetchPositions signature), :165-168 (return object)
    - frontend/src/utils/proficiency.ts:1-5 (PROFICIENCY_MAP keys)
    - frontend/src/router/index.ts:17-22 (route name 'position-detail')
    - backend/app/core/extraction/normalize.py:750-760 (_EXPERT_TERMS/_BEGINNER_TERMS)
    - backend/tests/integration/test_position_conformance.py:25-83 (mock session pattern)
  </read_first>
  <action>
    按 review findings 实施定向代码修复。每条修改只动目标行/函数，不重构周围代码；新增行需 `// fix: ...` 或 `# fix: ...` 注释说明来源（H2/H3/M1/M2/M3/M5/L2/L3）。

    **H3 — 新增 `GET /api/v1/positions/industries` 端点**
    - 在 `backend/app/api/v1/position.py` 的 `list_positions` 路由前新增 `list_industries` 函数（参考 `list_positions` 的查询风格）
    - 签名：`async def list_industries(session, page, page_size, industry, search, status, include_all) -> dict[str, list[str]]`
    - 行为：复用 `_escape_like` + 同样的 search-OR name+industry 过滤（P-F2 一致性），复用 effective_status 公开 approved 默认（P-F1 一致性），复用 `include_all` admin 覆盖。返回数据库级 distinct industry（`sa.select(PositionRecord.industry).distinct().order_by(PositionRecord.industry.asc())`，过滤掉 NULL 与空字符串）。
    - 注册路由：`@router.get("/industries", response_model=IndustriesResponse)`，在 `list_positions` 之前注册以避免 `/{position_id}` 路径吞掉 `industries` 段
    - 新增响应模型 `class IndustriesResponse(BaseModel): items: list[str]`
    - 添加注释 `# fix: H3 industries list is page-bounded on client; this endpoint returns DB-level distinct industries`

    **M1 — Neo4j fallback 写回 `review_status`**
    - 修改 `backend/app/api/v1/position.py` 的 `_list_positions_neo4j`（位置：line ~392-400 的 `items.append(PositionNode(...))`）
    - 在 `PositionNode(...)` 构造里添加 `review_status=props.get("review_status")`（与 PG 路径 `getattr(r, "review_status", None)` 对齐；Neo4j 节点属性可能不存在，所以用 `.get` 而非索引）
    - 添加注释 `# fix: M1 Neo4j fallback omitted review_status, masked admin status badge via M5 fallback`

    **H2 — 前端控件触发 refetch**
    - 在 `frontend/src/pages/PositionList.vue` 的 `<el-input v-model="searchQuery" ...>` (line ~193-200) 上增加 `:debounce` 不可行（Element Plus `el-input` 不原生支持），改用 `watch` + `setTimeout`：
      - 引入 `import { ref, watch } from 'vue'`（如果文件尚未 import `ref`）
      - **关键（C2-M1）**：用 `const searchDebounceTimer = ref<ReturnType<typeof setTimeout> | undefined>(undefined)` 替代模块级 `let`，避免 SPA 内多个 PositionList 实例（如 `/positions` + 预览路由）共享同一 `let` 变量导致的清理歧义。`clearTimeout`/`setTimeout` 通过 `searchDebounceTimer.value` 读写；`watch` 回调中先 `if (searchDebounceTimer.value) clearTimeout(searchDebounceTimer.value)` 再 `searchDebounceTimer.value = setTimeout(() => { page.value = 1; fetchPositions() }, 300)`。
      - 在 `onMounted` 之后追加 `watch(searchQuery, () => { if (searchDebounceTimer.value) clearTimeout(searchDebounceTimer.value); searchDebounceTimer.value = setTimeout(() => { page.value = 1; fetchPositions() }, 300) })`
      - 添加注释 `# fix: H2 search input did not refetch; debounced to 300ms`
    - 修改 `frontend/src/pages/PositionList.vue` 的 industry `el-tag` 点击：
      - 「全部」tag（line 230）：`@click="selectedIndustry = ''; onIndustryChange()"`
      - 行业 tag（line 243）：`@click="selectedIndustry = selectedIndustry === ind ? '' : ind; onIndustryChange()"`
      - 新增 `function onIndustryChange() { page.value = 1; fetchPositions() }`（与 `onStatusFilterChange` 同形，line 160-163）
      - 添加注释 `# fix: H2 industry chip click did not refetch; onIndustryChange resets page and calls fetchPositions`
    - 移除 `filteredPositions` computed（line 53-67）—— 后端已做完整过滤，前端不再需要客户端过滤；模板中所有 `filteredPositions` 替换为 `positions`
    - 校验：模板 `:v-if="filteredPositions.length || loading"` 改为 `:v-if="positions.length || loading"`；`v-for="pos in filteredPositions"` 改为 `v-for="pos in positions"`

    **M5 — admin 状态徽标反映真实值**
    - 修改 `frontend/src/pages/PositionList.vue:142` `review_status: p.review_status ?? 'approved'` 改为 `review_status: p.review_status ?? null`（去掉前端硬编码 fallback；admin 徽标映射见 `statusLabel` line 93-106，null 时显示「未分类」或空 —— 由 `statusLabel` 的 default 分支返回 `status ?? ''` 自然处理；admin 看到的 null 徽标需明确为「未审核」或「无状态」文案）
    - 在 `statusLabel` 函数中追加 `default` 分支处理 null：当前 default `return status ?? ''`（line 104）—— 改为当 `status` 为 null 时返回 `'未分类'`
    - 添加注释 `# fix: M5 review_status ?? 'approved' masked Neo4j-fallback null state; rely on backend truth (PG line 151, Neo4j line M1 fix)`

    **M3 — PositionDetail 路由参数变化 refetch**
    - 修改 `frontend/src/pages/PositionDetail.vue`：
      - 引入 `import { ref, computed, onMounted, watch } from 'vue'`
      - 把当前 `onMounted` 内的 fetch 逻辑提取为独立 `async function loadDetail(id: string)` 复用 `fetchToken` 竞态机制（保持现有 `myToken !== fetchToken` 守卫）
      - 在 `onMounted` 之后追加 `watch(() => route.params.name, (newName) => { if (newName) loadDetail(String(newName)) })`
      - 修改 `onMounted` 改为 `onMounted(() => loadDetail(positionName.value))`
      - 添加注释 `# fix: M3 fetchToken mechanism only covered onMounted; route param changes between /position/A and /position/B reuse the same component instance, so add a watch to refetch`
      - 注意：移除原 `let fetchToken = 0` 模块级变量改放进 `loadDetail` 闭包外仍 module 级；保持 `myToken !== fetchToken` 守卫语义

    **M2 — `fetchPositionSkills` 标 deprecated**
    - 修改 `frontend/src/stores/jd.ts:93-96`：在 `fetchPositionSkills` 函数前添加 JSDoc `@deprecated since 2026-07-28 — use fetchPositionDetail instead; /graph/position/{name}/skills has no frontend caller (verified via grep).`
    - 不要删除函数本身（`tests/integration/api-contract.test.ts:98-113` 和 `tests/e2e/full_e2e_test.py:212-223` 仍调用 `/graph/position/{name}/skills`，后端 route 保留）
    - 添加注释 `// fix: M2 dead-code; route retained for E2E/contract tests but no production caller`

    **L2 — 移除 `PROFICIENCY_MAP` 死分支 fallback**
    - 修改 `frontend/src/pages/PositionDetail.vue:47` `PROFICIENCY_MAP[s.proficiency] ?? 0.5` 改为 `PROFICIENCY_MAP[s.proficiency]`（PG/Neo4j 永远返回 `精通/熟悉/了解`；如类型不严可加 `as number` 断言，但不要默认值遮蔽真实 bug）
    - 添加注释 `// fix: L2 ?? 0.5 was dead branch; backend always returns 精通/熟悉/了解 (utils/proficiency.ts) — let undefined surface as a real bug`

    **L3 — 未知 category 文案统一**
    - 修改 `frontend/src/pages/PositionDetail.vue:251` `{{ CATEGORY_LABELS[row.category] ?? row.category }}` 改为 `{{ CATEGORY_LABELS[row.category] ?? '其他' }}`
    - 添加注释 `<!-- fix: L3 free-form backend category falls back to '其他' instead of raw English -->`
    - 不动 `CATEGORY_LABELS` 9 项白名单

    **新端点的 backend 测试（先建文件，Task 3 再扩展）**
    - 创建 `backend/tests/integration/test_position_industries_endpoint.py` 基础骨架（含 `client_no_neo4j` fixture 复用 `test_position_conformance.py:40-44` 模式；3 个测试用占位符，Task 3 填充断言）
    - 文件头注释说明：本测试锁定 `/positions/industries` 响应形状、distinct+sort 顺序、search/industry 过滤与 `/positions` 的 P-F2 一致性

    **不做的（明确排除）**
    - 不修改 `fetchPositions` 已正确的 search/industry 参数传递（PositionList.vue:111-125）
    - 不修改 PG `proficiency` 已正确的 required→精通 映射（position.py:201）
    - 不修改 `_escape_like`、不修改 P-F1/P-F2 已锁定的过滤逻辑
    - 不修改 `goDetail` 的 `router.push('/position/' + id)`（L1 no-op）
    - 不删除 `fetchPositionSkills` 函数（M2 仅标 deprecated）
    - 不删除后端 `/graph/position/{name}/skills` 路由（E2E/契约测试依赖）
    - 不新增「空状态文案优化」「错误重试按钮」等体验项（L4 评 ROI 低）
  </action>
  <verify>
    <automated>cd backend && poetry run ruff check --quiet backend/app/api/v1/position.py backend/tests/integration/test_position_industries_endpoint.py && cd ../frontend && npx eslint --quiet src/pages/PositionList.vue src/pages/PositionDetail.vue src/stores/jd.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/v1/positions/industries` returns HTTP 200 with `{"items": ["金融", "互联网/IT", ...]}` sorted ASC; query params `search`/`industry`/`status`/`include_all` follow the same P-F1/P-F2/`_escape_like` invariants as `/positions`.
    - `_list_positions_neo4j` constructs `PositionNode(... review_status=props.get("review_status"))` (verified by `grep -n "review_status" backend/app/api/v1/position.py` showing two write sites: line 151 PG path and the new Neo4j line).
    - `PositionList.vue` `el-input` change to `searchQuery` triggers a 300ms-debounced `fetchPositions` that resets `page.value = 1`; the `filteredPositions` computed is removed and the template iterates `positions` directly.
    - `PositionList.vue` industry `el-tag` click sets `selectedIndustry` and calls `onIndustryChange` which sets `page.value = 1` and calls `fetchPositions`.
    - `PositionList.vue:142` no longer hard-codes `?? 'approved'`; admin status badge for a `review_status = null` row renders as `未分类` (verified by setting `p.review_status = null` in test).
    - `PositionDetail.vue` `watch(() => route.params.name, ...)` triggers `loadDetail(newName)` on route param change; the existing `myToken !== fetchToken` guard is preserved inside `loadDetail`.
    - `jdStore.fetchPositionSkills` carries `@deprecated` JSDoc referencing the review note.
    - `PROFICIENCY_MAP[s.proficiency]` no longer has `?? 0.5`; the type is `number` (TS may need `!` or fallback for the index access — see utils/proficiency.ts `Record<string, number>`).
    - `CATEGORY_LABELS` keeps 9 entries; `?? row.category` is replaced by `?? '其他'`.
    - `backend/tests/integration/test_position_industries_endpoint.py` exists with at least 3 test function stubs.
    - `cd backend && poetry run ruff check --quiet backend/app/api/v1/position.py backend/tests/integration/test_position_industries_endpoint.py` exits 0.
    - `cd frontend && npx eslint --quiet src/pages/PositionList.vue src/pages/PositionDetail.vue src/stores/jd.ts` exits 0.
  </acceptance_criteria>
  <done>
    - H2: search input + industry chip 触发 refetch；`filteredPositions` 客户端过滤被移除
    - H3: `/positions/industries` 端点 live
    - M1: Neo4j fallback 写回 review_status
    - M2: fetchPositionSkills JSDoc @deprecated
    - M3: PositionDetail 路由参数变化 refetch
    - M5: 不再用 `?? 'approved'` 掩盖 Neo4j null 状态
    - L2: PROFICIENCY_MAP 死分支移除
    - L3: 未知 category 文案统一为「其他」
    - 新 backend test 文件骨架就位
    - ruff + eslint 双绿
  </done>
</task>

<task type="auto">
  <name>Task 3: 测试增强（jdStore mock 模式 + 新行为用例 + 端点契约锁）</name>
  <files>
    frontend/src/pages/__tests__/PositionList.spec.ts
    frontend/src/pages/__tests__/PositionDetail.spec.ts
    backend/tests/integration/test_position_industries_endpoint.py
  </files>
  <read_first>
    - frontend/src/pages/__tests__/PositionList.spec.ts:1-48 (current 2 tests + mount helper)
    - frontend/src/pages/__tests__/PositionDetail.spec.ts:1-40 (current 1 test + mount helper)
    - frontend/src/stores/__tests__/learningPlan.test.ts:7-20 (vi.mock @/stores/user pattern)
    - frontend/src/pages/__tests__/AuditLog.spec.ts:9-17 (vi.mock @/api/request pattern)
    - frontend/src/pages/__tests__/Home.spec.ts:13-53 (composables mock pattern)
    - backend/tests/integration/test_position_conformance.py:25-83 (mock session / TestClient pattern)
    - frontend/vitest.config.ts:11-13 (environment: jsdom, globals: true)
    - frontend/src/stores/jd.ts:69-119 (useJdStore exports to mock)
    - frontend/src/api/request.ts:218-235 (TypedRequest.get signature for the mock factory)
  </read_first>
  <action>
    按 review M4 反馈，引入 `vi.mock('@/stores/jd', ...)` 模式（参考 `learningPlan.test.ts:7-20` 写法），并补齐以下行为用例 + 新端点契约锁。

    **M4 — PositionList.spec.ts 引入 jdStore mock**
    - 删除现有 `vi.mock('@/composables/useAuthBootstrap', ...)`（PositionList.vue 未 import 该 composable，现状属死 mock —— review §3 M4 引用）
    - 在文件顶部新增 `const mockFetchPositions = vi.fn().mockResolvedValue({ items: [], total: 0, page: 1, page_size: 24 })`
    - 新增 `vi.mock('@/stores/jd', () => ({ useJdStore: () => ({ fetchPositions: mockFetchPositions, fetchPositionDetail: vi.fn().mockResolvedValue(null), searchPositions: vi.fn().mockResolvedValue([]), fetchList: vi.fn(), fetchCostSummary: vi.fn(), extractJd: vi.fn(), clearResult: vi.fn(), list: { value: [] }, loading: { value: false }, extractResult: { value: null }, extractLoading: { value: false } }) }))`
    - 把现有 mount helper 改为 `setActivePinia(createPinia())` + `useUserStore` 的 `isAdmin` 显式设置（admin 测试需要 `true`）—— 使用 `vi.mock('@/stores/user', () => ({ useUserStore: () => ({ isAdmin: { value: false } }) }))`，admin 场景另开 describe 块用 `vi.mock` 重置
    - 在 `beforeEach` 中 `mockFetchPositions.mockClear()` 防止跨测试污染

    **PositionList 新增测试用例（不删除现有 2 个，但断言改为 mock 驱动）**
    - **search input triggers fetchPositions (debounced 300ms)**：调用 `wrapper.find('input').setValue('后端')` → `vi.advanceTimersByTime(350)` → `expect(mockFetchPositions).toHaveBeenCalledWith(expect.objectContaining({ search: '后端', page: 1 }))`。需要 `vi.useFakeTimers()` 在 `beforeEach` 启用。
    - **industry chip click triggers fetchPositions**：找到「互联网/IT」chip（mocked 数据需先返回包含该 industry 的 item 才能渲染 chip —— 改用 `mockFetchPositions.mockResolvedValueOnce({ items: [{ position_id: 'p1', name: 'X', industry: '互联网/IT', name_cn: '' }], total: 1, page: 1, page_size: 24 })`），`await flushPromises()` 渲染 chip，`await wrapper.findAll('[role="button"]').find(el => el.text().includes('互联网/IT')).trigger('click')` → `expect(mockFetchPositions).toHaveBeenCalledWith(expect.objectContaining({ industry: '互联网/IT', page: 1 }))`
    - **admin status badge reflects actual review_status (M5)**: 用 admin 变体 (`vi.doMock('@/stores/user', () => ({ useUserStore: () => ({ isAdmin: { value: true } }) }))`) + `mockFetchPositions.mockResolvedValueOnce({ items: [{ position_id: 'p1', name: 'X', industry: '互联网/IT', review_status: 'pending_review' }], total: 1, page: 1, page_size: 24 })` → `await flushPromises()` → 断言渲染的 chip 文本含「待审核」（非「已发布」）；同时验证 `null` review_status 渲染「未分类」
    - **empty state when total=0 and loading=false**: `mockFetchPositions.mockResolvedValueOnce({ items: [], total: 0, page: 1, page_size: 24 })` → mount → `await flushPromises()` → 断言 `.empty-guide` 元素存在
    - **pageSize 24 default and `total > pageSize` shows pagination**: `mockFetchPositions.mockResolvedValueOnce({ items: [...Array(24).keys()].map(i => ({ position_id: `p${i}`, name: `P${i}`, industry: 'X' })), total: 50, page: 1, page_size: 24 })` → 断言 `.pagination-wrapper` 存在（`el-pagination` 在 stub 列表内，验证 `v-if="total > pageSize"` 触发即可）

    **M4 — PositionDetail.spec.ts 引入 jdStore mock**
    - 删除 `vi.mock('@/composables/useAuthBootstrap', ...)`（同 M4 死 mock）
    - 在文件顶部新增 `const mockFetchPositionDetail = vi.fn()`
    - 新增 `vi.mock('@/stores/jd', () => ({ useJdStore: () => ({ fetchPositions: vi.fn().mockResolvedValue({ items: [], total: 0 }), fetchPositionDetail: mockFetchPositionDetail, searchPositions: vi.fn().mockResolvedValue([]), fetchList: vi.fn(), fetchCostSummary: vi.fn(), extractJd: vi.fn(), clearResult: vi.fn(), list: { value: [] }, loading: { value: false }, extractResult: { value: null }, extractLoading: { value: false } }) }))`
      - **关键（C2-M2）**：`vi.mock` 工厂通过闭包捕获模块顶层 `const mockFetchPositionDetail`；**不要把工厂改成 inline 箭头函数** 或把 `const` 提到 `vi.mock` 之后 —— Vitest 会 hoist `vi.mock` 到模块顶部，但 `const` 声明不会 hoist，TDZ 报错。工厂本体内只引用外层 `const`，exec 时机是 lazy 的（首次 import `@/stores/jd` 时才求值），所以放在 `const` 之后不会出问题。
    - mount helper 中 `useRoute` mock 改为可变 + Vue-reactive：`vi.mock('vue-router', () => ({ useRoute: () => routeRef.value, useRouter: () => ({ push: vi.fn() }) }))` 且 `const routeRef = ref({ params: { name: 'Backend-Engineer' }, query: {}, path: '/position/x', meta: {} })`，并 `export function setRouteParams(p: Record<string, string>) { routeRef.value = { ...routeRef.value, params: { ...routeRef.value.params, ...p } } }`
      - **关键（C2-L1）**：`routeRef` 必须用 `ref()` 包裹，否则 Vue 的 `watch(() => route.params.name, ...)` 不会响应 mutation —— `watch` 追踪 Proxy traps，不追踪 raw object mutation；若 `setRouteParams` 直接改 `routeRef.params.name` 而 `routeRef` 是普通对象，watcher 不触发，**M3 用例会 silently pass**（onMounted 触发的初次调用无关 watch），M3 路径根本没有被验证。`setRouteParams` 通过替换整个 `routeRef.value` 触发 reactivity，watcher 必触发。
      - 调用 `setRouteParams` 后 `await wrapper.vm.$nextTick() + flushPromises` 等待 watcher 回调完成再断言。
    - `beforeEach` 中 `mockFetchPositionDetail.mockClear()` + `routeRef.value = { params: { name: 'Backend-Engineer' }, query: {}, path: '/position/x', meta: {} }`（重置 route 状态到初始）

    **PositionDetail 新增测试用例（不删除现有 1 个）**
    - **loads position detail on mount**：默认 `mockFetchPositionDetail.mockResolvedValue({ name: 'Backend Engineer', name_cn: '后端工程师', industry: '互联网/IT', description: '', skills_required: [{ skill_id: 's1', name: 'Python', category: 'hard_skill', proficiency: '精通', confidence: 0.9, source_count: 5 }] })` → mount → `await flushPromises()` → 断言 `mockFetchPositionDetail` 被调用一次（参数 = `Backend-Engineer` 或 `position-id`）
    - **route param change refetches (M3)**：
      - **关键（C2-L2）**：用 `mockImplementation` 按入参返回不同 payload，避免 `mockResolvedValueOnce` 链 + `flushPromises` 中间态导致的第三次 fire 拿到第二次的 payload 而断言错位：
      ```ts
      mockFetchPositionDetail.mockImplementation((id: string) => {
        if (id === 'Frontend-Engineer') return Promise.resolve({ name: 'Frontend Engineer', industry: 'Y', skills_required: [] })
        return Promise.resolve({ name: 'Backend Engineer', industry: 'X', skills_required: [] })
      })
      ```
      → mount → `await flushPromises()` → 断言第 1 次入参为 `Backend-Engineer` → `setRouteParams({ name: 'Frontend-Engineer' })` → `await wrapper.vm.$nextTick() + flushPromises` → 断言 `mockFetchPositionDetail.mock.calls.length >= 2` 且 `mockFetchPositionDetail.mock.calls[1][0] === 'Frontend-Engineer'`
    - **proficiency tags render 精通/熟悉/了解 (L2)**: `mockFetchPositionDetail.mockResolvedValue({ name: 'X', skills_required: [{ skill_id: 's1', name: 'Python', category: 'hard_skill', proficiency: '精通', confidence: 0.9, source_count: 1 }] })` → mount → flushPromises → 断言渲染文本含「精通」（不出现 0.5 字符串）
    - **notFound when detail returns null (silent 404)**: `mockFetchPositionDetail.mockResolvedValue(null)` → mount → flushPromises → 断言 `.empty-guide` 或「未找到该岗位」文案存在

    **Backend: 补全 `test_position_industries_endpoint.py`**
    - 复用 `test_position_conformance.py:40-44` 的 `client_no_neo4j` fixture（或直接 import）
    - 测试 1 — `test_industries_endpoint_returns_distinct_sorted`：`mock_session` 返回 `[(row,)]` 列表（`PositionRecord.industry` distinct sort 后的值如 `["A", "B", "C"]`）→ `r = c.get('/api/v1/positions/industries')` → 断言 200、`r.json()['items']` 严格等于 `["A", "B", "C"]`
    - 测试 2 — `test_industries_filters_by_search_OR_industry_p_f2`：mock 捕获 SQL，断言包含 `OR industry`（P-F2 跨端一致）；空 `search` 时 SQL 不含 `OR`（baseline）
    - 测试 3 — `test_industries_public_defaults_to_approved_p_f1`：mock 捕获 SQL，断言 `effective_status='approved'` 注入；`include_all=true` 不注入
    - 测试 4 — `test_industries_escapes_like_wildcards`：**关键（C2-M3）** —— SQLAlchemy 渲染 `ilike(pattern, escape="\\")` 后参数走 bindparam（`%(param_1)s`），不在 SQL 字符串里，所以不能断言字面量 `a\\%b`。改为捕获 `mock_session.execute.call_args_list[i]` 的 `kwargs['params']` 字典，断言 `params['search'] == "a\\%b"`（即用户传入的 search 已被 `_escape_like` 转义，bindparam 字典中保留转义后的字符串）。可参考 `test_position_conformance.py:48-50` 已有的 keyword 断言模式（`" or "`, `"industry"`, `"name"`），但本测试只检查 search 字面值是否被转义。

    **L4 — 不新增「空状态文案优化」「错误重试按钮」等体验项测试**（review §3 评 ROI 低，5/5 项中 4 项仅装饰）

    **不做的**
    - 不修改 `test_position_conformance.py`（已锁 P-F1/P-F2 跨端契约，2-VALIDATION.md §1 三端同值已验证）
    - 不修改 `SkillRadar.vue`（保持 stub 即可，data.length >= 3 阈值已正确）
    - 不新增「CATEGORY_LABELS 文案 i18n」「分页可访问性」等
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/PositionList.spec.ts src/pages/__tests__/PositionDetail.spec.ts --reporter=verbose 2>&1 | tail -40 && cd ../backend && poetry run pytest tests/integration/test_position_conformance.py tests/integration/test_position_industries_endpoint.py -v --no-header 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `PositionList.spec.ts` uses `vi.mock('@/stores/jd', ...)` to stub `useJdStore`; onMounted no longer triggers real axios; `useAuthBootstrap` dead mock removed.
    - `PositionList.spec.ts` adds at least 4 new test cases: search input triggers debounced fetchPositions; industry chip click triggers fetchPositions with reset page; admin status badge reflects actual review_status (pending_review, null→未分类); empty state visible at total=0; pagination visible at total>pageSize.
    - `PositionDetail.spec.ts` uses `vi.mock('@/stores/jd', ...)` to stub `useJdStore.fetchPositionDetail`; onMounted no longer triggers real axios; `useAuthBootstrap` dead mock removed.
    - `PositionDetail.spec.ts` adds at least 3 new test cases: route param change refetches; proficiency tag renders 精通/熟悉/了解 (no 0.5 leakage); notFound friendly state when detail returns null.
    - `test_position_industries_endpoint.py` runs and asserts 4 cases: distinct+sort order, P-F2 search OR baseline, P-F1 public approved default, `_escape_like` wildcard escape. All pass.
    - `test_position_conformance.py` still 4 passed (P-F1, P-F2, include_all no-op, public approved).
    - `cd frontend && npx vitest run src/pages/__tests__/PositionList.spec.ts src/pages/__tests__/PositionDetail.spec.ts` exits 0; full `npx vitest run` (run separately if needed) reports no new failures.
    - `cd backend && poetry run pytest tests/integration/test_position_conformance.py tests/integration/test_position_industries_endpoint.py` exits 0.
  </acceptance_criteria>
  <done>
    - 两个 spec 文件通过 vi.mock('@/stores/jd', ...) 隔离网络，新增 7+ 个行为用例
    - search/industry 控件触发 refetch 有覆盖
    - PositionDetail 路由参数变化 refetch 有覆盖（M3）
    - admin status badge 反映真实 review_status（M5）有覆盖
    - L2 死分支（0.5 fallback）回归有覆盖
    - /positions/industries 端点契约锁就位（P-F1/P-F2/排序/转义）
    - 既有 P-F1/P-F2 回归锁不变绿
    - pytest + vitest 全绿
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → API | Untrusted user input crosses here (search query, industry filter, page/page_size, status filter) |
| API → PostgreSQL | search/industry parameters use LIKE with `_escape_like`; distinct industries query uses simple SELECT |
| API → Neo4j | position_id and search params are parameterized queries via `$param` placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | position.py `list_positions` search parameter | medium | mitigate | `_escape_like()` already escapes `%` and `_` in LIKE wildcards. Search is read-only, no mutation risk. P-F1/P-F2 locked by `test_position_conformance.py:48-83`. |
| T-02-02 | Tampering | position.py `get_position` position_id parameter | medium | mitigate | UUID validation (line 174-180) + name lookup fallback, both use parameterized queries. No SQL injection risk. |
| T-02-03 | Denial of Service | `list_positions` Neo4j fallback with empty search query | low | accept | Neo4j fallback only triggers when PG count is 0, limiting blast radius. |
| T-02-04 | Information Disclosure | Position list endpoint returns all positions | low | accept | Position data is intentionally public within the application. Status filtering for admin only. |
| T-02-05 | Tampering | `_list_positions_neo4j` Cypher query construction | low | mitigate | All user inputs use parameterized `$param` syntax. `where_str` only uses `AND`-joined clauses with no direct interpolation. |
| T-02-06 | Tampering | position.py NEW `list_industries` search parameter | medium | mitigate | Inherits `_escape_like()` and P-F2 OR-matching from `list_positions`. New test `test_position_industries_endpoint.py::test_industries_filters_by_search_OR_industry_p_f2` locks the contract. |
| T-02-07 | Information Disclosure | position.py `list_industries` discloses industry set | low | accept | Same threat profile as `list_positions` (T-02-04); industry list is already inferable from list endpoint. |
| T-02-08 | Information Disclosure | Neo4j fallback `PositionNode` omits `review_status` (M1) | medium | mitigate | Task 2 M1 fix writes `review_status=props.get("review_status")` so admin status badge reflects truth. Coupled with Task 2 M5 `?? 'approved'` removal on frontend. |
| T-02-09 | Spoofing | Stale data shown on route param change in PositionDetail (M3) | low | mitigate | Task 2 M3 adds `watch(() => route.params.name, refetch)`; `myToken !== fetchToken` guard preserved. |
| T-02-10 | Tampering | Dead-code surface: `fetchPositionSkills` (M2) | low | accept | JSDoc `@deprecated`; route retained for E2E/contract tests. No production caller (verified via grep). |
</threat_model>

<verification>
## Phase Verification

1. **Code correctness**: All modified files pass lint (ruff + ESLint)
   - `cd backend && poetry run ruff check --quiet backend/app/api/v1/position.py backend/tests/integration/test_position_industries_endpoint.py`
   - `cd frontend && npx eslint --quiet src/pages/PositionList.vue src/pages/PositionDetail.vue src/stores/jd.ts src/pages/__tests__/PositionList.spec.ts src/pages/__tests__/PositionDetail.spec.ts`
2. **Test pass**: `cd backend && poetry run pytest -x --no-header -q` passes (no regression vs current baseline)
3. **Test pass**: `cd frontend && npx vitest run --reporter=verbose` passes
4. **Conformance lock intact**: `cd backend && poetry run pytest tests/integration/test_position_conformance.py -v --no-header` shows 4 passed (P-F1, P-F2, include_all no-op, public approved)
5. **New endpoint contract locked**: `cd backend && poetry run pytest tests/integration/test_position_industries_endpoint.py -v --no-header` shows 4 passed (distinct+sort, P-F2 OR baseline, P-F1 public approved, _escape_like wildcard)
6. **Reactivity verified**: Playwright/manual screenshot shows search input → list refetch within 1s of stopping typing; industry chip click → list refetch; route change from /position/A to /position/B → PositionDetail refetches skills
7. **M1 verified**: Backend integration test mocks Neo4j driver returning Position with `review_status='pending_review'`; assert response includes `review_status='pending_review'`
8. **M5 verified**: PositionList spec asserts `review_status='pending_review'` renders 「待审核」 chip, NOT 「已发布」; null renders 「未分类」
</verification>

<success_criteria>
## Success Criteria

1. [ ] `docs/archive/position-source-analysis.md` 已重写，区分 invariant / 当前真实缺陷 / 已归档项
2. [ ] `GET /api/v1/positions/industries` 端点 live，返回数据库级 distinct sort 后的 industry 列表
3. [ ] PositionList 的 el-input 输入触发 debounced fetchPositions (300ms)
4. [ ] PositionList 的 industry el-tag 点击触发 fetchPositions + page 重置 1
5. [ ] PositionList 移除 `filteredPositions` 客户端过滤（后端已做完整过滤）
6. [ ] PositionList 不再用 `?? 'approved'` 掩盖 review_status（M5）
7. [ ] Neo4j fallback `PositionNode` 写回 `review_status`（M1）
8. [ ] PositionDetail 监听 `route.params.name` 变化 refetch（M3）
9. [ ] `jdStore.fetchPositionSkills` 标 `@deprecated`（M2），生产代码无调用方（grep 验证）
10. [ ] `PROFICIENCY_MAP[s.proficiency]` 移除 `?? 0.5` 死分支（L2）
11. [ ] `CATEGORY_LABELS` 未知 category 渲染为「其他」（L3）
12. [ ] `PositionList.spec.ts` 通过 `vi.mock('@/stores/jd')` 隔离网络，新增 ≥4 个行为用例
13. [ ] `PositionDetail.spec.ts` 通过 `vi.mock('@/stores/jd')` 隔离网络，新增 ≥3 个行为用例
14. [ ] `backend/tests/integration/test_position_industries_endpoint.py` 4 passed
15. [ ] `backend/tests/integration/test_position_conformance.py` 仍 4 passed（P-F1/P-F2 不回退）
16. [ ] `pytest` + `vitest` 全绿
17. [ ] `ruff` + `eslint` 双绿
</success_criteria>

<output>
Create `.planning/phases/02-position-module/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Modified
- `docs/archive/position-source-analysis.md` — re-anchored source analysis (Task 1); flags H2/H3/M1-M5/L2/L3, marks already-fixed items as invariants
- `backend/app/api/v1/position.py` — adds `list_industries` route + `IndustriesResponse` model (Task 2 H3); adds `review_status=props.get("review_status")` to `_list_positions_neo4j` `PositionNode(...)` (Task 2 M1)
- `frontend/src/stores/jd.ts` — adds `@deprecated` JSDoc on `fetchPositionSkills` (Task 2 M2)
- `frontend/src/pages/PositionList.vue` — adds `watch(searchQuery)` debounced refetch (Task 2 H2); adds `onIndustryChange` helper + wires to industry el-tag clicks; removes `filteredPositions` computed; replaces `?? 'approved'` with `?? null` + admin `未分类` status label (Task 2 H2/M5)
- `frontend/src/pages/PositionDetail.vue` — extracts `loadDetail` from `onMounted`; adds `watch(() => route.params.name, refetch)` (Task 2 M3); removes `?? 0.5` from `PROFICIENCY_MAP[s.proficiency]` (Task 2 L2); replaces `?? row.category` with `?? '其他'` (Task 2 L3)
- `frontend/src/pages/__tests__/PositionList.spec.ts` — adds `vi.mock('@/stores/jd', ...)` mock factory; adds 4+ behavior tests (Task 3 M4)
- `frontend/src/pages/__tests__/PositionDetail.spec.ts` — adds `vi.mock('@/stores/jd', ...)` mock factory; adds 3+ behavior tests (Task 3 M4)

### New
- `backend/tests/integration/test_position_industries_endpoint.py` — locks `/positions/industries` shape (distinct+sort, P-F2 OR baseline, P-F1 public approved, `_escape_like` wildcard)

### New Symbols / Endpoints
- Backend: `GET /api/v1/positions/industries?search=&industry=&status=&include_all=` — returns `IndustriesResponse { items: list[str] }`
- Backend Pydantic: `class IndustriesResponse(BaseModel): items: list[str]`
- Backend: `async def list_industries(session, page, page_size, industry, search, status, include_all) -> dict[str, list[str]]`
- Frontend store: `fetchPositionSkills` JSDoc `@deprecated` (no signature change)
- Frontend page: `function onIndustryChange()` in PositionList.vue
- Frontend page: `function loadDetail(id: string)` in PositionDetail.vue
- Frontend spec helper: `setRouteParams(p: Record<string, string>)` exported from `PositionDetail.spec.ts` for route-change tests
- Frontend spec mock: `mockFetchPositions`, `mockFetchPositionDetail` (vi.fn instances shared across `vi.mock('@/stores/jd', ...)`)
- Frontend spec helper: `mockRouteRef` — `const routeRef = ref({ params: { name: 'Backend-Engineer' }, query: {}, path: '/position/x', meta: {} })` shared across `vi.mock('vue-router', ...)` factory; replaced whole-object via `setRouteParams` to trigger Vue reactivity (C2-L1 mitigation; plain-object mutation does NOT trigger `watch`)
- Frontend page: `const searchDebounceTimer = ref<ReturnType<typeof setTimeout> | undefined>(undefined)` inside PositionList.vue `<script setup>` (C2-M1 mitigation; module-scope `let` avoided to prevent cross-instance leak in SPAs)
</content>
</invoke>