# 11-03: JD 抽取 → PositionRecord 自动创建

**Phase:** 11-feature-loop-closure
**Wave:** 1
**Depends on:** none
**Requirements:** LOOP-05
**Decision refs:** D-10

## Goal

JD 抽取成功后自动创建/更新 PostgreSQL `PositionRecord`，使 `/positions` 端点可查到抽取的岗位。

## Tasks

### Task 1: 新增 _write_extraction_to_pg 函数
- **Type:** code
- **Files:** `backend/app/api/v1/extract.py`
- **Details:**
  1. 新增 `_write_extraction_to_pg()` 异步函数：
     ```python
     async def _write_extraction_to_pg(
         pipeline_result: dict[str, Any],
         session: AsyncSession,
     ) -> bool | None:
         """Write extraction result to PostgreSQL PositionRecord. Returns True on success, None on failure."""
         from app.models.extraction_models import PositionRecord, SkillRecord
         import sqlalchemy as sa
     
         data = pipeline_result.get("data")
         if not data or not data.get("position_name"):
             logger.debug("Skipping PG write: no extraction data or position_name")
             return None
     
         position_name = data["position_name"]
     
         try:
             # Upsert PositionRecord
             await session.execute(
                 sa.text("""
                     INSERT INTO position_records (name, source, created_at, updated_at)
                     VALUES (:name, 'extraction', NOW(), NOW())
                     ON CONFLICT (name) DO UPDATE SET updated_at = NOW(), source = 'extraction'
                 """),
                 {"name": position_name},
             )
     
             # Upsert SkillRecords for extracted skills
             all_skills = []
             for s in data.get("required_skills", []):
                 name = s.get("skill") or s.get("name") if isinstance(s, dict) else str(s)
                 if name:
                     all_skills.append(name)
             for s in data.get("preferred_skills", []):
                 name = s.get("skill") or s.get("name") if isinstance(s, dict) else str(s)
                 if name:
                     all_skills.append(name)
     
             for skill_name in set(all_skills):
                 await session.execute(
                     sa.text("""
                         INSERT INTO skill_records (name, category, created_at, updated_at)
                         VALUES (:name, 'hard_skill', NOW(), NOW())
                         ON CONFLICT (name) DO UPDATE SET updated_at = NOW()
                     """),
                     {"name": skill_name},
                 )
     
             await session.commit()
             logger.info("PG write complete: PositionRecord '{}' + {} skills upserted", position_name, len(set(all_skills)))
             return True
         except Exception as e:
             await session.rollback()
             logger.warning("PG write failed (non-blocking): {}", e)
             return None
     ```
- **Test:** 验证函数在 pipeline_result 有效时插入 PositionRecord

### Task 2: 修改 extract_jd 端点签名和调用
- **Type:** code
- **Files:** `backend/app/api/v1/extract.py`
- **Details:**
  1. 在 `extract_jd()` 函数签名中添加 `session` 依赖：
     ```python
     async def extract_jd(
         request: ExtractionRequest,
         neo4j_driver: Any = Depends(get_neo4j_driver),
         session: AsyncSession = Depends(get_db_session),  # 新增
     ) -> dict[str, Any]:
     ```
  2. 在 `_write_extraction_to_graph()` 调用后，新增 `_write_extraction_to_pg()` 调用：
     ```python
     # Write extraction to Neo4j graph (non-blocking)
     graph_summary = await _write_extraction_to_graph(pipeline_result, neo4j_driver)
     if graph_summary:
         logger.info("Graph integration: {} triples written", graph_summary["triples_merged"])
     
     # Write extraction to PostgreSQL (non-blocking)
     pg_result = await _write_extraction_to_pg(pipeline_result, session)
     if pg_result:
         logger.info("PG integration: PositionRecord created")
     ```
  3. 同样修改 `extract_resume()` 端点添加 session 依赖和 PG 写入
- **Test:** `POST /extract/jd` 成功后，`GET /positions` 可查到抽取的岗位名称

### Task 3: 添加 AsyncSession import
- **Type:** code
- **Files:** `backend/app/api/v1/extract.py`
- **Details:**
  1. 添加 import：
     ```python
     from sqlalchemy.ext.asyncio import AsyncSession
     from app.dependencies import get_db_session
     ```
- **Test:** `ruff check` + `mypy` 通过

## Verification
- [ ] `POST /extract/jd` 成功后 `PositionRecord` 表新增记录
- [ ] `GET /positions` 返回包含刚抽取的岗位
- [ ] 重复抽取同一岗位不报错（ON CONFLICT DO UPDATE）
- [ ] `SkillRecord` 表同步更新
- [ ] PG 写入失败不影响抽取结果返回（非阻塞）
- [ ] `cd backend && poetry run ruff check app/api/v1/extract.py && poetry run mypy app/api/v1/extract.py`

## Rollback
移除 `_write_extraction_to_pg()` 函数和调用，恢复 `extract_jd()` 原始签名。
