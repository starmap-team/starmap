# 11-01: 认证登录端点 + 登录页面

**Phase:** 11-feature-loop-closure
**Wave:** 1
**Depends on:** none
**Requirements:** LOOP-01
**Decision refs:** D-01, D-02, D-03

## Goal

新增 `POST /auth/login` 后端端点和 `Login.vue` 前端页面，打通 JWT 认证全链路，使生产环境可登录。

## Tasks

### Task 1: 后端配置新增 AUTH_USERS 和 TOKEN_EXPIRE_HOURS
- **Type:** code
- **Files:** `backend/app/config.py`
- **Details:**
  1. 在 `Settings` 类中新增两个字段：
     ```python
     auth_users: str = Field(default="", description="用户列表，格式: username:password:role,username2:password2:role2")
     token_expire_hours: int = Field(default=24, ge=1, le=720, description="JWT token 有效期（小时）")
     ```
  2. 新增 property 方法 `parsed_users` 解析 `auth_users` 字符串为 `list[dict]`：
     ```python
     @property
     def parsed_users(self) -> list[dict[str, str]]:
         if not self.auth_users:
             return []
         users = []
         for entry in self.auth_users.split(","):
             parts = entry.strip().split(":")
             if len(parts) == 3:
                 users.append({"username": parts[0], "password": parts[1], "role": parts[2]})
         return users
     ```
  3. 在 `.env.example` 中补全：`AUTH_USERS=admin:starmap2024:admin,demo:demo123:user` 和 `TOKEN_EXPIRE_HOURS=24`
- **Test:** 在 Python 中验证 `Settings(auth_users="admin:pass:admin").parsed_users` 返回正确列表

### Task 2: 新增 POST /auth/login 端点
- **Type:** code
- **Files:** `backend/app/api/v1/auth.py` (new), `backend/app/api/v1/router.py`
- **Details:**
  1. 创建 `backend/app/api/v1/auth.py`：
     ```python
     """认证 API：用户登录、token 签发。"""
     from __future__ import annotations
     import base64, hashlib, hmac, json, time
     from fastapi import APIRouter, HTTPException, status
     from pydantic import BaseModel, Field
     from app.config import settings
     from app.dependencies import _decode_token
     
     router = APIRouter(prefix="/auth", tags=["认证"])
     
     class LoginRequest(BaseModel):
         username: str = Field(..., min_length=1, description="用户名")
         password: str = Field(..., min_length=1, description="密码")
     
     class LoginResponse(BaseModel):
         token: str = Field(..., description="JWT token")
         user: dict = Field(..., description="用户信息")
     
     def _encode_jwt(payload: dict[str, str | int | float]) -> str:
         """手动签发 JWT（HMAC-SHA256），与 _decode_token 验证逻辑一致。"""
         header = base64.urlsafe_b64encode(
             json.dumps({"alg": "HS256", "typ": "JWT"}).encode()
         ).rstrip(b"=").decode()
         payload_b64 = base64.urlsafe_b64encode(
             json.dumps(payload).encode()
         ).rstrip(b"=").decode()
         signing_input = f"{header}.{payload_b64}".encode()
         sig = hmac.new(settings.secret_key.encode(), signing_input, hashlib.sha256).digest()
         signature = base64.urlsafe_b64encode(sig).rstrip(b"=").decode()
         return f"{header}.{payload_b64}.{signature}"
     
     @router.post("/login", response_model=LoginResponse)
     async def login(request: LoginRequest) -> dict:
         """用户登录，验证凭据后签发 JWT token。"""
         users = settings.parsed_users
         matched = None
         for u in users:
             if u["username"] == request.username and u["password"] == request.password:
                 matched = u
                 break
         if not matched:
             raise HTTPException(status_code=401, detail="用户名或密码错误")
         now = time.time()
         payload = {
             "sub": matched["username"],
             "role": matched["role"],
             "username": matched["username"],
             "exp": now + settings.token_expire_hours * 3600,
             "iat": now,
         }
         token = _encode_jwt(payload)
         return {
             "token": token,
             "user": {"sub": payload["sub"], "role": payload["role"], "username": payload["username"]},
         }
     ```
  2. 在 `router.py` 中注册：`api_router.include_router(auth.router)` — 注意此路由不需要 `get_current_user` 依赖
- **Test:** `curl -X POST http://localhost:8000/api/v1/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"starmap2024"}'` 返回 200 + token

### Task 3: 新增 Login.vue 登录页面
- **Type:** code
- **Files:** `frontend/src/pages/Login.vue` (new)
- **Details:**
  1. 创建 Element Plus 表单页面：
     ```vue
     <script setup lang="ts">
     import { ref } from 'vue'
     import { useRouter } from 'vue-router'
     import { ElMessage } from 'element-plus'
     import { useUserStore } from '@/stores/user'
     import request from '@/api/request'
     
     const router = useRouter()
     const userStore = useUserStore()
     const username = ref('')
     const password = ref('')
     const loading = ref(false)
     
     async function handleLogin() {
       if (!username.value || !password.value) {
         ElMessage.warning('请输入用户名和密码')
         return
       }
       loading.value = true
       try {
         const data = await request.post('/auth/login', {
           username: username.value,
           password: password.value,
         }) as { token: string; user: { sub: string; role: string; username: string } }
         localStorage.setItem('starmap_token', data.token)
         userStore.initUser()
         ElMessage.success('登录成功')
         router.push('/')
       } catch (e: unknown) {
         ElMessage.error('登录失败: ' + (e instanceof Error ? e.message : '用户名或密码错误'))
       } finally {
         loading.value = false
       }
     }
     </script>
     
     <template>
       <div class="login-page">
         <div class="login-card">
           <h2 class="login-title">⭐ StarMap 星图</h2>
           <p class="login-subtitle">人才能力星云导航系统</p>
           <el-form @submit.prevent="handleLogin" class="login-form">
             <el-form-item>
               <el-input v-model="username" placeholder="用户名" prefix-icon="User" size="large" />
             </el-form-item>
             <el-form-item>
               <el-input v-model="password" type="password" placeholder="密码" prefix-icon="Lock" size="large" show-password @keyup.enter="handleLogin" />
             </el-form-item>
             <el-form-item>
               <el-button type="primary" size="large" :loading="loading" @click="handleLogin" style="width: 100%">登 录</el-button>
             </el-form-item>
           </el-form>
         </div>
       </div>
     </template>
     
     <style scoped>
     .login-page { display: flex; justify-content: center; align-items: center; min-height: 100vh; background: var(--el-bg-color); }
     .login-card { width: 400px; padding: 40px; border-radius: 12px; background: var(--el-bg-color-page); box-shadow: var(--el-box-shadow-light); }
     .login-title { text-align: center; margin-bottom: 8px; font-size: 24px; color: var(--el-text-color-primary); }
     .login-subtitle { text-align: center; margin-bottom: 32px; color: var(--el-text-color-secondary); font-size: 14px; }
     </style>
     ```
- **Test:** 访问 `/login` 显示登录表单，输入凭据后跳转首页

### Task 4: 修改 /login 路由指向 Login.vue
- **Type:** code
- **Files:** `frontend/src/router/index.ts`
- **Details:**
  1. 找到 `/login` 路由定义，将 `component` 从 `Home.vue` 改为 lazy-load `Login.vue`：
     ```ts
     { path: '/login', name: 'Login', component: () => import('@/pages/Login.vue'), meta: { requiresAuth: false } }
     ```
  2. 确保 `/login` 路由没有 `requiresAuth: true`（登录页不需要认证）
- **Test:** 未登录时访问任意受保护路由自动跳转到 `/login`，登录页正常显示

### Task 5: user store 新增 login action
- **Type:** code
- **Files:** `frontend/src/stores/user.ts`
- **Details:**
  1. 新增 `login(username: string, password: string)` async action
  2. 调用 `request.post('/auth/login', { username, password })`
  3. 成功后 `localStorage.setItem('starmap_token', data.token)` + `initUser()`
- **Test:** Login.vue 使用此 action 后正常工作

## Verification
- [ ] `POST /auth/login` 返回 JWT token（有效用户）
- [ ] `POST /auth/login` 返回 401（无效用户）
- [ ] `_decode_token()` 可正确解码签发的 token
- [ ] `/login` 页面可登录并跳转首页
- [ ] 登录后 localStorage 有 `starmap_token`
- [ ] 未登录访问受保护路由跳转 `/login`
- [ ] `cd backend && poetry run ruff check app/api/v1/auth.py && poetry run mypy app/api/v1/auth.py`
- [ ] `cd frontend && npm run typecheck`

## Rollback
删除 `auth.py`、`Login.vue`，恢复 `router/index.ts` 的 `/login` 路由，移除 `config.py` 新增字段。
