---
phase: 05-style-unify
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/design-tokens.css
  - frontend/src/components/NodeTooltip3D.vue
autonomous: true
requirements:
  - COLOR-03
  - STYLE-04

must_haves:
  truths:
    - "NodeTooltip3D.vue uses CSS variables for the 3 Slate-tier colors and keeps the existing TYPE_INFO fallback intact"
    - "design-tokens.css exposes --slate-200, --slate-400, --slate-500 in :root"
    - "Frontend type-check passes after the change"
  artifacts:
    - path: "frontend/src/styles/design-tokens.css"
      provides: "--slate-200, --slate-400, --slate-500 tokens in :root"
      contains: "--slate-200:"
    - path: "frontend/src/components/NodeTooltip3D.vue"
      provides: "var(--slate-*) references for tt-name, tt-stat-label, tt-stat-value"
      min_lines: 170
  key_links:
    - from: "frontend/src/components/NodeTooltip3D.vue"
      to: "frontend/src/styles/design-tokens.css"
      via: "CSS var references in <style scoped>"
      pattern: "var\\(--slate-(200|400|500)\\)"
---

<objective>
Add `--slate-200/400/500` to `design-tokens.css` (D-13) and replace the three Slate-tier hex literals in `NodeTooltip3D.vue` with those CSS vars (D-12), keeping the `TYPE_INFO[...].color` fallback untouched (D-14 transparency-concat pattern preserved).

Purpose: Closes COLOR-03 and STYLE-04. After this plan, the only hex inside `NodeTooltip3D.vue` is the single `TYPE_INFO` fallback at line 30 (which itself is a known fallback, not a hardcoded style choice). Future Slate tone changes happen in design tokens, not in components.

Output: 3 CSS variables added; 3 hex replaced; vue-tsc still clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-style-unify/05-CONTEXT.md (D-12 / D-13 / D-14)
@frontend/src/styles/design-tokens.css (existing :root structure to extend)
@frontend/src/components/NodeTooltip3D.vue (target file)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --slate-* tokens to design-tokens.css</name>
  <read_first>
    - frontend/src/styles/design-tokens.css (verify no --slate-* already exists; observe the existing :root block pattern for spacing/typography tokens to mirror style)
    - frontend/src/components/NodeTooltip3D.vue (confirm the exact three Slate hex values needing tokens)
  </read_first>
  <files>frontend/src/styles/design-tokens.css</files>
  <action>
Per D-13, extend `design-tokens.css` with `--slate-200/400/500` tokens. Insertion location: inside the existing `:root { ... }` block, in a new comment-block titled exactly `Slate scale (Tailwind-compatible)` placed immediately after the spacing scale block (which already exists in the file) and before the typography/radius/shadow blocks. Add a single 3-line token group with the exact values below (these are the slate tones used in NodeTooltip3D — no extras, no expansion to the full Tailwind scale per D-07 "do not add other graph-related tokens"):

```
--slate-200: #e2e8f0;
--slate-400: #94a3b8;
--slate-500: #64748b;
```

Do NOT add `--slate-100/300/600/700/800/900`. Do NOT add any other color tokens. Do NOT remove or refactor existing tokens. Do NOT add a dark-theme override block for slate — the file's dark theme is opt-in via `html.dark` and these tones are intentionally static. Do NOT move the tokens into `html.dark`; D-07 keeps token extension minimal.
  </action>
  <verify>
    <automated>grep -nE -- "--slate-(200|400|500):" frontend/src/styles/design-tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -nE -- "--slate-(200|400|500):" frontend/src/styles/design-tokens.css returns exactly 3 lines
    - Token values match exactly: --slate-200: #e2e8f0, --slate-400: #94a3b8, --slate-500: #64748b
    - No other --slate-* tier introduced (verify: grep -cE -- "--slate-(100|300|600|700|800|900):" frontend/src/styles/design-tokens.css returns 0)
    - Tokens declared inside :root block (verify: head -1 frontend/src/styles/design-tokens.css shows the file structure still parses — no syntax errors)
  </acceptance_criteria>
  <done>Three slate tokens declared in :root; no scope creep.</done>
</task>

<task type="auto">
  <name>Task 2: Replace 3 Slate hex in NodeTooltip3D.vue with var(--slate-*)</name>
  <read_first>
    - frontend/src/components/NodeTooltip3D.vue (full file — see line 30 TYPE_INFO fallback, lines 124 / 145 / 151 hex sites in <style scoped>)
    - frontend/src/styles/design-tokens.css (verify Task 1 tokens present before editing the component)
  </read_first>
  <files>frontend/src/components/NodeTooltip3D.vue</files>
  <action>
Per D-12, replace exactly three hex literals in `NodeTooltip3D.vue` `<style scoped>` block with `var(--slate-*)`. The exact line targets (read-first first to confirm line numbers):

- Line 124 (`.tt-name { color: #e2e8f0; }`) → replace `#e2e8f0` with `var(--slate-200)`.
- Line 145 (`.tt-stat-label { color: #64748b; }`) → replace `#64748b` with `var(--slate-500)`.
- Line 151 (`.tt-stat-value { color: #94a3b8; }`) → replace `#94a3b8` with `var(--slate-400)`.

Do NOT touch line 30 (`?? { label: ..., color: '#64748b' }`) — this is the legitimate `TYPE_INFO` fallback for unknown node types per D-14 transparency-concat pattern (`typeInfo.color + '22'`). Do NOT touch lines 98, 101, 104 (these are `rgba(10, 14, 26, 0.92)` and similar alpha literals — they are NOT Slate-tier hex and are out of scope). Do NOT touch the `border-color` / `box-shadow` rgba on lines 101 / 104. Do NOT refactor any selector. Do NOT change `<script setup>`.
  </action>
  <verify>
    <automated>grep -nE "var\(--slate-(200|400|500)\)" frontend/src/components/NodeTooltip3D.vue && grep -nE "#[0-9a-fA-F]{3,8}" frontend/src/components/NodeTooltip3D.vue</automated>
  </verify>
  <acceptance_criteria>
    - NodeTooltip3D.vue contains `var(--slate-200)` exactly once (line 124 area)
    - NodeTooltip3D.vue contains `var(--slate-500)` exactly once (line 145 area)
    - NodeTooltip3D.vue contains `var(--slate-400)` exactly once (line 151 area)
    - Hex literal grep returns 1 match total: line 30 fallback `'#64748b'` only (verify: grep -nE "#[0-9a-fA-F]{3,8}" frontend/src/components/NodeTooltip3D.vue | wc -l returns 1)
    - vue-tsc --noEmit exits 0 (run: cd frontend && npx vue-tsc --noEmit)
  </acceptance_criteria>
  <done>Three hex replaced with CSS vars; TYPE_INFO fallback preserved; type check clean.</done>
</task>

</tasks>

<verification>
1. `grep -nE -- "--slate-(200|400|500):" frontend/src/styles/design-tokens.css` — exactly 3 matches
2. `grep -nE "#[0-9a-fA-F]{3,8}" frontend/src/components/NodeTooltip3D.vue` — exactly 1 match (TYPE_INFO fallback on line 30)
3. `grep -nE "var\(--slate-" frontend/src/components/NodeTooltip3D.vue` — exactly 3 matches
4. `cd frontend && npx vue-tsc --noEmit` — exits 0
</verification>

<success_criteria>
- Slate tones flow through design-tokens.css; NodeTooltip3D.vue has no hex literals outside the preserved TYPE_INFO fallback.
- Tailwind scale NOT expanded beyond the three used tiers.
</success_criteria>

<output>
Create `.planning/phases/05-style-unify/05-02-SUMMARY.md` when done.
</output>