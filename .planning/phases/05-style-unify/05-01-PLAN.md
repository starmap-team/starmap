---
phase: 05-style-unify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/utils/graphColors.ts
  - frontend/src/pages/DataDashboard.vue
  - frontend/src/stores/quality.ts
autonomous: true
requirements:
  - COLOR-01
  - COLOR-02
  - COLOR-04
  - STYLE-01
  - STYLE-03

must_haves:
  truths:
    - "DataDashboard.vue contains no inline hex color literals (excluding CSS comments)"
    - "graphColors.ts exports a named ECHARTS_PALETTE constant consumed by DataDashboard"
    - "stores/quality.ts contains no inline hex color literals for KPI cards"
    - "Frontend type-check + lint pass after the change"
    - "D-04: Migration happens in a single plan window — no intermediate state with mixed hex + ECHARTS_PALETTE across DataDashboard.vue / stores/quality.ts"
  artifacts:
    - path: "frontend/src/utils/graphColors.ts"
      provides: "ECHARTS_PALETTE (PIE_BORDER='#0a0a1a', LABEL='#ffffff', KPI palette)"
      contains: "export const ECHARTS_PALETTE"
    - path: "frontend/src/pages/DataDashboard.vue"
      provides: "ECharts option objects using ECHARTS_PALETTE for borders/labels"
      min_lines: 1200
    - path: "frontend/src/stores/quality.ts"
      provides: "KPI color values imported from graphColors.ts ECHARTS_PALETTE.KPI"
  key_links:
    - from: "frontend/src/pages/DataDashboard.vue"
      to: "frontend/src/utils/graphColors.ts"
      via: "import { ECHARTS_PALETTE } from '@/utils/graphColors'"
      pattern: "ECHARTS_PALETTE\\."
    - from: "frontend/src/stores/quality.ts"
      to: "frontend/src/utils/graphColors.ts"
      via: "import { ECHARTS_PALETTE } from '@/utils/graphColors'"
      pattern: "ECHARTS_PALETTE\\.KPI"
---

<objective>
Migrate the remaining ECharts hex literals in `DataDashboard.vue` and KPI hex literals in `stores/quality.ts` to a new `ECHARTS_PALETTE` constant exported from `graphColors.ts`, satisfying D-01 / D-02 / D-03 (batch migration, semantic isolation from NODE_TYPE_COLORS).

Purpose: Closes COLOR-01 → COLOR-04 / STYLE-01 / STYLE-03 by removing the last ~9 hardcoded hex in the dashboard surface without per-PR churn. Single edit window keeps the color source unified.

Output: `graphColors.ts` gains `ECHARTS_PALETTE`; `DataDashboard.vue` and `stores/quality.ts` reference it; `vue-tsc` + `eslint` still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-style-unify/05-CONTEXT.md
@.planning/phases/03-frontend-closure/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ECHARTS_PALETTE constant to graphColors.ts</name>
  <read_first>
    - frontend/src/utils/graphColors.ts (current single source — see existing DOMAIN_COLORS / SCENE_PALETTE shapes)
    - frontend/src/utils/chartTheme.ts (existing ECharts helper, kept untouched per D-05/D-06)
    - frontend/src/pages/DataDashboard.vue lines 130-260 (exact hex usage sites for PIE_BORDER and LABEL)
    - frontend/src/stores/quality.ts lines 100-120 (4 KPI hex literals)
  </read_first>
  <files>frontend/src/utils/graphColors.ts</files>
  <action>
Per D-02 (semantic isolation from NODE_TYPE_COLORS) and D-07 (no graphColors coupling to CSS vars), add a new top-level export `ECHARTS_PALETTE` to `graphColors.ts`, placed after `SCENE_PALETTE` and before `toThreeHex`. Use the exact keys and values below (no re-grouping, no renaming — these are the only hex strings the executor needs to migrate):

- `PIE_BORDER: '#0a0a1a'` — used at DataDashboard.vue lines 142, 209, 215, 247 (borderColor for pie/treemap rings matching dark background).
- `LABEL: '#ffffff'` — used at DataDashboard.vue line 204 (treemap label color, with textShadow on dark tiles).
- `KPI: ['#409eff', '#67c23a', '#e6a23c', '#f56c6c']` — used at stores/quality.ts lines 109-112 (Element-Plus-aligned KPI colors: total_nodes=blue, avg_trust_score=green, hallucination_rate=amber, pending_review=red). Document the index mapping with an inline comment.

Declare as `export const ECHARTS_PALETTE = { ... } as const` to mirror the existing `DOMAIN_COLORS` / `SCENE_PALETTE` shape (already proven pattern in the same file). No JSDoc narrative beyond a single-line block comment naming the consuming files so the next reviewer doesn't need to grep. Do NOT touch `NODE_TYPE_COLORS`, `EDGE_TYPE_COLORS`, `TYPE_INFO`, `DOMAIN_COLORS`, or `SCENE_PALETTE`. Do NOT add any helper function. Do NOT introduce `getComputedStyle` (D-06).
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | head -20 && grep -nE "export const ECHARTS_PALETTE" src/utils/graphColors.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/utils/graphColors.ts contains the literal line `export const ECHARTS_PALETTE`
    - graphColors.ts PIE_BORDER = '#0a0a1a' (verify: grep -nE "PIE_BORDER" frontend/src/utils/graphColors.ts)
    - graphColors.ts LABEL = '#ffffff' (verify: grep -nE "LABEL:" frontend/src/utils/graphColors.ts)
    - graphColors.ts KPI array contains exactly ['#409eff', '#67c23a', '#e6a23c', '#f56c6c'] in that order
    - vue-tsc --noEmit exits 0 (no new type errors from the addition)
    - NODE_TYPE_COLORS / EDGE_TYPE_COLORS / SCENE_PALETTE / DOMAIN_COLORS all still present and unchanged (verify: grep -cE "NODE_TYPE_COLORS|EDGE_TYPE_COLORS|SCENE_PALETTE|DOMAIN_COLORS" frontend/src/utils/graphColors.ts returns >=4)
  </acceptance_criteria>
  <done>`ECHARTS_PALETTE` exported with PIE_BORDER / LABEL / KPI; type check clean; existing palette entries untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate DataDashboard.vue and stores/quality.ts to ECHARTS_PALETTE</name>
  <read_first>
    - frontend/src/utils/graphColors.ts (the new ECHARTS_PALETTE export from Task 1)
    - frontend/src/pages/DataDashboard.vue lines 1-15 (existing imports), 130-260 (5 hex usage sites: lines 142, 204, 209, 215, 247)
    - frontend/src/stores/quality.ts lines 1-15 (existing imports), 100-120 (4 KPI hex literals at lines 109-112)
  </read_first>
  <files>frontend/src/pages/DataDashboard.vue, frontend/src/stores/quality.ts</files>
  <behavior>
    - DataDashboard.vue line 142 (borderColor) uses `ECHARTS_PALETTE.PIE_BORDER`
    - DataDashboard.vue line 204 (label.color) uses `ECHARTS_PALETTE.LABEL`
    - DataDashboard.vue line 209 (itemStyle.borderColor) uses `ECHARTS_PALETTE.PIE_BORDER`
    - DataDashboard.vue line 215 (itemStyle.borderColor) uses `ECHARTS_PALETTE.PIE_BORDER`
    - DataDashboard.vue line 247 (itemStyle.borderColor) uses `ECHARTS_PALETTE.PIE_BORDER`
    - stores/quality.ts lines 109-112 use `ECHARTS_PALETTE.KPI[0..3]` in declaration order (matches label list '节点总数','平均信任度','幻觉率','待审核')
  </behavior>
  <action>
Add `ECHARTS_PALETTE` to the existing `@/utils/graphColors` import in `DataDashboard.vue` (line ~7 area, merge with the existing named imports — do NOT add a second import line). Replace exactly 5 hex sites:

1. `frontend/src/pages/DataDashboard.vue`:
   - Add `ECHARTS_PALETTE` to the existing `import { ... } from '@/utils/graphColors'` line.
   - Line 142: replace `'#0a0a1a'` → `ECHARTS_PALETTE.PIE_BORDER`.
   - Line 204: replace `'#fff'` → `ECHARTS_PALETTE.LABEL`. (Note `'#fff'` is the short form — the spec is to use `ECHARTS_PALETTE.LABEL`, the resolved value is `'#ffffff'`.)
   - Line 209: replace `'#0a0a1a'` → `ECHARTS_PALETTE.PIE_BORDER`.
   - Line 215: replace `'#0a0a1a'` → `ECHARTS_PALETTE.PIE_BORDER`.
   - Line 247: replace `'#0a0a1a'` → `ECHARTS_PALETTE.PIE_BORDER`.
   - Do NOT modify any other line. Do NOT touch `cc.chart[*]` / `cc.foreground` / `cc.success` / `cc.warning` / `cc.danger` references (those already route through `chartTheme.ts` and are D-05/D-06 out of scope).

2. `frontend/src/stores/quality.ts`:
   - Add `import { ECHARTS_PALETTE } from '@/utils/graphColors'` at the top of the file with the existing imports.
   - Lines 109-112: replace the four literal color strings in declaration order: `'#409eff'` → `ECHARTS_PALETTE.KPI[0]`, `'#67c23a'` → `ECHARTS_PALETTE.KPI[1]`, `'#e6a23c'` → `ECHARTS_PALETTE.KPI[2]`, `'#f56c6c'` → `ECHARTS_PALETTE.KPI[3]`. Preserve label order exactly.

After editing, run the grep gate below to confirm zero non-comment hex remain in either file.
  </action>
  <verify>
    <automated>cd frontend && grep -nE "'#[0-9a-fA-F]{3,8}'" src/pages/DataDashboard.vue src/stores/quality.ts | grep -v "^[^:]*:[0-9]*:[[:space:]]*//" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep gate above returns `0` (zero non-comment hex string literals remain in the two files)
    - DataDashboard.vue contains `ECHARTS_PALETTE.PIE_BORDER` at >= 4 distinct line offsets (verify: grep -cE "ECHARTS_PALETTE\.PIE_BORDER" frontend/src/pages/DataDashboard.vue returns >= 4)
    - DataDashboard.vue contains `ECHARTS_PALETTE.LABEL` exactly once (verify: grep -cE "ECHARTS_PALETTE\.LABEL" frontend/src/pages/DataDashboard.vue returns 1)
    - stores/quality.ts contains `ECHARTS_PALETTE.KPI[0]`, `ECHARTS_PALETTE.KPI[1]`, `ECHARTS_PALETTE.KPI[2]`, `ECHARTS_PALETTE.KPI[3]` exactly once each (verify: grep -cE "ECHARTS_PALETTE\.KPI\[" frontend/src/stores/quality.ts returns 4)
    - vue-tsc --noEmit exits 0 (run: cd frontend && npx vue-tsc --noEmit)
  </acceptance_criteria>
  <done>Both files import `ECHARTS_PALETTE`; 9 hex literals replaced; grep gate is zero; type check clean.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vue-tsc --noEmit` — type check (must exit 0)
2. `grep -nE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/DataDashboard.vue frontend/src/stores/quality.ts | grep -v "//"` — must be empty
3. `grep -nE "ECHARTS_PALETTE" frontend/src/pages/DataDashboard.vue frontend/src/stores/quality.ts frontend/src/utils/graphColors.ts` — must show import + usage + definition
</verification>

<success_criteria>
- `ECHARTS_PALETTE` is the single source for ECharts border / label / KPI colors.
- DataDashboard.vue and stores/quality.ts contain zero hex string literals (excluding comments).
- vue-tsc + eslint both clean.
</success_criteria>

<output>
Create `.planning/phases/05-style-unify/05-01-SUMMARY.md` when done.
</output>