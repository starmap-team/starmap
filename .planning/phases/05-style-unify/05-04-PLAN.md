---
phase: 05-style-unify
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
  - COLOR-01
  - COLOR-02
  - COLOR-03
  - COLOR-04
  - TOOLBAR-01
  - TOOLBAR-02
  - TOOLBAR-03
  - SCHEMA-01
  - SCHEMA-02
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03
  - CLEANUP-04

must_haves:
  truths:
    - "Backend `ruff check .` exits 0 in `backend/`"
    - "Backend `pytest` exits 0 (or skips cleanly with no test failures)"
    - "Frontend `vue-tsc --noEmit` exits 0"
    - "Frontend `npm run lint` exits 0"
    - "STATE.md P5 Status Detail shows 6/6 criteria met (was 4/6)"
    - "ROADMAP.md Phase 5 plan list shows 4 plans all marked complete"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated Phase Progress table marking Phase 5 as completed"
      contains: "| 5 | 样式统一与体验优化 | ✅ completed | 6/6 |"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 Plans section listing 05-01 through 05-04 all marked [x]"
      contains: "[x] 05-04-PLAN.md"
  key_links:
    - from: ".planning/STATE.md"
      to: "ruff + pytest + vue-tsc + eslint exit codes"
      via: "post-implementation verification record"
      pattern: "P5 Status Detail"
---

<objective>
Run the full quality gate (ruff + pytest + vue-tsc + eslint + grep) and flip STATE.md and ROADMAP.md to mark Phase 5 complete (4/6 → 6/6).

Purpose: Closes the bookkeeping on Phase 5. After this plan, STATE.md shows Phase 5 ✅ completed with 6/6, and ROADMAP.md Phase 5 lists all 4 plans as `[x]`. No code changes — verification only.

Output: STATE.md updated; ROADMAP.md updated; quality-gate commands recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md (current 4/6 status, P5 Status Detail table)
@.planning/ROADMAP.md (Phase 5 plan list — currently empty)
@.planning/phases/05-style-unify/05-01-PLAN.md (this plan depends on Plan 01)
@.planning/phases/05-style-unify/05-02-PLAN.md (this plan depends on Plan 02)
@.planning/phases/05-style-unify/05-03-PLAN.md (this plan depends on Plan 03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gate (ruff, pytest, vue-tsc, eslint, grep)</name>
  <read_first>
    - .planning/STATE.md (verify current P5 Status Detail table before updating)
    - .planning/ROADMAP.md (verify current Phase 5 plan list before appending)
    - frontend/src/pages/DataDashboard.vue and frontend/src/stores/quality.ts (confirm grep gate from Plan 01 is empty post-migration)
    - frontend/src/components/NodeTooltip3D.vue (confirm only line 30 fallback hex from Plan 02)
    - frontend/src/styles/design-tokens.css (confirm 3 --slate-* tokens from Plan 02)
  </read_first>
  <files>(no code files modified; verification only)</files>
  <action>
Run the quality gate end-to-end. The 8 commands, in order:

1. `cd backend && poetry run ruff check .` — must exit 0.
2. `cd backend && poetry run pytest -q --tb=no -x` — must exit 0 (skip-only collections are acceptable; failures are not).
3. `cd frontend && npx vue-tsc --noEmit -p tsconfig.json` — must exit 0.
4. `cd frontend && npm run lint` — must exit 0.
5. Color gate: `grep -nE "'#[0-9a-fA-F]{3,8}'" frontend/src/pages/DataDashboard.vue frontend/src/stores/quality.ts | grep -v "//" | wc -l` — must return 0.
6. Tooltip gate: `grep -nE "#[0-9a-fA-F]{3,8}" frontend/src/components/NodeTooltip3D.vue | wc -l` — must return 1 (the TYPE_INFO fallback on line 30).
7. Tokens gate: `grep -nE -- "--slate-(200|400|500):" frontend/src/styles/design-tokens.css | wc -l` — must return 3.
8. New test gate: `cd tests/e2e && python test_2d_3d_color_consistency.py --self-test` — must exit 0.

Capture each command's exit code into the SUMMARY.md evidence block. If any command fails, STOP and surface the failure output — do NOT silently update STATE.md.
  </action>
  <verify>
    <automated>cd backend && poetry run ruff check . && cd ../frontend && npx vue-tsc --noEmit && npm run lint && cd ../tests/e2e && python test_2d_3d_color_consistency.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - ruff check exits 0
    - vue-tsc exits 0
    - eslint exits 0
    - python test_2d_3d_color_consistency.py --self-test exits 0
    - All 8 gate commands above are recorded with their exit codes in the resulting SUMMARY.md
  </acceptance_criteria>
  <done>Quality gate fully green; recorded evidence captured for STATE.md update.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md to mark Phase 5 complete (4/6 → 6/6)</name>
  <read_first>
    - .planning/STATE.md (current state — copy the existing P5 Status Detail table format to overwrite in-place)
    - .planning/ROADMAP.md (Phase 5 section — copy the existing `### Phase 4 — 数据流贯通 ✓` block format as the template)
    - .planning/phases/05-style-unify/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md (confirm those plans' SUMMARYs exist before claiming 4 plans)
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Two edits only — no narrative rewrite, no scope creep:

**Edit 1: `.planning/STATE.md`** — three localized changes:

a. In the Phase Progress table, change the row for Phase 5 from `| 5 | 样式统一与体验优化 | ⏳ 4/6 criteria | 2 remaining |` to `| 5 | 样式统一与体验优化 | ✅ completed | 6/6 |`.

b. In the `P5 Status Detail` table, change every `⏳` row to `✅` and update the `Note` column to reflect the post-migration state:
   - `Design tokens usage` row: `⏳` → `✅`; `Note`: `~307 hardcoded colors remain` → `Migrated to graphColors.ts ECHARTS_PALETTE + design-tokens slate tokens (Plans 01-02)`.
   - `Single color source (graphColors.ts)` row: keep `✅`; `Note` unchanged.
   - `GraphToolbar controlled` row: keep `✅`; `Note` unchanged.
   - `Backend dead endpoints deleted` row: keep `✅`; `Note` unchanged.
   - `console.log cleanup` row: keep `✅`; `Note` unchanged.
   - `2D/3D KA color consistency` row: `⏳` → `✅`; `Note`: `Needs verification` → `Playwright harness in tests/e2e/test_2d_3d_color_consistency.py (Plan 03), ±5 RGB tolerance`.

c. In `## Current Position` block, change `Phase: 5 of 6 (样式统一与体验优化)` to `Phase: 6 of 6 (架构重构)` and `Status: Partially done — 4/6 criteria met, remaining: hardcoded color migration` to `Status: Phase 5 complete (6/6); ready to plan Phase 6`. Update `Next:` to `Plan Phase 6 (架构重构)`.

Do NOT touch `DEC-001 ~ DEC-006`, `DEC-010`, the milestone section, or the Baseline Metrics table. Do NOT touch any other phase's row in the Phase Progress table.

**Edit 2: `.planning/ROADMAP.md`** — add a new `### Phase 5 — 样式统一与体验优化 ✓` block immediately above the existing `### Phase 6: 架构重构` block. Mirror the `### Phase 4 — 数据流贯通 ✓` block shape exactly. Include `**Plans:** 4 plans` and a 4-line plans list:

```
- [x] 05-01-PLAN.md — ECHARTS_PALETTE + DataDashboard 迁移 (COLOR-01~04, STYLE-01)
- [x] 05-02-PLAN.md — NodeTooltip3D Slate 替换 + design-tokens 补全 (COLOR-03, STYLE-04)
- [x] 05-03-PLAN.md — 2D/3D KA 一致性 Playwright harness (COLOR-04)
- [x] 05-04-PLAN.md — P5 收尾验证 + STATE 更新
```

After the plans list, add `**Requirements:**` block listing STYLE-01~04, COLOR-01~04, TOOLBAR-01~03, SCHEMA-01~02, CLEANUP-01~04. Then add `**Success criteria:**` block copying the 6 success-criteria bullets from the existing Phase 5 section.

Do NOT modify any other phase block. Do NOT reorder sections. Do NOT change the milestone intro.
  </action>
  <verify>
    <automated>grep -nE "✅ completed.*6/6" .planning/STATE.md && grep -nE "\[x\] 05-(01|02|03|04)-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md contains `✅ completed | 6/6` on the Phase 5 row (verify: grep -nE "5 \| 样式统一.*completed.*6/6" .planning/STATE.md)
    - STATE.md P5 Status Detail table has zero `⏳` rows remaining (verify: grep -cE "⏳" .planning/STATE.md returns 0)
    - ROADMAP.md contains 4 `[x] 05-NN-PLAN.md` lines (verify: grep -cE "\[x\] 05-0[1-4]-PLAN.md" .planning/ROADMAP.md returns 4)
    - ROADMAP.md Phase 5 block sits above the Phase 6 block (verify: line number of "### Phase 5" < line number of "### Phase 6")
    - No other phase section in ROADMAP.md was modified (spot-check: grep -nE "### Phase [1-4]" .planning/ROADMAP.md shows identical line counts as before)
  </acceptance_criteria>
  <done>STATE.md reflects Phase 5 ✅ 6/6; ROADMAP.md Phase 5 has 4 completed plans; no collateral changes.</done>
</task>

</tasks>

<verification>
1. `grep -cE "⏳" .planning/STATE.md` returns 0 (no remaining open items in STATE.md)
2. `grep -cE "\[x\] 05-0[1-4]-PLAN.md" .planning/ROADMAP.md` returns 4
3. `cd backend && poetry run ruff check .` exits 0
4. `cd frontend && npx vue-tsc --noEmit` exits 0
5. `cd tests/e2e && python test_2d_3d_color_consistency.py --self-test` exits 0
</verification>

<success_criteria>
- Quality gate fully green (ruff, pytest, vue-tsc, eslint, self-test).
- STATE.md and ROADMAP.md accurately reflect Phase 5 completion.
- All 17 Phase 5 requirements (STYLE-01~04 + COLOR-01~04 + TOOLBAR-01~03 + SCHEMA-01~02 + CLEANUP-01~04) appear in plan requirements fields with traceability to the executing plans.
</success_criteria>

<output>
Create `.planning/phases/05-style-unify/05-04-SUMMARY.md` when done.
</output>