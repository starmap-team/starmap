---
phase: 06-evolution-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/evolution/orchestrator.py
  - backend/tests/unit/test_evolution_pipeline.py
autonomous: true
requirements: [EVOL-01, EVOL-02, EVOL-03]
user_setup: []
must_haves:
  truths:
    - "Phase 2 Wave 3 已处理 evolution/orchestrator.py 的 except Exception 替换"
    - "evolution 模块已有 EvolutionPipelineError 异常类"
    - "DiffEngine / TrustScorer / PathRecommender 接口需要契约化"
  artifacts:
    - evolution 核心模块接口契约文档
    - Integration tests for evolution pipeline
    - 所有现有测试通过
---

<objective>
升级演化引擎的接口契约和测试覆盖，补全 E2E 集成测试。

**Purpose:** 演化管道（snapshot→diff→trust→path）是核心业务链路，但缺乏测试覆盖。Phase 2 已处理异常，本阶段补全测试和接口契约。

**Output:**
- Evolution E2E 集成测试
- 接口契约检查和测试
- 所有测试通过
</objective>

<execution_context>
@backend/app/core/evolution/orchestrator.py
@backend/app/core/evolution/emergence_finder.py
</execution_context>

<tasks>

<task type="tracer">
  <name>Task 1: Write evolution pipeline unit tests</name>
  <files>backend/tests/unit/test_evolution_pipeline.py</files>
  <action>
    Read backend/app/core/evolution/orchestrator.py first. Then create tests:
    1. test_evolution_orchestrator_init — verify orchestrator construction
    2. test_evolution_orchestrator_imports — verify all needed exports
    3. test_evolution_positions_finder — module level test for position-based evolution
    4. test_evolution_error_handling — verify EvolutionPipelineError propagation
    5. test_evolution_emergence_finder — test emergence finder module imports
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_evolution_pipeline.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>- Evolution E2E 测试创建完成并可通过</done>
  <reversibility rating="reversible">纯增量</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add emergence_finder unit tests</name>
  <files>backend/tests/unit/test_evolution_emergence.py</files>
  <action>
    Read backend/app/core/evolution/emergence_finder.py and create tests:
    1. test_emergence_finder_imports — verify module exports
    2. test_emergence_domain_keywords — verify domain keyword loading
    3. test_emergence_finder_error_handling — verify error handling
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_evolution_emergence.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>- emergence_finder 测试完成</done>
  <reversibility rating="reversible">纯增量</reversibility>
</task>

</tasks>

<verification>
1. cd backend && python -m pytest tests/unit/test_evolution_pipeline.py --no-cov -q
2. cd backend && python -m pytest tests/unit/test_evolution_emergence.py --no-cov -q
</verification>

<success_criteria>
- [ ] Evolution pipeline 测试 ≥8 个 test case
- [ ] emergence_finder 测试 ≥3 个 test case
- [ ] 所有测试通过
</success_criteria>
</output>