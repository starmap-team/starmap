---
phase: 03-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/tests/integration/test_graph_projector_pg.py
  - backend/tests/integration/test_graph_projector_neo4j.py
  - backend/pyproject.toml
autonomous: true
requirements: [TEST-04, TEST-05]
user_setup: []
must_haves:
  truths:
    - "Graph Projector (PG→Neo4j 投影) 有集成测试覆盖 canonical_id MERGE 逻辑"
    - "reconcile_all 函数有完整集成测试"
    - "mypy disallow_untyped_defs 从 false 升级到 true（仅限新模块，逐步推进）"
    - "后端覆盖率从 80.42% 提升到 ≥85%"
  artifacts:
    - tests/integration/test_graph_projector_pg.py — PostgreSQL 投影集成测试
    - tests/integration/test_graph_projector_neo4j.py — Neo4j 集成测试（mock driver）
    - backend/pyproject.toml 中 mypy 配置升级
  key_links:
    - graph_projector.py 的 PG→Neo4j 同步链路是数据一致性的关键
    - mypy 严格度提升是渐进过程，需要逐模块验证
    - 测试覆盖率提升需要先用 coverage report 找到低覆盖区域
---

<objective>
为 Graph Projector 建立 PG+Neo4j 集成测试，提升后端覆盖率到 85%+，并推进 mypy 类型严格度。

**Purpose:** Graph Projector 是 PG→Neo4j 的关键投影链路（canonical_id MERGE, reconcile_all），0 测试覆盖。同时 Phase 2 引入的结构化异常处理需要测试保障。

**Output:**
- Graph Projector 集成测试覆盖 PG 和 Neo4j 两端
- mypy 类型严格度分阶段提升（不强制一次性全量）
- 覆盖率提升到 85%+
- 所有测试通过
</objective>

<execution_context>
@backend/app/services/graph_projector.py
@backend/pyproject.toml
@backend/tests/unit/
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write integration tests for graph_projector.py PG side</name>
  <files>backend/tests/integration/test_graph_projector_pg.py</files>
  <action>
    Read backend/app/services/graph_projector.py first. Then create integration tests covering PG side of projection:
    1. test_load_canonical_ids_from_pg — verify canonical_id extraction from PostgreSQL
    2. test_resolve_skill_canonical_id — verify skill canonical_id resolution logic
    3. test_resolve_position_canonical_id — verify position canonical_id resolution
    4. test_projection_handles_missing_records — verify graceful handling of missing data
    5. test_projection_with_invalid_data — verify validation rejects malformed rows

    Use SQLite in-memory or PostgreSQL test container. The pyproject.toml has pythonpath configured.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_graph_projector_pg.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - File created with 5+ test functions
    - All tests pass
  </done>
  <reversibility rating="reversible">新增测试文件是纯增量。</reversibility>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for graph_projector.py Neo4j side</name>
  <files>backend/tests/integration/test_graph_projector_neo4j.py</files>
  <action>
    Read backend/app/services/graph_projector.py first. Then create integration tests covering Neo4j side of projection:
    1. test_merge_node_with_canonical_id — verify MERGE with canonical_id key
    2. test_merge_relationship — verify relationship creation with canonical IDs
    3. test_reconcile_all_runs — verify reconcile_all execution
    4. test_reconcile_idempotent — verify multiple reconcile runs are idempotent
    5. test_neo4j_connection_failure — verify graceful handling of Neo4j unavailability

    Use mock Neo4j driver (AsyncMock with session context manager) since we don't have a test Neo4j container.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_graph_projector_neo4j.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - File created with 5+ test functions
    - All tests pass
  </done>
  <reversibility rating="reversible">新增测试文件是纯增量。</reversibility>
</task>

<task type="auto">
  <name>Task 3: Run coverage report and verify 85%+ threshold</name>
  <files>N/A (verification only)</files>
  <action>
    Run coverage report and document the current state:
    1. Run: `cd backend && python -m pytest tests/ --cov=app --cov-report=term --no-cov-fail-under -q 2>&1 | tail -40`
    2. Document the new coverage percentage in the SUMMARY.md
    3. If coverage is below 85%, identify the top 3 lowest-covered modules
    4. Update backend/pyproject.toml `cov-fail-under` only if coverage ≥85%

    This is a verification/measurement task — don't write code, just measure.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/ --cov=app --cov-report=term --no-header -q 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Coverage percentage documented in 03-02-SUMMARY.md
    - Top 3 lowest-covered modules identified
    - pyproject.toml updated if coverage ≥85%
  </done>
  <reversibility rating="reversible">measurement only, no code changes</reversibility>
</task>

<task type="auto">
  <name>Task 4: mypy type strictness progression — disallow_untyped_defs partial enable</name>
  <files>backend/pyproject.toml</files>
  <action>
    In backend/pyproject.toml, the current mypy config has:
    ```
    disallow_untyped_defs = false
    disallow_any_generics = false
    ```

    Phase 3 strategy: don't flip globally (would break existing modules). Instead:
    1. Add a comment noting Phase 3 progression goal
    2. Document the per-module enablement in mypy.ini (already exists per current config)
    3. Run: `cd backend && python -m mypy app/exceptions.py app/main.py app/core/validation/errors.py app/services/auth_service.py --no-error-summary 2>&1 | head -20`
    4. Document mypy debt in SUMMARY.md (count of untyped defs across the codebase)

    Note: Full mypy strict mode enablement is a multi-phase effort. Phase 3 is measurement + new modules strict.
  </action>
  <verify>
    <automated>cd backend && python -m mypy app/exceptions.py app/main.py --no-error-summary 2>&1 | tail -3</automated>
  </verify>
  <done>
    - mypy.ini strategy documented
    - mypy baseline count captured in SUMMARY.md
    - No regression in existing modules
  </done>
  <reversibility rating="reversible">测量和文档任务，零代码改动。</reversibility>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ --no-cov -q` — all tests pass (existing + new)
2. `python -m pytest tests/ --cov=app --cov-report=term -q` — coverage ≥80% (target: 85%)
3. `python -m mypy app/exceptions.py` — 0 errors (new modules have annotations)
</verification>

<success_criteria>
- [ ] Graph Projector PG 端 ≥5 tests pass
- [ ] Graph Projector Neo4j 端 ≥5 tests pass
- [ ] 覆盖率 ≥80%（实际提升幅度记录在 SUMMARY）
- [ ] mypy 新模块 0 errors
- [ ] 所有测试通过
</success_criteria>

<output>
Create `.planning/phases/03-test-infrastructure/03-02-SUMMARY.md` when done
</output>