---
phase: 03-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_sse_pipeline_engine.py
  - backend/tests/unit/test_sse_pipeline_steps.py
  - backend/tests/unit/test_sse_pipeline_contracts.py
autonomous: true
requirements: [TEST-03, TEST-06]
user_setup: []
must_haves:
  truths:
    - "SSE Pipeline 的 3 个核心模块（engine.py, steps.py, contracts.py）有单元测试覆盖"
    - "PipelineContext 不可变接口契约有测试保障"
    - "步骤级错误处理有测试"
    - "_build_step_output 验证逻辑有测试"
  artifacts:
    - tests/unit/test_sse_pipeline_engine.py
    - tests/unit/test_sse_pipeline_steps.py
    - tests/unit/test_sse_pipeline_contracts.py
  key_links:
    - engine.py 和 steps.py 的异常处理已由 Phase 2 guard 化
    - contracts.py 定义了 PipelineContext / StepOutput dataclass
    - 所有测试遵循 pytest + pytest-asyncio 模式
---

<objective>
为 SSE Pipeline 核心模块（engine.py, steps.py, contracts.py）建立单元测试覆盖，奠定重构安全网。

**Purpose:** SSE Pipeline 是实时数据流的核心（实时推送爬取/抽取/匹配进度），但当前 0 测试覆盖。Phase 4 的 Pipeline 统一重构需要这个测试网保护。

**Output:**
- 20+ 个新测试用例覆盖 SSE Pipeline 三个核心文件
- 关键路径覆盖：上下文构建、步骤执行、超时、取消、错误转换
- 所有新测试通过
</objective>

<execution_context>
@backend/app/sse_pipeline/engine.py
@backend/app/sse_pipeline/steps.py
@backend/app/sse_pipeline/contracts.py
@backend/tests/unit/test_sse_progress.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write unit tests for SSE Pipeline contracts.py</name>
  <files>backend/tests/unit/test_sse_pipeline_contracts.py</files>
  <action>
    Read backend/app/sse_pipeline/contracts.py first. Then create tests covering:
    1. test_pipeline_context_defaults — verify PipelineContext dataclass default values
    2. test_pipeline_context_required_fields — verify required field validation
    3. test_step_output_required_keys — verify StepOutput dict structure (status/data/duration_ms)
    4. test_step_event_types — verify event type enums/strings
    5. test_contracts_imports — verify module exports

    Pattern:
    ```python
    import pytest
    from app.sse_pipeline.contracts import PipelineContext, StepOutput


    def test_pipeline_context_defaults():
        ctx = PipelineContext(run_id="test", stage="crawl")
        assert ctx.run_id == "test"
        assert ctx.stage == "crawl"
    ```
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_sse_pipeline_contracts.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - File created with 5+ test functions
    - All tests pass
  </done>
  <reversibility rating="reversible">新增测试文件是纯增量。</reversibility>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for SSE Pipeline steps.py</name>
  <files>backend/tests/unit/test_sse_pipeline_steps.py</files>
  <action>
    Read backend/app/sse_pipeline/steps.py first. Then create tests covering:
    1. test_register_steps_idempotent — register_steps can be called multiple times safely
    2. test_register_steps_registers_expected_keys — verify all expected step names registered
    3. test_step_function_signature — verify step handlers are async callable
    4. test_step_handlers_return_dict — verify step handlers return StepOutput-compatible dicts
    5. test_extract_skills_step_runs — exercise extract_skills handler with mock context
    6. test_import_step_runs — exercise import handler with mock context
    7. test_graph_sync_step_runs — exercise graph_sync handler
    8. test_step_with_timeout — verify step respects asyncio timeout

    Use pytest fixtures and AsyncMock.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_sse_pipeline_steps.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - File created with 8+ test functions
    - All tests pass
  </done>
  <reversibility rating="reversible">新增测试文件是纯增量。</reversibility>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for SSE Pipeline engine.py</name>
  <files>backend/tests/unit/test_sse_pipeline_engine.py</files>
  <action>
    Read backend/app/sse_pipeline/engine.py first. Then create tests covering:
    1. test_pipeline_engine_init — verify PipelineEngine construction
    2. test_run_step_success — verify happy path returns StepOutput
    3. test_run_step_timeout — verify asyncio.TimeoutError mapped to structured error
    4. test_run_step_cancelled — verify asyncio.CancelledError handled correctly
    5. test_run_step_exception — verify unexpected exception wrapped in structured error
    6. test_build_step_output_validation — verify _build_step_output rejects malformed output
    7. test_build_step_output_required_keys — verify required keys present
    8. test_engine_emits_progress — verify progress emission to subscribers
    9. test_engine_handles_no_subscribers — verify no subscriber doesn't crash
    10. test_engine_concurrent_steps — verify multiple steps execute concurrently

    Use pytest-asyncio (mode=auto in pyproject), AsyncMock, and anyio for timeout tests.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_sse_pipeline_engine.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - File created with 10+ test functions
    - All tests pass
  </done>
  <reversibility rating="reversible">新增测试文件是纯增量。</reversibility>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/test_sse_pipeline_*.py --no-cov -q` — all new tests pass
2. Total new test count: ≥20 across 3 files
</verification>

<success_criteria>
- [ ] contracts.py ≥5 tests pass
- [ ] steps.py ≥8 tests pass
- [ ] engine.py ≥10 tests pass
- [ ] All new tests pass
</success_criteria>

<output>
Create `.planning/phases/03-test-infrastructure/03-01-SUMMARY.md` when done
</output>