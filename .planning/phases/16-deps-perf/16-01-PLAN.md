---
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml (deps)
  - frontend/package.json (deps)
  - backend/app/core/matching/scorer.py (batch query)
  - backend/alembic/versions/010_*.py (new - composite indexes)
  - backend/app/db/session.py (auto commit/rollback)
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05]
---

# PLAN-01: Dependency Upgrade + Performance (Wave 1)

## Tasks

### Task 1.1: Backend dependency upgrade
FastAPI 0.139, Redis 8, Neo4j 6, Celery 5.6. Test after each upgrade.

### Task 1.2: Frontend dependency upgrade
Vite 8, Vitest 4, Pinia 3, ECharts 6. Test after each upgrade.

### Task 1.3: ChromaDB batch query
Replace per-skill loop in scorer.py with single batch query.

### Task 1.4: Database composite indexes
Add alembic migration for plan_id+skill_id, skill_name+window_start indexes.

### Task 1.5: Session commit consistency
Fix get_db_session to auto commit/rollback on yield.

## Acceptance Criteria
- All deps at target versions
- scorer.py single ChromaDB query
- Composite indexes in migration
- `cd backend && poetry run pytest -q` — 0 new failures
- `cd frontend && npx vitest run` — 0 failures
