---
phase: 06-extract-jd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/archive/extract-source-analysis.md
  - docs/archive/extract-optimization-design.md
  - frontend/src/stores/jd.ts
  - frontend/src/pages/ExtractJD.vue
  - frontend/src/pages/__tests__/ExtractJD.spec.ts
  - backend/app/api/v1/extract.py
  - backend/app/core/extraction/jd_extract.py
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03

must_haves:
  truths:
    - ExtractJD.vue + jd.ts + extract.py + core/extraction 的源码缺陷已完整分析并归档（含严重级别与行号）
    - 基于分析结果产出了 3-5 个具体的优化需求与设计方案
    - JD 抽取请求体与后端 ExtractionRequest 对齐（前端发送 jd_content、50000 字上限；后端 Pydantic min_length=1 / max_length=50000，链路一致）
    - 抽取结果展示完整字段：position_name、required_skills / preferred_skills（带 proficiency）、normalized_skills（带 method + 置信度）、confidence（百分比）、反幻觉检查结果
    - 反幻觉检查结果在前端可见：hallucinated_skills、missing_skills、issues 三段渲染
    - /extract/cost-summary 数据流可达：前端 store / 页面 / 仪表盘任意一处有渲染入口（即使不在主路径）
    - ExtractJD.spec.ts 新增 5+ 测试覆盖：空文本守卫、抽取成功渲染、抽取失败态、归一化表渲染、cost-summary 调用
    - 所有现有测试通过（pytest + vitest）
  artifacts:
    - docs/archive/extract-source-analysis.md
    - docs/archive/extract-optimization-design.md
    - frontend/src/stores/jd.ts (fixed)
    - frontend/src/pages/ExtractJD.vue (fixed)
    - frontend/src/pages/__tests__/ExtractJD.spec.ts (updated)
    - backend/app/api/v1/extract.py (fixed)
    - backend/app/core/extraction/jd_extract.py (fixed)
  key_links:
    - ExtractJD.handleExtract → jd.extractJd(jdText) → request.post('/extract/jd', { jd_content }) → POST /api/v1/extract/jd (router.py 第 211 行 include_router prefix=/api/v1) → extract_jd (extract.py 第 152 行) → extract_from_jd (jd_extract.py 第 401 行) → JDExtractionPipeline.run → _build_result → ExtractionResult
    - LLM 降级链 call_llm_with_fallback (llm_client.py 第 245 行) → MiMo → DeepSeek → 星火 → Qwen/Ollama；任一环节失败自动切换，全部失败抛 LLMConnectionError
    - 反幻觉: llm_client.validate_extraction (第 373 行) → normalize_skill_list/normalize_str_list (anti_hallucination.py) → AntiHallucinationResult → result["validation"] → _build_result.hallucination_score (extract.py 第 88 行)
    - 标准化: batch_normalize_skills (normalize.py 第 710 行) → NormalizationResult → result["normalization"] → _build_result.normalized_skills (extract.py 第 89 行)
    - 成本: tracker.record (llm_client.py 第 259 行) → tracker.summary → GET /api/v1/extract/cost-summary (extract.py 第 145 行)
    - 入库: _write_extraction_to_pg (extract.py 第 126 行) + _write_extraction_to_graph (第 93 行) 同步双写 PositionRecord + Neo4j 节点（解耦抽取 → 持久化的数据流）

---

<objective>
对 JD 抽取模块（ExtractJD — StarMap 核心 LLM 入口）进行全链路源码分析、缺陷发现、优化设计、联调修复和闭环验证。

**Purpose:** 确保"JD 文本 → PII 脱敏 → 多 LLM 降级抽取 → JSON 校验 → 技能归一化 → 反幻觉检查 → 双写 (PostgreSQL + Neo4j)"主业务流前后端数据一致；反幻觉检查结果可见；成本数据可达；测试覆盖完整（当前仅 1 个 vacuously-green 冒烟测试）。

**Output:**
1. `docs/archive/extract-source-analysis.md` — 前后端源码缺陷分析报告
2. `docs/archive/extract-optimization-design.md` — 用户优化点与需求设计方案
3. 修复后的前端 store / 页面与后端 extract.py / jd_extract.py 源码
4. 更新后的 ExtractJD.spec.ts 测试文件（5+ 用例）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-extract-jd/RESEARCH.md
@frontend/src/pages/ExtractJD.vue
@frontend/src/stores/jd.ts
@frontend/src/pages/__tests__/ExtractJD.spec.ts
@frontend/src/api/request.ts
@backend/app/api/v1/extract.py
@backend/app/core/extraction/jd_extract.py
@backend/app/core/extraction/normalize.py
@backend/app/core/extraction/anti_hallucination.py
@backend/app/core/extraction/llm_client.py
@backend/app/main.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: JD 抽取模块源码缺陷分析（前后端全链路）</name>
  <files>
    docs/archive/extract-source-analysis.md
  </files>
  <action>
    逐层阅读并分析 JD 抽取模块的全部源码，验证以下预判发现并补充遗漏问题。按层级组织分析报告，每个问题标注文件 + 行号 + 严重级别（CRITICAL / HIGH / MEDIUM / LOW）+ 影响范围 + 修复方向：

    **第 1 层 — 前端页面 (ExtractJD.vue, 311 行)**
    - **结果对象多字段全 fallback（HIGH）**：result.position_name ?? result.job_title（第 142 行）、result.experience_required ?? result.experience_years（第 145 行）、result.education_required ?? result.education（第 147 行）、skill.skill ?? skill.name ?? s（第 168、187 行）——后端 ExtractionResult 字段是固定的 snake_case（position_name、experience_required、education_required、required_skills[i].skill），前端对"兼容多 schema"做的兜底全是死代码（响应键不会变化）。`?? result.job_title` 永远走前者。结合 store JDExtractResult 接口（JDExtractResult type，jd.ts 第 19-32 行）确认 8 个字段无歧义，清理冗余 fallback
    - **proficiency 透传丢失（MEDIUM）**：extract.py `_map_skill_item`（第 61-72 行）会把 skill 归一为 `{skill, category, proficiency}` 三键结构，前端 required_skills / preferred_skills 标签（第 162-169、180-187 行）只渲染 skill.name，未渲染 proficiency——必备/加分技能的可视化无熟练度区分（与"加分技能"页面要传达的"非必备"信号冲突）
    - **置信度字段 `confidence` 默认 0 风险（LOW）**：第 152 行 `Math.round((result.confidence ?? 0) * 100)` —— 后端 `_build_result` 第 87 行 `validation.get("confidence", 0.85)` 默认给 0.85 但仅在 pipeline_result 缺 validation 时生效；正常路径下 result.confidence 来自 validation.confidence（jd_extract.py 第 358 行）。当前没看到显式 None 风险，但 `result.confidence ?? 0` 在 API 失败但前端未 catch 时会显示 0%，视觉上像"完全不可信"
    - **cost-summary 数据流不可达（MEDIUM）**：后端 extract.py 第 145 行暴露了 GET /extract/cost-summary 返回 `tracker.summary()`（进程内累计成本），前端 `jd.ts` 与 ExtractJD.vue 均未调用、ExtractJD 页面也无渲染入口——成本数据对用户不可见。`app/core/llm/cost_tracker.py` 的 summary 结构（model、calls、prompt_tokens、completion_tokens、total_cost）需先 Read 确认（执行时 Read，不要在 action 里写实现）
    - **normalize_skills confidence 错位（LOW）**：第 222 行 `((row.confidence ?? 0) * 100).toFixed(0)`——后端 NormalizationResult.confidence（normalize.py 第 651 行）在 alias 命中时是 0.95、vector 命中是 vector_threshold、identity 是 0.5。范围已经 0-1 但前端做了 `* 100`，需确认后端是否真的发 0-1 而非 0-100 百分比（根据 normalize.py 第 651 行 dataclass 默认 0.0 且只在 0-1 范围赋值，前端 `* 100` 是对的，但需在分析报告里记下这一确认）
    - **反幻觉结果完全未渲染（HIGH）**：后端 _build_result.hallucination_score（extract.py 第 88 行）= `None if validation.get("is_valid", True) else validation.get("confidence")`——前端 result.hallucination_score ?? null（jd.ts 第 27 行有定义）但 ExtractJD.vue 模板无任何 el-descriptions / el-alert / el-collapse 消费它。validation 的 hallucinated_skills / missing_skills / issues（anti_hallucination.py 第 17-21 行）三个字段全部丢弃。EXTRACT-03 反幻觉可见性需求完全未实现
    - 正面确认：第 23-54 行 handleExtract 的 progress timer 双重点击防护（BUG-004 修复）、第 32 行 `if (progressTimer) { clearInterval }` 防双 interval、第 42-53 行 try/catch/finally 清理、第 61-63 行 onUnmounted 清理 timer —— 全部是正确的既有防护，分析报告中记录为"保留项"

    **第 2 层 — Store 层 (jd.ts, 152 行)**
    - **JDExtractResult 接口字段冗余（LOW）**：第 19-32 行定义了 12 个可选字段，其中 `skills`（第 29 行 `{name, category, confidence, is_new}`）、`position`（第 30 行）是过去 schema 残留——后端 ExtractionResult 不返回这些字段（extract.py 第 32-43 行 Pydantic 模型无这两个键）。死代码字段影响可读性
    - **extractJd 错误吞掉（MEDIUM）**：第 135-145 行 try/finally 只翻 extractLoading，无 catch——非 2xx 响应（500/502/422 等）由 axios 抛出，被上层 ExtractJD 第 47-50 行的 `catch (e: unknown)` 捕获并以 `e.message` 显示。后端 HTTPException.detail（extract.py 第 171 行 `f"LLM service unavailable: {e}"`）在 axios 错误里是 `e.response.data.detail` 而非 `e.message`，导致用户看到的是 axios 默认 "Request failed with status code 502" 而非 "LLM service unavailable"。需在 catch 中读 `e.response?.data?.detail`
    - **clearResult 与空状态守卫**：第 147-149 行 `clearResult()` 在 handleClear（第 56-59 行）调用 —— 当前行为正确，分析报告记录
    - **URL 正确性核查（Phase 4 遗留关注点）**：逐一核对 jd.ts 第 139 行 `request.post('/extract/jd', ...)`、fetchList 第 72 行 `request.get('/positions', ...)`、fetchPositionSkills 第 91 行 `/graph/position/{name}/skills`、fetchPositionDetail 第 96 行 `/positions/{name}` —— 经 main.py 第 211 行 `include_router(api_router, prefix="/api/v1")` 核实**前端 `/extract/jd` 与后端 `POST /api/v1/extract/jd`（extract.py router prefix=/extract + 第 152 行 `@router.post("/jd")`）拼装一致，无 /admin/ 前缀错配**，在报告中明确记录该结论

    **第 3 层 — 后端 API (extract.py, 251 行)**
    - **ExtractionResult 字段与 jd_extract.py 数据漂移（HIGH）**：第 32-43 行 ExtractionResult Pydantic 模型字段（position_name/required_skills/preferred_skills/experience_required/education_required/responsibilities/confidence/hallucination_score/normalized_skills）与 jd_extract.py 第 116-131 行 JDExtractionResult 字段（含 industry/description/knowledge_areas/tools/prerequisites/learning_resources/evolves_to 共 7 个后端字段）**严重不对齐**。当前 _build_result（第 75-90 行）只透传 5 个核心字段，丢弃了 tools / prerequisites / learning_resources / evolves_to 等高价值结构化数据。Phase 1-5 已完成 PositionRecord 写入主流程（extract.py 第 135-142 行 `write_extraction_to_pg`），但额外字段未投影到 DB。前端需求 EXTRACT-02 要求"抽取结果展示完整"——目前漏了 4 个字段
    - **`/extract/cost-summary` 进程内聚合无持久化（MEDIUM）**：第 145-148 行返回 tracker.summary()，tracker 是 app/core/llm/cost_tracker.py 的进程内单例（执行时 Read 确认）。多进程部署（uvicorn --workers N）下各 worker 独立计数，前端拉取命中不同 worker 会得到不同结果——数据一致性破坏。修复方向：移到 Redis 聚合（成本数据已在 Redis 计数器基础上加）或接受现状加注释（多 worker 部署需知此约束）
    - **`_map_skill_item` 静默吞 proficiency（LOW）**：第 61-72 行 `proficiency: _map_proficiency(payload.get("proficiency") or payload.get("level"))` —— 当 LLM 返回空 proficiency 时回退 "熟悉"（_map_proficiency 第 58 行 mapping.get default），是正确的兜底；但当 payload 完全无 proficiency 键时（如 LLM 只给 "Python" 字符串），前端拿到 proficiency="熟悉" 看似"已精通"——与"LLM 未指定"语义偏差。注释说明即可，不阻塞
    - **Pydantic 模型内联违反 Schema 集中管理（LOW）**：第 25-43 行 ExtractionRequest / ExtractionResult 内联在路由文件——AGENTS.md"Schema 集中管理"约定（`backend/app/schemas/extract.py` 应存在）。先 grep 确认 schemas/extract.py 是否已迁移（若已存在说明前期已拆，仅未删除内联），若是则仅删除内联；若不存在，评估迁移成本
    - **422 detail 信息泄露风险（LOW）**：第 171 / 174 / 187 行 HTTPException(detail=str(e)) 直接把 ValueError / ConnectionError 异常消息透出——若底层 LLM SDK 异常消息包含 API key 片段或内部堆栈，会泄露给客户端。先 Read llm_client.py 异常消息文本确认（执行时 Read）

    **第 4 层 — 抽取核心 (jd_extract.py 422 行 + normalize.py + anti_hallucination.py + llm_client.py)**
    - **Pydantic fallback 字段覆盖不完整（MEDIUM）**：jd_extract.py 第 238-276 行 Pydantic 校验失败后的 manual field-by-field fallback 不覆盖 `industry`、`description`、`knowledge_areas`、`tools`、`prerequisites`、`learning_resources`、`evolves_to`——列表字段中后 4 个（tools/prerequisites/learning_resources/evolves_to）若 LLM 返回结构错位会丢失。BL-02 注释（"Complete fallback for ALL fields"）承诺与代码不符
    - **miMo reasoning 模型 max_tokens 风险（MEDIUM）**：llm_client.py 第 88 行 MiMo max_tokens=8192 是 reasoning model 的硬编码——推理模型 reasoning_tokens + output 都计入 max_tokens，8192 对长 reasoning 链可能不够，超长响应会被截断 JSON 解析失败。需在分析报告中记下"风险上限"，建议改为 settings.llm_max_tokens 配可调
    - **降级链 Qwen/Ollama 选项硬编码（LOW）**：llm_client.py 第 309 行 `"model": "qwen2.5:7b"` 与第 310 行 `"num_predict": 4096` 硬编码——不读 settings.qwen_model_name 也不读 fallback 配置。多模型部署时降级链断在硬编码值。先 Read settings.py 确认是否有 qwen_model_name（执行时 Read）
    - **anti-hallucination 失败静默（MEDIUM）**：jd_extract.py 第 369-371 行 `except (LLMConnectionError, LLMResponseError, LLMTimeoutError) as e` 只捕获 LLM 异常——其他异常（如 Pydantic ValidationError、KeyError）会冒泡到 API 层返回 500。AntiHallucinationResult 字段若 LLM 返回结构偏离 schema（reasoning model 频繁），整个抽取流程会失败。捕获范围需扩到 ValidationError + Exception
    - **dict filter 误杀（LOW）**：jd_extract.py 第 378-392 行 `extract_dict_skills` 后过滤 LLM 找到的 skill——LLM 找到但不在 572 词字典中的技能会被丢弃（如新出现的 "LLM Engineering"、"MCP Protocol"）。注释（第 374-377 行）已说明"on the 110-sample golden set, this lifts F1 from 0.8637 → 0.9258"——属已知权衡，分析报告记录

    **第 5 层 — 契约 (openapi.yaml)**
    - Read starmap-contracts/openapi.yaml 的 /extract/jd、/extract/resume、/extract/cost-summary 端点定义（执行时 Read），核对 ExtractionResult schema 与后端 Pydantic 模型的一致性、cost-summary 响应 schema 与 tracker.summary() 实际返回的一致性。若有偏差，在分析报告记下契约需变更的端点

    **第 6 层 — 测试覆盖 (ExtractJD.spec.ts, 43 行)**
    - 仅 1 个测试"renders without crashing"（第 39-42 行）；stubs 列表（第 28-34 行）stub 了 el-input/el-button/el-card——主表单交互不验证、抽取按钮点击不触发、未 mock `@/api/request` —— 当前因 handleExtract 在未点击时不发起 API 而侥幸绿灯（vacuous pass）
    - 未覆盖：空文本守卫、handleExtract 抽取成功、handleExtract 抽取失败、归一化表渲染、反幻觉结果渲染、cost-summary 调用、proficiency 显示、handleClear 行为
    - 参考既有模式：frontend/src/pages/__tests__/MatchDiagnosis.spec.ts / frontend/src/stores/__tests__/learningRecommendation.test.ts 已 mock request 模块——Phase 4 datasources、Phase 5 match-diagnosis 的测试模式可直接复用

    将分析结果写入 `docs/archive/extract-source-analysis.md`。
  </action>
  <verify>
    <automated>test -f docs/archive/extract-source-analysis.md && grep -c 'CRITICAL\|HIGH\|MEDIUM\|LOW' docs/archive/extract-source-analysis.md</automated>
  </verify>
  <done>
    docs/archive/extract-source-analysis.md 已创建，包含 6 层源码分析，每个问题标注了严重级别、行号和修复方向；结果字段冗余 fallback、反幻觉结果未渲染、ExtractionResult 与 JDExtractionResult 字段漂移、Pydantic fallback 不完整、anti-hallucination 异常捕获不全五个结论均经前后端双向核实；URL 正确性核查（含"无 /admin/ 前缀错配"结论）已记录。
  </done>
</task>

<task type="auto">
  <name>Task 2: 用户优化点调研与需求设计</name>
  <files>
    docs/archive/extract-optimization-design.md
  </files>
  <action>
    基于 Task 1 的分析结果产出 3-5 个具体可执行的优化需求。每个需求包含：需求背景（引用 Task 1 发现 + 行号）、优化目标、设计方案（修改文件、核心变更、前后端数据流变化、兼容性）、验收标准、可行性评估（实现复杂度 + 是否需要契约变更/迁移）。

    需求候选（按根因聚类，不限于此）：

    **需求 A: 抽取结果字段全量透传 + 反幻觉可见性（修两个 HIGH）**
    - 背景: ExtractionResult 与 JDExtractionResult 字段漂移（extract.py 第 32-43 行 vs jd_extract.py 第 116-131 行）丢弃 tools/prerequisites/learning_resources/evolves_to；反幻觉 hallucination_score/hallucinated_skills/missing_skills/issues 完全未渲染（ExtractJD.vue 模板无对应 el-alert / el-collapse）
    - 设计方向: ① 后端 ExtractionResult（extract.py 第 32-43 行）增加可选字段 `tools: list[dict] = []`、`prerequisites: list[dict] = []`、`learning_resources: list[dict] = []`、`evolves_to: list[str] = []`，_build_result（第 75-90 行）从 pipeline_result["data"] 透传；② 前端 JDExtractResult（jd.ts 第 19-32 行）补同名可选字段；ExtractJD.vue 新增反幻觉结果区（el-alert type=warning 显示 hallucinated_skills 计数 + issues 列表；el-collapse 折叠 missing_skills 详情）；新增 tools / learning_resources 表格渲染（prerequisites / evolves_to 优先级低，二期处理）。权衡写入设计
    - 验收: POST /extract/jd 响应包含 tools/learning_resources 数组；ExtractJD 页面反幻觉结果区显示 hallucinated_skills 计数与 issues 列表；is_valid=false 时 alert 可见

    **需求 B: 错误信息透传修复（修 MEDIUM + 优化 UX）**
    - 背景: jd.ts extractJd 第 135-145 行 try/finally 无 catch，HTTPException.detail 字符串在 axios 错误对象里位于 `e.response.data.detail`，ExtractJD.vue 第 49 行 `e.message` 取到的是 axios 默认 "Request failed with status code 502"
    - 设计方向: store extractJd 改为 try/catch/finally，catch 中提取 `error.response?.data?.detail ?? error.message` 重新抛出带业务语义的 Error（附 `// fix: 把后端 HTTPException.detail 透到前端 ElMessage` 注释）；同步给 ExtractJD.vue 第 49 行 catch 加业务提示语
    - 验收: LLM 502 时 ElMessage 显示 "LLM service unavailable: <原始消息>" 而非 "Request failed with status code 502"

    **需求 C: 抽取流程鲁棒性增强（修两个 MEDIUM）**
    - 背景: Pydantic fallback 不完整（jd_extract.py 第 238-276 行缺 industry/description/knowledge_areas/tools/prerequisites/learning_resources/evolves_to 列表字段）；anti-hallucination 异常捕获范围窄（第 369-371 行只捕 LLM 异常，ValidationError/KeyError 冒泡 500）
    - 设计方向: ① Pydantic fallback 补齐 7 个遗漏字段（按已有块结构 for key in (...): if key in parsed and parsed[key]: setattr），保证 BL-02 注释承诺与代码一致；② anti-hallucination except 子句加 `except (PydanticValidationError, KeyError, TypeError) as e` + `except Exception as e` 兜底，失败时 `result["warnings"].append(f"Validation error: {e}")` 而非抛 500（业务上反幻觉失败不应阻塞抽取主流程）
    - 验收: Pydantic 校验失败后 pipeline_result.data 包含 LLM 返回的全部字段；LLM 返回结构偏离时反幻觉失败但不抛 500

    **需求 D: cost-summary 数据可见化（修 MEDIUM）**
    - 背景: 后端 GET /extract/cost-summary 已暴露（extract.py 第 145-148 行），前端无调用入口
    - 设计方向: ① jd.ts 增加 `fetchCostSummary()` 函数（request.get('/extract/cost-summary')，返回 `{models: [...], total_calls, total_tokens, total_cost}` 结构——先 Read cost_tracker.py 确认 summary 实际结构）；② ExtractJD.vue 在"抽取结果"卡片底部新增 el-descriptions 折叠区"LLM 成本摘要"，每次 handleExtract 成功后异步刷新；或放到 MainLayout 顶部统一显示（与已有页面布局决策权衡）。低成本路线：仅在抽取按钮旁加一个小问号 tooltip 触发调用
    - 验收: 抽取成功后页面某处显示当次累计成本数字；GET /extract/cost-summary 200 响应

    **需求 E: 页面级清理 + proficiency 显示 + Schema 迁移评估（打包 MEDIUM/LOW）**
    - 背景: ExtractJD.vue 多字段冗余 fallback（`?? result.job_title` 等 5 处）；required_skills / preferred_skills 标签未渲染 proficiency；422 detail 可能泄露内部错误；extract.py 内联 Pydantic 模型（已可能迁移）
    - 设计方向: ① 删 ExtractJD.vue 第 142、145、147、168、187 行的 `?? result.xxx` 死代码 fallback，加注释 `// fix: 后端 Pydantic 模型字段已锁定，无需前端兼容旧 schema`；② skill 标签显示 "Python (精通)" 形式（skill.skill ?? skill.name ?? s + ` (${s.proficiency ?? '熟悉'})`）；③ llm_client.py 异常消息 Read 确认后若含敏感信息则改 sanitize；④ grep schemas/extract.py 是否已迁移——若已迁则删内联；未迁则在分析报告记为低优先级候选
    - 验收: 抽取结果技能标签含熟练度；`?? result.job_title` 等死代码全部清除；前端 lint 通过

    将设计方案写入 `docs/archive/extract-optimization-design.md`。
  </action>
  <verify>
    <automated>test -f docs/archive/extract-optimization-design.md && grep -c '需求' docs/archive/extract-optimization-design.md</automated>
  </verify>
  <done>
    docs/archive/extract-optimization-design.md 已创建，包含 3-5 个优化需求，每个需求含背景（带行号引用）、方案、验收标准、可行性评估（含契约变更需求标注）。
  </done>
</task>

<task type="auto">
  <name>Task 3: 前后端联调验证与数据流修复</name>
  <files>
    frontend/src/stores/jd.ts
    frontend/src/pages/ExtractJD.vue
    backend/app/api/v1/extract.py
    backend/app/core/extraction/jd_extract.py
  </files>
  <action>
    执行前后端联调验证并修复数据流问题。原则：不做大重构，每个修复只改目标行并加 `// fix:` 或 `# fix:` 注释；schemas/extract.py 迁移等结构性改造本 Task 不实施，仅在代码中以 ponytail 注释标注上限。

    **Step 1: 抽取结果字段透传 + 反幻觉可见性（需求 A）**
    - 读 `backend/app/core/llm/cost_tracker.py` 确认 `tracker.summary()` 实际返回结构（影响需求 D）
    - 修改 `extract.py`（第 32-43 行）ExtractionResult Pydantic 模型：新增 `tools: list[dict[str, Any]] = []`、`learning_resources: list[dict[str, Any]] = []`、`evolves_to: list[str] = []` 三个可选字段
    - 修改 `_build_result`（extract.py 第 75-90 行）：从 `pipeline_result["data"]` 透传 `tools` / `learning_resources` / `evolves_to`；`hallucination_score` 改为：当 `validation.get("is_valid", True)` 为 True 时取 `validation.get("confidence", 1.0)`（EXTRACT-03 透明度：把有效置信度直接展示），否则取 `0.0`（低分视觉警告）；并新增 `hallucinated_skills: list[str] = []`、`missing_skills: list[str] = []`、`issues: list[str] = []` 三个反幻觉字段透传 `validation` 内容
    - 修改 `jd.ts`（第 19-32 行）JDExtractResult 接口：补同名 6 个可选字段（tools / learning_resources / evolves_to / hallucinated_skills / missing_skills / issues）；删除 `skills` 与 `position` 死代码字段，加注释 `// fix: 后端字段已收敛，前端不再保留旧 schema 兜底`
    - 修改 `ExtractJD.vue` 模板（"抽取结果"卡片内）：
      - 在归一化表后新增 el-alert（type="warning" v-if="result.hallucinated_skills?.length || result.issues?.length"）显示"反幻觉检查发现 X 个问题"，closable 折叠
      - el-collapse 折叠面板"反幻觉详情"展示 `result.issues`（列表）+ `result.hallucinated_skills`（tag 列表）+ `result.missing_skills`（tag 列表）
      - 新增 el-descriptions 行"反幻觉分数"显示 `result.hallucination_score` 百分比
      - 新增"工具/技术"区渲染 `result.tools`（tag 列表）
      - 新增"学习资源"区渲染 `result.learning_resources`（el-table，列 title/type/url）

    **Step 2: 错误透传修复（需求 B）**
    - 修改 `jd.ts`（第 135-145 行）extractJd：try/catch/finally 结构，catch 中提取 `err.response?.data?.detail ?? (err instanceof Error ? err.message : '抽取失败')` 抛出新 Error，加注释 `// fix: HTTPException.detail 在 axios 错误对象里位于 response.data.detail，message 字段是 axios 默认文案`
    - 同步检查 `ExtractJD.vue` 第 47-50 行 catch：若依赖 `e.message` 走业务语义，现在会自动透传，验收时再核对

    **Step 3: 抽取鲁棒性增强（需求 C）**
    - 修改 `jd_extract.py`（第 238-276 行）Pydantic fallback 块：补 `industry`、`description`、`knowledge_areas` 三个字段（按已有 for key 模式）；`tools`、`prerequisites`、`learning_resources`、`evolves_to` 四个列表字段也按已有结构补 setattr；加注释 `# fix: BL-02 注释承诺补齐全部字段，否则 LLM 返回的 tools/learning_resources 等结构化数据丢失`
    - 修改 `jd_extract.py`（第 369-371 行）anti-hallucination except：原 `except (LLMConnectionError, LLMResponseError, LLMTimeoutError) as e` 改为 `except (LLMConnectionError, LLMResponseError, LLMTimeoutError, ValidationError, KeyError, TypeError) as e`，再加 `except Exception as e` 兜底 → `result["warnings"].append(f"Validation error: {e}")`；加注释 `# fix: 反幻觉失败不应阻塞主抽取流程，LLM reasoning 模型经常返回结构偏离 schema`

    **Step 4: cost-summary 数据可见化（需求 D，低成本路线）**
    - `jd.ts` 新增 `fetchCostSummary()`：`async function fetchCostSummary() { return await request.get('/extract/cost-summary') }`；暴露到 store return
    - `ExtractJD.vue` 在"抽取结果"卡片底部新增折叠区域"LLM 成本摘要"（el-button text @click 触发 + el-descriptions 显示）；数据从 `await jd.fetchCostSummary()` 取，加 loading 状态

    **Step 5: 页面级清理（需求 E）**
    - 删除 `ExtractJD.vue` 第 142、145、147 行的 `?? result.job_title` / `?? result.experience_years` / `?? result.education` 死代码 fallback（保留 `?? '-'` 默认值）
    - 删除 `ExtractJD.vue` 第 168、187 行 skill 渲染的 `?? s.name ?? s` 死代码分支（后端永远发 `{skill, category, proficiency}` 字典，不会传字符串）
    - skill 标签显示 `{{ s.skill ?? s.name ?? s }} ({{ s.proficiency ?? '熟悉' }})`（含熟练度）
    - 422 detail 泄露风险：Read `llm_client.py` 异常消息文本确认——若无敏感信息则加注释说明；若有则 sanitize
    - 检查 `backend/app/schemas/extract.py` 是否存在——若已迁移则删 `extract.py` 第 25-43 行内联 Pydantic 模型；未迁移则在分析报告记为低优先级候选

    **Step 6: 静态检查**
    - `cd frontend && npx eslint --quiet src/stores/jd.ts src/pages/ExtractJD.vue`
    - `cd frontend && npm run typecheck`（vue-tsc --noEmit）
    - `cd backend && poetry run ruff check app/api/v1/extract.py app/core/extraction/jd_extract.py`
  </action>
  <verify>
    <automated>cd frontend && npx eslint --quiet src/stores/jd.ts src/pages/ExtractJD.vue && cd ../backend && poetry run ruff check app/api/v1/extract.py app/core/extraction/jd_extract.py</automated>
  </verify>
  <done>
    - 抽取结果透传 tools/learning_resources/evolves_to/hallucinated_skills/missing_skills/issues 六个新字段
    - 反幻觉结果区可见（el-alert + el-collapse 渲染 hallucinated_skills/missing_skills/issues）
    - LLM 502 时 ElMessage 显示后端 detail 而非 axios 默认文案
    - Pydantic fallback 补齐全部字段；anti-hallucination 异常不再冒泡 500
    - cost-summary 折叠区可调用 GET /extract/cost-summary 并展示数据
    - 死代码 fallback 全部清除；skill 标签含熟练度
    - eslint / vue-tsc / ruff 全部通过
  </done>
</task>

<task type="auto">
  <name>Task 4: 业务闭环验证与测试增强</name>
  <files>
    frontend/src/pages/__tests__/ExtractJD.spec.ts
    frontend/src/stores/__tests__/jd.test.ts
  </files>
  <action>
    验证 JD 抽取模块端到端业务闭环，并增强测试覆盖（EXTRACT-01 / EXTRACT-02 / EXTRACT-03）。

    **Step 1: 数据流闭环链路验证（阅读确认，不改代码）**
    - 链路 A（主业务流）: ExtractJD.handleExtract → jd.extractJd(jdText) → request.post('/extract/jd', { jd_content }) → POST /api/v1/extract/jd (main.py 第 211 行 include_router) → extract_jd (extract.py 第 152 行) → extract_from_jd (jd_extract.py 第 401 行) → JDExtractionPipeline.run (7 步：mask_pii → get_prompt → call_llm_with_fallback → parse_llm_json_response → JDExtractionResult 校验 → batch_normalize_skills → validate_extraction) → _build_result → ExtractionResult（含 tools/learning_resources/hallucinated_skills 等新字段）→ 前端 JDExtractResult 消费
    - 链路 B（反幻觉链路）: validate_extraction (llm_client.py 第 373 行) → call_llm_with_fallback → parse_llm_json_response → normalize_skill_list/normalize_str_list (anti_hallucination.py) → AntiHallucinationResult → result["validation"] → _build_result.hallucinated_skills / missing_skills / issues
    - 链路 C（持久化双写）: extract_jd → _write_extraction_to_graph (extract.py 第 93 行) → write_extraction_to_graph + _write_extraction_to_pg (第 126 行) → write_extraction_to_pg (repositories/extract_repo) → PostgreSQL PositionRecord + Neo4j 节点
    - 链路 D（成本）: tracker.record (llm_client.py 第 259 行) → tracker.summary → GET /api/v1/extract/cost-summary → 前端 fetchCostSummary → el-descriptions 折叠区
    - 验证点: 四条链路字段名一致、类型兼容、无静默 undefined；ExtractionResult（含新字段）与前端 JDExtractResult 接口逐字段核对

    **Step 2: 增强 ExtractJD.spec.ts（不删除现有测试）**
    修复 vacuous pass 根源：mock `@/api/request` 模块（保留 store 真实逻辑覆盖，参考 Phase 4 / Phase 5 做法）：
    ```typescript
    vi.mock('@/api/request', () => ({
      default: { get: vi.fn().mockResolvedValue({}), post: vi.fn().mockResolvedValue({}) },
    }))
    ```
    新增用例（shallowMount + 保留现有 stubs，按需追加子组件 stub）：
    - **空文本守卫**: 设置 jdText 为空字符串，触发 handleExtract（直接调用 wrapper.vm.handleExtract），验证 ElMessage.warning 被调用（mock ElMessage），且 request.post 未被调用
    - **抽取成功渲染**: 设置 jdText 为测试 JD 文本，mock request.post('/extract/jd') 返回完整 ExtractionResult（含 tools / hallucinated_skills / missing_skills / normalized_skills），触发 handleExtract，flushPromises 后验证：① result 区域显示 position_name；② 反幻觉 el-alert 渲染（v-if 命中）；③ 归一化表行数 = normalized_skills.length；④ skill tag 含 proficiency（如 "Python (精通)"）
    - **抽取失败态**: mock request.post rejected（模拟 502），验证 ElMessage.error 路径被触发且 message 含后端 detail（不是 "Request failed with status code 502"）
    - **归一化表渲染**: mock 返回 normalized_skills 含 2 条（original/normalized/method/confidence），验证 el-table-column 4 列数据正确
    - **cost-summary 调用**: 触发折叠区按钮（先 verify 按钮存在），mock request.get('/extract/cost-summary') 返回 { models: [...], total_calls: 3 }，验证 el-descriptions 渲染成本数字
    - **proficiency 显示**: 在抽取成功测试基础上，断言技能 tag 文本含 "(精通)" 或 "(熟悉)"

    **Step 3: 新增 store 级测试**
    新建 `frontend/src/stores/__tests__/jd.test.ts`（已有 request mock 基建，参考 learningRecommendation.test.ts）：
    ```typescript
    vi.mock('@/api/request', () => ({
      default: { get: vi.fn(), post: vi.fn() },
    }))
    ```
    - extractJd 正确传递 `{ jd_content: '...' }` 到 request.post('/extract/jd')（断言入参）
    - extractJd 错误透传：mock request.post rejected with `{ response: { data: { detail: 'LLM service unavailable' } } }`，验证抛出 Error message === 'LLM service unavailable'（不是 'Request failed with status code 502'）
    - extractJd 成功路径：mock 返回 ExtractionResult，验证 extractResult.value 被赋值
    - fetchCostSummary：mock 返回成本对象，验证函数返回该对象

    **Step 4: 运行完整测试套件**
    - `cd frontend && npx vitest run src/pages/__tests__/ExtractJD.spec.ts src/stores/__tests__/jd.test.ts --reporter=verbose`
    - `cd frontend && npx vitest run --reporter=verbose`（全量前端）
    - `cd backend && poetry run pytest -x --no-header -q`（全量后端，含抽取相关 tests/unit）
    - 如后端 ExtractionResult 有新字段（tools / learning_resources / hallucinated_skills 等），grep tests/ 下消费 extract 响应的断言同步更新
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/ExtractJD.spec.ts src/stores/__tests__/jd.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ExtractJD.spec.ts 新增至少 5 个测试用例（空文本守卫 / 抽取成功 / 失败态 / 归一化表 / cost-summary / proficiency），mock 层在 @/api/request
    - 新建 jd.test.ts 覆盖 extractJd 入参 / 错误透传 / 成功路径 / fetchCostSummary
    - 四条闭环链路验证完成，字段一致无静默丢失
    - 前端 vitest 全量通过、后端 pytest 全量通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → API | JD 文本 / 简历文件 / 成本查询请求跨此边界 |
| API → LLM Providers | PII 脱敏后的 JD 文本经 HTTPS 发往 MiMo / DeepSeek / 星火 / Qwen 跨此边界 |
| API → Neo4j / PostgreSQL | 抽取结果写入 PositionRecord / Neo4j 节点跨此边界 |
| API → Redis (cost counter) | 成本计数器在多 worker 部署下需 Redis 聚合（当前进程内单例） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | POST /extract/jd 在 LLM 调用前 mask_pii（jd_extract.py 第 35-39、44-51、63-81 行脱敏手机/身份证/邮箱/中文姓名），但若 mask_pii 漏掉某种 PII 模式（地址、银行账号），原始 JD 全文会发往第三方 LLM | medium | mitigate | 当前 PII 模式已覆盖主流 4 类；address/bank account 未覆盖——在 Task 1 分析报告记录为长期风险，不在本阶段实施；强制 task description 中"使用前确认 JD 不含敏感 PII"作为产品契约 |
| T-06-02 | Information Disclosure | HTTPException(detail=str(e))（extract.py 第 171、174、187、238 行）直接把 ValueError / ConnectionError 异常消息透出——若 LLM SDK 异常消息含 API key 片段或内部堆栈 | low | mitigate | Task 3 Step 5 读 llm_client.py 异常消息确认；含敏感信息则 sanitize；无敏感则加注释说明 |
| T-06-03 | Denial of Service | POST /extract/jd 单次调用串联 7 步（含 2 次 LLM 调用：抽取 + 反幻觉），最长链路 ~120s——多并发用户同时点击"抽取"会占满 LLM 配额 | medium | accept | 已有全局限流中间件（main.py）+ 前端 ExtractJD.vue handleExtract 的 progress timer 单飞防护（BUG-004 修复，第 32 行 clearInterval 防双 interval）；反幻觉步骤失败不阻塞主抽取（Task 3 Step 3 修） |
| T-06-04 | Tampering | cost_tracker 进程内单例无持久化（extract.py 第 145-148 行）——多 worker 部署下前端拉取命中不同 worker 拿到不同数据 | low | mitigate | Task 1 分析报告记下风险；当前部署是单 worker（docker-compose.dev.yml）评估可行；多 worker 部署需迁移到 Redis 聚合，记入设计文档 |
| T-06-05 | Repudiation | LLM 抽取结果无审计日志（哪个 prompt 版本、哪个模型、何时调用、token 多少）——除 cost_tracker 的 token 计数外无完整审计 | low | accept | 演示项目语境；audit trail 需 structured logging + ELK 接入，超出本阶段范围；cost_tracker 已记录 model/prompt/content 三元组 |
| T-06-06 | Information Disclosure | LLM 反幻觉步骤对每条抽取都额外调用一次 LLM（jd_extract.py 第 348 行 validate_extraction），包含完整 validated.model_dump() 作为 prompt——可能把内部字段（如 industry/description）发给第三方 | low | accept | AntiHallucinationResult schema 是公开模型输出，反幻觉步骤的 prompt 包含 extraction_json 是设计内行为（提供完整上下文让 LLM 判断合理性）；不可省略 |
</threat_model>

<verification>
## Phase Verification

1. **Code correctness**: 所有修改文件通过 eslint / vue-tsc / ruff
2. **Test pass**: `cd backend && poetry run pytest -x --no-header -q` 通过
3. **Test pass**: `cd frontend && npx vitest run --reporter=verbose` 通过
4. **Analysis quality**: `docs/archive/extract-source-analysis.md` 覆盖 6 层分析并含严重级别标注
5. **Design quality**: `docs/archive/extract-optimization-design.md` 含 3-5 个可行需求
6. **Field alignment**: ExtractionResult（含新字段 tools/learning_resources/evolves_to/hallucinated_skills/missing_skills/issues）与前端 JDExtractResult 接口逐字段一致
7. **Anti-hallucination visibility**: ExtractJD 页面反幻觉结果区渲染（el-alert + el-collapse），is_valid=false 时可见
8. **Error transparency**: LLM 502 时前端 ElMessage 显示后端 detail 而非 axios 默认文案
9. **Cost visibility**: GET /extract/cost-summary 调用成功，折叠区显示成本数字
10. **Robustness**: Pydantic fallback 补齐全部字段；anti-hallucination 异常不再冒泡 500
</verification>

<success_criteria>
## Success Criteria

1. [ ] docs/archive/extract-source-analysis.md 存在，覆盖 6 层源码分析
2. [ ] docs/archive/extract-optimization-design.md 存在，包含 3-5 个优化需求
3. [ ] eslint / vue-tsc / ruff 全部通过
4. [ ] 后端 pytest 全部通过
5. [ ] 前端 vitest 全部通过（含新增 5+ 个 ExtractJD 测试与 jd.test.ts store 测试）
6. [ ] 抽取结果透传 tools/learning_resources/evolves_to/hallucinated_skills/missing_skills/issues 六个新字段
7. [ ] 反幻觉结果区可见（el-alert + el-collapse 渲染）
8. [ ] LLM 502 时 ElMessage 显示后端 detail
9. [ ] cost-summary 折叠区可调用并展示数据
10. [ ] 死代码 fallback 全部清除；skill 标签含熟练度
</success_criteria>

<output>
Create `.planning/phases/06-extract-jd/06-01-SUMMARY.md` when done
</output>