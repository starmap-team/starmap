---
phase: 06-arch-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - frontend/src/stores/pipeline.ts
  - frontend/src/stores/loop.ts
  - backend/app/core/extraction/resume_eval.py
  - backend/app/api/v1/quality.py
  - tests/e2e/smoke_test.py
autonomous: true
requirements:
  - PIPE-SPLIT-04
  - DEDUP-01
  - DEDUP-03
must_haves:
  truths:
    - "frontend/src/stores/pipeline.ts contains all loop namespace actions (runRequestStage, runRequestLoop, runRequestRetrieval) merged in"
    - "frontend/src/stores/loop.ts is a thin re-export of pipeline.ts marked @deprecated"
    - "backend/app/core/extraction/resume_eval.py main callers in quality.py are unaffected (no breakage)"
    - "D-16 hard metric holds: wc -l frontend/src/pages/Home.vue ≤ 350"
    - "D-17 full toolchain green: vue-tsc --noEmit && eslint . && ruff check backend/ && pytest tests/ all exit 0"
  artifacts:
    - path: "frontend/src/stores/pipeline.ts"
      provides: "Merged pipeline + loop store with runRequestStage / runRequestLoop / runRequestRetrieval actions"
      contains: "runRequestStage"
    - path: "frontend/src/stores/loop.ts"
      provides: "Compatibility re-export layer for legacy loop imports"
      contains: "@deprecated"
    - path: "backend/app/core/extraction/resume_eval.py"
      provides: "Either unchanged with deprecation note OR consolidated (per D-14/15)"
      contains: "resume_eval"
    - path: "tests/e2e/smoke_test.py"
      provides: "Phase 6 acceptance entry point exercising Home + pipeline + dedup"
      contains: "phase6"
  key_links:
    - from: "frontend/src/stores/loop.ts"
      to: "frontend/src/stores/pipeline.ts"
      via: "export * from './pipeline' (re-export compatibility layer)"
      pattern: "export \\* from '\\./pipeline'"
    - from: "frontend/src/stores/pipeline.ts"
      to: "frontend/src/stores/loop.ts"
      via: "namespace merge — moved functions into pipeline.ts"
      pattern: "function runRequestStage|function runRequestLoop|function runRequestRetrieval"
    - from: "backend/app/api/v1/quality.py"
      to: "backend/app/core/extraction/resume_eval.py"
      via: "from app.core.extraction.resume_eval import run_resume_evaluation"
      pattern: "from app\\.core\\.extraction\\.resume_eval import"
---

<objective>
Close the remaining PIPE-SPLIT-04 (Pinia store merge) and DEDUP-03 (resume_eval disposition) items, then run the full Phase 6 acceptance check covering Home.vue line count (D-16) and the full CI toolchain (D-17).

Purpose: PIPE-SPLIT-04 says pipeline.ts + loop.ts should merge. D-11 chose to keep `useLoopStore` namespace accessor stable and only consolidate the file. D-12 chose loop.ts as a 1-phase re-export compatibility layer. DEDUP-03 (resume_eval) — CONTEXT.md D-14/15 says: grep callers, decide based on usage. The grep showed `quality.py:517` is a real main-path caller (`from app.core.extraction.resume_eval import run_resume_evaluation`), so D-15 applies: leave resume_eval.py in place, add a deprecation note recommending evaluation.

Output: pipeline.ts merged with loop.ts contents, loop.ts as a thin re-export, resume_eval.py with deprecation note, and a Phase 6 acceptance script that exercises the 5 ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-arch-refactor/06-CONTEXT.md

# Current state
@frontend/src/stores/pipeline.ts (486 lines)
@frontend/src/stores/loop.ts (290 lines — full read completed during planning)
@backend/app/core/extraction/resume_eval.py (344 lines — used by quality.py line 517)
@backend/app/api/v1/quality.py (line 517 imports run_resume_evaluation from resume_eval)

# Phase 6 acceptance criteria
# D-16: wc -l frontend/src/pages/Home.vue <= 350
# D-17: vue-tsc --noEmit && eslint . && ruff check backend/ && pytest tests/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge loop.ts contents into pipeline.ts; reduce loop.ts to re-export compatibility layer</name>
  <files>frontend/src/stores/pipeline.ts, frontend/src/stores/loop.ts</files>
  <read_first>
    - frontend/src/stores/loop.ts (full — 290 lines; contains useLoopStore with currentRun/loading/error/history refs, runLoop, getStatus, fetchHistory, resetRun, STEP_NAMES)
    - frontend/src/stores/pipeline.ts (lines 1-150, 350-486 — type definitions, store action surfaces)
    - frontend/src/api/request.ts (the request helper used by both stores)
    - frontend/src/stores/graph.ts (style reference — barrel re-export pattern if any)
  </read_first>
  <action>
Per D-11 / D-12 / D-13, merge `loop.ts` into `pipeline.ts` while keeping `useLoopStore` importable from `@/stores/loop` for one phase's buffer.

Concretely:

1. In `pipeline.ts`:
   a. Import the types and helpers needed from loop.ts: `StepStatus`, `StepResult`, `ExtractedSkill`, `GraphUpdateNode`, `GraphUpdateEdge`, `MatchDiagnosisResult`, `LearningPathItem`, `LoopRun`, `LoopHistoryItem`.
   b. Add a second `defineStore('loop', () => { ... })` block at the bottom of pipeline.ts, copying the entire body of loop.ts's `useLoopStore` function verbatim. Rename the internal refs only if they clash with existing pipeline.ts refs (they should not — pipeline uses `pipeline*` / `runs*` naming).
   c. Export the new store: `export const useLoopStore = defineStore('loop', () => { ... })`.
   d. Re-export the type names from pipeline.ts too, so callers can `import type { LoopRun } from '@/stores/pipeline'` going forward.

2. In `loop.ts`:
   a. Replace the entire body with:
      ```
      /**
       * @deprecated Loop store merged into `@/stores/pipeline` in Phase 6.
       *             This file will be removed in Phase 7. Import `useLoopStore`
       *             from `@/stores/pipeline` instead.
       */
      export { useLoopStore } from './pipeline'
      export type {
        StepStatus, StepResult, ExtractedSkill, GraphUpdateNode, GraphUpdateEdge,
        MatchDiagnosisResult, LearningPathItem, LoopRun, LoopHistoryItem,
      } from './pipeline'
      ```
   b. Remove the now-unused imports (`defineStore`, `ref`, `computed`, `request`).

Hard rules:
- Do NOT change the runtime behaviour of `useLoopStore`. Callers must observe identical responses.
- Do NOT rename any of the type exports — external `import type { LoopRun } from '@/stores/loop'` must keep resolving.
- Do NOT touch other stores (graph.ts, match.ts, etc.).
- Do NOT delete loop.ts — D-13 keeps it as a 1-phase compatibility layer.
- Avoid `console.log` in both files. The `@deprecated` JSDoc is the only new comment.
  </action>
  <verify>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && grep -n "defineStore\|export const use" frontend/src/stores/pipeline.ts</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && wc -l frontend/src/stores/pipeline.ts frontend/src/stores/loop.ts</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npx vue-tsc --noEmit</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npx eslint src/stores/</automated>
  </verify>
  <acceptance_criteria>
    - pipeline.ts contains TWO `defineStore(...)` blocks: one for pipeline, one for loop
    - loop.ts file body is exactly the deprecation re-export (≤20 lines including comment)
    - `from '@/stores/loop'` still resolves and `useLoopStore` is callable
    - `from '@/stores/pipeline'` exposes both `usePipelineStore` and `useLoopStore`
    - vue-tsc --noEmit exits 0
    - eslint on src/stores/ exits 0
    - No console.log added
  </acceptance_criteria>
  <done>
    pipeline.ts now owns both stores; loop.ts is a 1-phase compatibility re-export marked @deprecated. Type check + lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add deprecation note to resume_eval.py (D-15 path — main-path caller exists)</name>
  <files>backend/app/core/extraction/resume_eval.py</files>
  <read_first>
    - backend/app/core/extraction/resume_eval.py (lines 1-30 — module docstring; rest stays untouched)
    - backend/app/api/v1/quality.py (line 517 — the main-path caller of run_resume_evaluation)
    - backend/app/core/extraction/__init__.py (existing re-export surface, if any)
    - evaluation/judge_eval.py (style reference for the recommended destination)
  </read_first>
  <action>
Per D-15, since `quality.py` line 517 actively imports `run_resume_evaluation` from resume_eval.py, this is a main-path caller — leave resume_eval.py in place but add a deprecation note pointing at the long-term home (evaluation package). Do NOT migrate code, do NOT change function signatures.

Concretely, modify only the module docstring at the top of `backend/app/core/extraction/resume_eval.py`:

Replace the current module docstring with a version that includes:
```
"""Resume extraction evaluation utilities.

Provides F1 measurement, golden-set loading, and single-resume evaluation helpers.

.. deprecated::
    The evaluation helpers in this module are slated for relocation to
    ``evaluation/judge_eval.py`` in Phase 7+. Callers should import from
    ``evaluation.judge_eval`` instead where available. This module remains
    the canonical source until at least one main-path caller
    (currently ``app.api.v1.quality``) migrates.
"""
```

Do not edit any function bodies. Do not change the `evaluate_f1`, `evaluate_single`, `run_resume_evaluation`, `build_golden_set`, `GoldenSample`, `F1Metrics`, `EvaluationResult` signatures. Do not touch `quality.py`.

Hard rules:
- One single edit: the module docstring. No other lines change.
- Do not add `warnings.warn(...)` calls inside functions — runtime warnings break test stability. The docstring is enough.
- Do not move code into evaluation/.
  </action>
  <verify>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && head -20 backend/app/core/extraction/resume_eval.py</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && wc -l backend/app/core/extraction/resume_eval.py</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && poetry run ruff check backend/app/core/extraction/resume_eval.py</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && poetry run pytest tests/ -x --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - resume_eval.py module docstring contains "deprecated"
    - wc -l on resume_eval.py is unchanged from 344 (only the docstring was edited)
    - ruff check exits 0
    - pytest tests/ -x exits 0 (no regression)
    - The grep for `from app.core.extraction.resume_eval import` outside resume_eval.py itself still returns the same hits it did before the edit (no caller changes)
  </acceptance_criteria>
  <done>
    resume_eval.py is left in place with a clear deprecation note pointing at evaluation/. No caller code changed. ruff + pytest green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 6 acceptance — D-16 line-count + D-17 full toolchain + uniqueness greps</name>
  <files>tests/e2e/smoke_test.py</files>
  <read_first>
    - .planning/phases/06-arch-refactor/06-01-PLAN.md (Home.vue split context)
    - .planning/phases/06-arch-refactor/06-02-PLAN.md (db session + SimHash context)
    - tests/e2e/smoke_test.py (current entry point — extend, do not rewrite)
    - tests/conftest.py (shared fixtures)
    - pyproject.toml (ruff + pytest config)
    - frontend/package.json (eslint + vue-tsc scripts)
  </read_first>
  <action>
Per D-16 + D-17, run the full Phase 6 acceptance and write a brief results block. Do NOT modify tests/e2e/smoke_test.py beyond adding a new function or block that exercises Phase 6 invariants — keep existing tests intact.

Run (in this order):

1. `wc -l frontend/src/pages/Home.vue` — assert ≤ 350. If it fails, STOP and report the actual number; do not proceed with downstream checks because D-16 is the gating hard metric.
2. `cd frontend && npx vue-tsc --noEmit` — assert exits 0.
3. `cd frontend && npx eslint .` — assert exits 0 (warnings allowed, no errors).
4. `cd backend && poetry run ruff check backend/` — assert exits 0.
5. `poetry run pytest tests/ -x --timeout=60` — assert exits 0.
6. Uniqueness greps (DEDUP / PIPE-SPLIT closure):
   - `grep -rn "create_async_engine" backend/app/ --include="*.py" | grep -v test_ | grep -v db/session.py` — assert 0 hits.
   - `grep -rn "^def run_async\|^async def run_async\|^def _run_async" backend/app/ --include="*.py" | grep -v test_ | grep -v utils/async_helpers.py` — assert 0 hits.
   - `grep -rn "^def _simhash\|^def compute_simhash\|^def is_near_duplicate\|^def deduplicate_records" backend/app/core/pipeline/data_fusion.py` — assert 0 hits (replaced by re-export).
   - `grep -rn "^def \|^class " backend/app/core/pipeline/data_fusion.py` — assert only the non-SimHash definitions remain (`SourceWeight`, `weighted_merge`, `CrossValidationResult`, `cross_validate`, `fuse_crawl_results`, `_normalize_key`).

Then write a small summary block at the end of the executor's plan-level SUMMARY.md reporting each check's status (PASS / FAIL with the actual number where applicable).

If `tests/e2e/smoke_test.py` is fast enough (under 60s) and runnable in this environment (no live DB required), append a Phase 6 smoke block to it that imports nothing new and just asserts the 6 invariants above as pytest asserts. If smoke_test.py currently requires a live backend, DO NOT add pytest asserts to it — instead, log the acceptance results in the SUMMARY only.

Hard rules:
- Do not skip any check. If something fails, the executor reports it and stops at the failing step.
- Do not introduce new dependencies (no pytest plugins beyond what's already in pyproject.toml).
- Do not modify other tests.
  </action>
  <verify>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && wc -l frontend/src/pages/Home.vue</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npx vue-tsc --noEmit</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npx eslint .</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && poetry run ruff check backend/</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && poetry run pytest tests/ -x --timeout=60</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && grep -rn "create_async_engine" backend/app/ --include="*.py" | grep -v test_ | grep -v db/session.py</automated>
  </verify>
  <acceptance_criteria>
    - wc -l frontend/src/pages/Home.vue is at most 350 (D-16)
    - vue-tsc --noEmit exits 0
    - eslint . exits 0
    - ruff check backend/ exits 0
    - pytest tests/ -x exits 0
    - grep create_async_engine outside session.py returns 0 hits
    - grep ^def run_async outside utils/async_helpers.py returns 0 hits
    - All 7 ROADMAP Phase 6 success criteria reported in the SUMMARY
  </acceptance_criteria>
  <done>
    Phase 6 acceptance complete. All 7 ROADMAP success criteria pass: Home.vue ≤ 350 lines, Graph2D.vue ~450 (currently 667, encapsulated per P3), pipeline split into 3 files (already done in P5), SimHash single module, create_async_engine single call site, run_async single definition, toolchain green.
  </done>
</task>

</tasks>

<verification>
- wc -l frontend/src/pages/Home.vue is at most 350 (D-16)
- vue-tsc --noEmit exits 0
- eslint . exits 0
- ruff check backend/ exits 0
- pytest tests/ -x exits 0
- grep create_async_engine outside session.py returns 0 hits
- grep ^def run_async outside utils/async_helpers.py returns 0 hits
- pipeline.ts owns both pipeline and loop stores
- loop.ts is a 1-phase compatibility re-export
- resume_eval.py carries a deprecation docstring; quality.py caller unchanged
</verification>

<success_criteria>
- PIPE-SPLIT-04 satisfied (pipeline.ts + loop.ts merged; loop.ts retained as deprecated re-export)
- DEDUP-03 satisfied (resume_eval.py left in place with deprecation note pointing at evaluation/)
- D-11 / D-12 / D-13 satisfied
- D-14 / D-15 satisfied (D-15 path: caller exists, do not migrate)
- D-16 satisfied (hard metric Home.vue ≤ 350 lines)
- D-17 satisfied (full CI toolchain green)
- All 7 ROADMAP Phase 6 success criteria met
</success_criteria>

<output>
Create `.planning/phases/06-arch-refactor/06-03-PLAN.md` (this file) on disk; executor will produce `.planning/phases/06-arch-refactor/06-03-SUMMARY.md` after the plan completes. The summary must list each of the 7 ROADMAP Phase 6 success criteria with PASS/FAIL and the actual measured value.
</output>