---
phase: 06-arch-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Home.vue
  - frontend/src/composables/home/useGraphToolbarState.ts
  - frontend/src/composables/home/useHomeLayout.ts
  - frontend/src/composables/home/useEvolutionPanel.ts
  - frontend/src/composables/home/useNodeSelection.ts
  - frontend/src/composables/home/useGraph2DData.ts
  - frontend/src/composables/home/useGraph3DData.ts
  - frontend/src/composables/home/index.ts
autonomous: true
requirements:
  - HOME-SPLIT-01
  - HOME-SPLIT-02
  - HOME-SPLIT-03
  - HOME-SPLIT-04
must_haves:
  truths:
    - "wc -l frontend/src/pages/Home.vue reports 350 lines or fewer"
    - "Home.vue template still renders the Graph2D and Graph3D components via <Graph2D> and <Graph3D> tags"
    - "Graph2D.vue and Graph3D.vue are reused unchanged as child components (per D-01)"
    - "Per D-02, business hooks (useGraphToolbarState / useHomeLayout / useEvolutionPanel / useNodeSelection / useGraph2DData / useGraph3DData) live under frontend/src/composables/home/, each as a single `export function`; component tree stays Home.vue single-layer + existing Graph2D/Graph3D children"
    - "vue-tsc --noEmit and eslint pass with zero new errors after the split"
    - "Smoke test loads Home page route without runtime error"
  artifacts:
    - path: "frontend/src/pages/Home.vue"
      provides: "Slimmed Home.vue — template + minimal script delegating to composables"
      min_lines: 0
      max_lines: 350
    - path: "frontend/src/composables/home/useGraphToolbarState.ts"
      provides: "Toolbar state + handlers (layoutMode, maxNodesLimit, proficiencyFilter)"
      contains: "useGraphToolbarState"
    - path: "frontend/src/composables/home/useHomeLayout.ts"
      provides: "Layout mode / panel sizing state"
      contains: "useHomeLayout"
    - path: "frontend/src/composables/home/useEvolutionPanel.ts"
      provides: "Evolution panel toggle + graph3DEvolutionLinks derivation"
      contains: "useEvolutionPanel"
    - path: "frontend/src/composables/home/useNodeSelection.ts"
      provides: "Selected node state + handler bridge"
      contains: "useNodeSelection"
    - path: "frontend/src/composables/home/useGraph2DData.ts"
      provides: "2D graph data derivation (nodes/edges for G6)"
      contains: "useGraph2DData"
    - path: "frontend/src/composables/home/useGraph3DData.ts"
      provides: "3D graph data derivation (graph3DNodes / graph3DLinks / graph3DEvolutionLinks)"
      contains: "useGraph3DData"
    - path: "frontend/src/composables/home/index.ts"
      provides: "Barrel export of all home composables"
      contains: "export"
  key_links:
    - from: "frontend/src/pages/Home.vue"
      to: "frontend/src/components/Graph2D.vue"
      via: "<Graph2D> template tag"
      pattern: "<Graph2D"
    - from: "frontend/src/pages/Home.vue"
      to: "frontend/src/components/Graph3D.vue"
      via: "<Graph3D> template tag"
      pattern: "<Graph3D"
    - from: "frontend/src/pages/Home.vue"
      to: "frontend/src/composables/home"
      via: "import useGraphToolbarState / useHomeLayout / useEvolutionPanel / useNodeSelection / useGraph2DData / useGraph3DData"
      pattern: "from '@/composables/home'"
    - from: "frontend/src/composables/home/useGraph3DData.ts"
      to: "frontend/src/stores/graph.ts"
      via: "useGraphStore + graphStore.visibleNodes / .visibleEdges / .evolutionPaths"
      pattern: "graphStore\\."
---

<objective>
Reduce Home.vue from 821 lines to ≤350 lines (per D-03 / D-16) by extracting business logic into composables under `frontend/src/composables/home/`, while reusing existing Graph2D.vue (667) and Graph3D.vue (779) as child components (per D-01).

Purpose: HOME-SPLIT-02~04 are the last 20% of the Home.vue split. P5 closed the design tokens / colors / dead-endpoints side, P6 closes the size + structure side. Without this split Home.vue is hard to review, lint, and onboard onto. Meeting the 350-line ceiling is the ROADMAP success criterion #1 and D-16 hard metric.

Output: a slimmed Home.vue (template + minimal script) plus 6 focused composables behind a barrel index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-arch-refactor/06-CONTEXT.md

# Current split target
@frontend/src/pages/Home.vue

# Reused child components (DO NOT MODIFY per D-01)
@frontend/src/components/Graph2D.vue
@frontend/src/components/Graph3D.vue

# Existing composables for reference style
@frontend/src/composables/useKPIMetrics.ts
@frontend/src/composables/usePipelineMonitor.ts
@frontend/src/composables/useSSE.ts

# Store used by Home
@frontend/src/stores/graph.ts

# Phase 5 decisions to preserve (design tokens, color sources)
@.planning/phases/05-style-unify/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 6 composables under frontend/src/composables/home/ and wire Home.vue to them</name>
  <files>
    frontend/src/composables/home/useGraphToolbarState.ts,
    frontend/src/composables/home/useHomeLayout.ts,
    frontend/src/composables/home/useEvolutionPanel.ts,
    frontend/src/composables/home/useNodeSelection.ts,
    frontend/src/composables/home/useGraph2DData.ts,
    frontend/src/composables/home/useGraph3DData.ts,
    frontend/src/composables/home/index.ts,
    frontend/src/pages/Home.vue
  </files>
  <read_first>
    - frontend/src/pages/Home.vue (full file, current 821-line baseline)
    - frontend/src/composables/useKPIMetrics.ts (existing composable style to mirror)
    - frontend/src/composables/usePipelineMonitor.ts (existing composable style to mirror)
    - frontend/src/stores/graph.ts (for shape of GraphNode, visibleNodes, visibleEdges, evolutionPaths)
    - frontend/src/components/Graph2D.vue (props interface — do not modify)
    - frontend/src/components/Graph3D.vue (props interface — do not modify)
    - frontend/src/utils/graphColors.ts (KA_FALLBACK_COLORS, nodeColor)
  </read_first>
  <action>
Per D-01~03, DO NOT modify Graph2D.vue / Graph3D.vue. Extract the following blocks from Home.vue into composables, leaving Home.vue as template + thin <script setup> + scoped <style>. Each composable is a single `export function` named after the file, returning reactive state + handlers exactly as Home.vue already uses them.

Composables (each ≤80 lines, pure TS + Vue Composition API, no JSX):

1. `useGraphToolbarState` — moves these refs and any related handlers from Home.vue: `layoutMode` (LayoutMode = 'force'|'dagre'|'radial'), `maxNodesLimit` (number, default 80), `proficiencyFilter` (string[], default ['精通','熟悉','了解']). Returns `{ layoutMode, maxNodesLimit, proficiencyFilter }` plus any setter helpers actually used.

2. `useHomeLayout` — moves `viewMode` ('2d'|'3d') and `autoRotate3D` (boolean). Returns `{ viewMode, autoRotate3D }`.

3. `useEvolutionPanel` — moves `showEvolution` (boolean) and the existing computed `graph3DEvolutionLinks`. Returns `{ showEvolution, graph3DEvolutionLinks }`.

4. `useNodeSelection` — moves `selectedNode` (GraphNode | null) and the existing handler that bridges Graph2D/Graph3D node clicks into `graphStore.selectedNode` (or whichever selection path the existing code uses — read Home.vue's `onNodeClick` / equivalent first). Returns `{ selectedNode, onNodeSelect }`.

5. `useGraph2DData` — moves the 2D node/edge derivation logic (currently feeding <Graph2D :nodes=... :edges=...>). Read the props of Graph2D.vue to see exact field names. Returns the computed refs exactly as Home.vue currently binds them.

6. `useGraph3DData` — moves `graph3DNodes`, `graph3DLinks` and `graph3DEvolutionLinks` (if not already in useEvolutionPanel — pick the cleaner home). Move `kaColorMap` here too since it is 3D-only data prep. Returns `{ graph3DNodes, graph3DLinks, kaColorMap }`.

7. `frontend/src/composables/home/index.ts` — barrel: `export * from './useGraphToolbarState'` etc. for all 6.

Then in Home.vue:
- Delete the moved code blocks.
- Add a single `import { useGraphToolbarState, useHomeLayout, useEvolutionPanel, useNodeSelection, useGraph2DData, useGraph3DData } from '@/composables/home'` near the top of <script setup>.
- Call each composable and destructure its return values. Keep the names identical to the local bindings the template already uses — this lets the <template> section remain unchanged.
- Keep `cv()` CSS variable resolver and the ECharts `use([...])` imports if they belong to Home's own ECharts radar (do not move them — they are template-rendered, not reusable hooks).
- Preserve all existing imports (MainLayout, GraphToolbar, Graph2D, Graph3D, DetailPanel, GraphSearchBar, useGraphStore, tooltipStyle, KA_FALLBACK_COLORS, nodeColor, useKPIMetrics) — they are used directly in <template> or at the top level.
- Do NOT change the template structure, classes, or scoped styles. The only valid test that the split succeeded is `wc -l Home.vue ≤ 350` AND the dev server / smoke test rendering identically.

Hard rules:
- No new dependencies.
- No `console.log` in the new composables.
- Do not rename refs that the template binds to. If a ref is referenced in <template>, the destructured name must match.
- Keep imports sorted (Element Plus icons block, ECharts block, then app imports — match existing style).
  </action>
  <verify>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && wc -l frontend/src/pages/Home.vue</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap && wc -l frontend/src/composables/home/*.ts</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npm run typecheck 2>&1 | tail -20</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npx eslint src/pages/Home.vue src/composables/home/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l frontend/src/pages/Home.vue` outputs a number ≤ 350
    - All 7 new files exist under `frontend/src/composables/home/` with non-zero content
    - `npm run typecheck` exits 0 (no new TS errors — pre-existing errors not introduced by this plan are out of scope)
    - `npx eslint src/pages/Home.vue src/composables/home/` exits 0
    - Home.vue still imports and renders `<Graph2D>` and `<Graph3D>` (grep `^import Graph2D\|^import Graph3D\|<Graph2D\|<Graph3D` returns matches)
    - Home.vue <script setup> contains exactly one import from `@/composables/home` (the barrel)
    - No `console.log` added to any new composable (`grep -c console.log frontend/src/composables/home/*.ts` returns 0)
  </acceptance_criteria>
  <done>
    Home.vue ≤ 350 lines, all 6 composables extracted, typecheck + eslint clean, Graph2D/Graph3D components untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Home.vue split end-to-end via build + dev-server route load</name>
  <files>
    frontend/src/pages/Home.vue
  </files>
  <read_first>
    - frontend/src/pages/Home.vue (post-extraction state)
    - tests/e2e/smoke_test.py (smoke entry, optional)
  </read_first>
  <action>
Per D-17, full toolchain must pass. Run the frontend production build and confirm no Vue compile errors. If `vite build` exists in package.json scripts, run it; otherwise run `vue-tsc --noEmit && vite build`. The build output should succeed without warnings about unknown components (Graph2D, Graph3D, MainLayout, GraphToolbar, DetailPanel, GraphSearchBar, GraphSearchBar). If smoke test exists locally and fast to run, invoke it targeting / route to confirm runtime render of the slimmed Home.vue. If smoke is too slow for in-line verify, skip and rely on typecheck + build as the binding check.

If anything fails (missing component import, broken ref binding in template), debug and patch in Home.vue (not the composables — the bug will almost always be a missed ref destructuring). Do NOT add new components or restructure the template to fix the build — only fix import/wiring issues.
  </action>
  <verify>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npx vue-tsc --noEmit 2>&1 | tail -10</automated>
    <automated>cd C:/Users/LiShuai/Desktop/Agents/starmap/frontend && npx eslint . 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --noEmit` exits 0
    - `npx eslint .` exits 0 (or shows only pre-existing warnings, no new errors)
    - Home.vue wc -l still ≤ 350 after any fixes
    - All Graph2D/Graph3D/MainLayout/GraphToolbar/DetailPanel/GraphSearchBar component tags in Home.vue resolve (no "Failed to resolve component" warnings)
  </acceptance_criteria>
  <done>
    Frontend build pipeline green for the Home.vue split — typecheck + eslint pass, no template resolution regressions.
  </done>
</task>

</tasks>

<verification>
- Home.vue wc -l ≤ 350 (D-16 hard metric)
- 6 composables under frontend/src/composables/home/ + barrel index
- vue-tsc --noEmit exits 0
- eslint . exits 0
- Graph2D.vue + Graph3D.vue file mtimes unchanged (per D-01 reuse rule)
- No new console.log introduced in extracted composables
</verification>

<success_criteria>
- HOME-SPLIT-02 satisfied (Home.vue ≤ 350 lines)
- HOME-SPLIT-01 preserved (Graph2D.vue already exists; reuse not recreate)
- HOME-SPLIT-03 satisfied (G6 init/render/highlight/resize already encapsulated in Graph2D per P3 — this plan does not regress that boundary)
- HOME-SPLIT-04 satisfied (2D/3D mode toggle is via `viewMode` ref now centralised in `useHomeLayout`; `grep -n graph3DReady frontend/src/pages/Home.vue` returns 0 lines — verified absent in current baseline 821 行 file, so no `graph3DReady` hack to remove)
- D-16 met: hard line-count metric
- D-17 frontend half met: vue-tsc + eslint green
</success_criteria>

<output>
Create `.planning/phases/06-arch-refactor/06-01-PLAN.md` (this file) on disk; executor will produce `.planning/phases/06-arch-refactor/06-01-SUMMARY.md` after the plan completes.
</output>