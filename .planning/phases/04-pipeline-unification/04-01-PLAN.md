---
phase: 04-pipeline-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/pipeline/orchestrator.py
  - backend/app/core/pipeline/loop_orchestrator.py
  - backend/app/core/pipeline/bootstrap.py
  - backend/app/core/pipeline/cron_scheduler.py
autonomous: true
requirements: [ARCH-01, PIPE-02, PIPE-03]
user_setup: []
must_haves:
  truths:
    - "Phase 2 已处理 executor.py 和 pipeline/routes.py 的 except Exception 替换"
    - "Phase 3 已为 SSE Pipeline 添加单元测试"
    - "loop_orchestrator.py 和 orchestrator.py 的异常处理统一为 PipelineError 层次"
  artifacts:
    - loop_orchestrator.py 步骤级错误处理升级
    - 死爬虫代码清理（移除 v2ex_remote 假平台注册）
    - 现有测试全部通过
---

<objective>
统一 Pipeline 核心模块的异常处理，清理死爬虫代码，建立阶段性统一基础。

**Purpose:** Phase 2 已处理 executor.py 的异常，Phase 4 接管剩余 Pipeline 模块。死爬虫代码（v2ex_remote 假平台）是技术债务，清理后减少维护负担。

**Output:**
- loop_orchestrator.py 步骤级错误处理升级
- 死爬虫代码清理
- 所有测试通过
</objective>

<execution_context>
@backend/app/core/pipeline/loop_orchestrator.py
@backend/app/core/pipeline/orchestrator.py
@backend/app/core/pipeline/bootstrap.py
@backend/app/core/pipeline/cron_scheduler.py
</execution_context>

<tasks>

<task type="tracer">
  <name>Task 1: Upgrade loop_orchestrator.py error handling</name>
  <files>backend/app/core/pipeline/loop_orchestrator.py</files>
  <action>
    Read backend/app/core/pipeline/loop_orchestrator.py. Find all `except Exception` blocks and replace with:
    - `PipelineStageError` for pipeline stage failures
    - `StarMapError` for domain errors (re-raise)
    - `Exception` as last resort with `logger.exception()`
  </action>
  <verify>
    <automated>cd backend && python -c "from app.core.pipeline.loop_orchestrator import *"</automated>
  </verify>
  <done>- loop_orchestrator.py 异常处理升级完成</done>
  <reversibility rating="reversible">机械性重构</reversibility>
</task>

<task type="auto">
  <name>Task 2: Clean up dead crawler code (v2ex_remote facade registrations)</name>
  <files>backend/app/core/pipeline/executor.py</files>
  <action>
    Read backend/app/core/pipeline/executor.py. Search for `v2ex_remote`, `v2ex`, and dead platform registration code. Remove:
    - Fake platform registrations that point to v2ex_remote but are never used
    - Commented-out crawl platform references
    - Dead code blocks that reference non-existent platforms

    This is a cleanup-only task — do not remove functional code.
  </action>
  <verify>
    <automated>grep -c "v2ex_remote\|v2ex" backend/app/core/pipeline/executor.py || echo "0 found"</automated>
  </verify>
  <done>- 死爬虫代码清理完成，平台注册更清晰</done>
  <reversibility rating="reversible">清理死代码，git revert 可恢复</reversibility>
</task>

<task type="auto">
  <name>Task 3: Pipeline orchestrator.py error handling upgrade</name>
  <files>backend/app/core/pipeline/orchestrator.py</files>
  <action>
    Read backend/app/core/pipeline/orchestrator.py. Find all remaining `except Exception` (Phase 2 might have handled some). Replace remaining with PipelineError hierarchy.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.core.pipeline.orchestrator import *"</automated>
  </verify>
  <done>- orchestrator.py 异常处理升级完成</done>
  <reversibility rating="reversible">机械性重构</reversibility>
</task>

</tasks>

<verification>
1. cd backend && python -c "from app.core.pipeline.orchestrator import *"
2. cd backend && python -c "from app.core.pipeline.loop_orchestrator import *"
3. cd backend && python -m pytest tests/unit/ -q --no-cov 2>&1 | tail -5
</verification>

<success_criteria>
- [ ] loop_orchestrator.py except Exception 从 15 处减少到 ≤3
- [ ] orchestrator.py except Exception 从 6 处减少到 ≤2
- [ ] 死爬虫代码清理（v2ex_remote 引用移除）
- [ ] 所有测试通过
</success_criteria>
</output>