---
wave: 2
depends_on: [PLAN-01]
files_modified:
  - frontend/src/stores/learning.ts
  - frontend/src/stores/learningPlan.ts (new)
  - frontend/src/stores/learningRecommendation.ts (new)
  - frontend/src/stores/learningAnalytics.ts (new)
  - frontend/src/stores/pipeline.ts
  - frontend/src/stores/pipelineRun.ts (new)
  - frontend/src/stores/pipelineConfig.ts (new)
  - frontend/src/stores/__tests__/learning.test.ts
  - frontend/src/stores/__tests__/learningPlan.test.ts (new)
  - frontend/src/stores/__tests__/learningRecommendation.test.ts (new)
  - frontend/src/stores/__tests__/learningAnalytics.test.ts (new)
  - frontend/src/stores/__tests__/pipeline.test.ts
  - frontend/src/stores/__tests__/pipelineRun.test.ts (new)
  - frontend/src/stores/__tests__/pipelineConfig.test.ts (new)
autonomous: true
requirements: [REFACTOR-03, REFACTOR-04]
---

# PLAN-02: Store Splitting (Wave 2)

**Wave:** 2 (blocked on Wave 1 — component imports must be stable)
**Goal:** Split learning.ts (481→3 stores <200 lines each) and pipeline.ts (468→2 stores).

## Tasks

### Task 2.1: Split learning.ts into 3 stores

- `learningPlan.ts` — Plan types, mappers, createPlan, fetchPlan, fetchPlans, updateProgress, addSkillToPlan, restorePlanFromLocalStorage
- `learningRecommendation.ts` — Recommendation types, fetchRecommendations, batch match
- `learningAnalytics.ts` — Competitiveness, career path, industry trends

Keep `learning.ts` as a re-export barrel for backward compatibility:
```ts
export { useLearningPlanStore } from './learningPlan'
export { useLearningRecommendationStore } from './learningRecommendation'
export { useLearningAnalyticsStore } from './learningAnalytics'
export const useLearningStore = () => ({
  ...useLearningPlanStore(),
  ...useLearningRecommendationStore(),
  ...useLearningAnalyticsStore(),
})
```

### Task 2.2: Split pipeline.ts into 2 stores

- `pipelineRun.ts` — Run types, run state, run actions, SSE handlers, liveEvents, qualityAlerts
- `pipelineConfig.ts` — Schedule types, config types, schedule CRUD, config CRUD

Keep `pipeline.ts` as a re-export barrel.

### Task 2.3: Update test files

Split learning.test.ts into 3 test files, pipeline.test.ts into 2 test files.

## Acceptance Criteria

- Each new store < 200 lines
- learning.ts and pipeline.ts remain as re-export barrels (backward compat)
- All existing consumers work without import changes
- `cd frontend && npx vitest run` — 0 failures
