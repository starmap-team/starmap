---
phase: 16-pipeline-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/pipeline/executor.py
  - backend/app/core/dashboard/sse_broadcaster.py
  - backend/app/core/pipeline/health_monitor.py
  - backend/app/api/v1/pipeline/routes.py
  - backend/app/services/import_service.py
  - backend/tests/integration/test_pipeline_e2e.py
autonomous: true
requirements:
  - AUDIT-BE-01 (Backend 功能 + 状态机审计)
must_haves:
  truths:
    - 触发 pipeline 后所有 5 阶段实际执行（不只在爬虫阶段停了）
    - SSE reconnect 在 token 失效后 < 30s 恢复
    - cancel 操作正确传递语义，上游保留/下游 skipped/cancelled
    - retry 单个阶段不重跑整条 pipeline
    - error message 全部人类可读
    - success_rate 计算不包含 cancelled（与 failed 区分）
  artifacts:
    - backend/tests/integration/test_pipeline_e2e.py (NEW 端到端集成测试)
  key_links:
    - SSE reconnect 流程：前端 useSSE → silentRefreshForSSE → 重新连接
    - cancel 流程：前端 cancelRun → /runs/{id}/cancel → STOP flag + 标记 cancelled
    - retry 流程：前端 PipelineStageCard retry 按钮 → /runs/{id}/retry/{stage}
---

# Plan 16-01: Backend 功能 + 状态机审计

## 目标

通过真实运行测试（不是代码审查）发现 backend 功能 bug，列出已知问题并修复。

## Task 1: 创建端到端集成测试 [backend]

**文件:** `backend/tests/integration/test_pipeline_e2e.py`

```python
"""E2E 集成测试 — 真实触发 pipeline 走完所有阶段"""
import asyncio
import time
from datetime import datetime, UTC
import httpx
import pytest

API = "http://localhost:8000/api/v1"


@pytest.mark.asyncio
async def test_full_pipeline_runs_all_stages():
    """触发 full pipeline → 验证所有 5 阶段最终 completed/failed 不是 skipped"""
    async with httpx.AsyncClient(timeout=30) as client:
        # 登录
        tok_resp = await client.post(f"{API}/auth/login", json={"username": "admin", "password": "starmap2024"})
        tok = tok_resp.json()["access_token"]
        headers = {"Authorization": f"Bearer {tok}"}

        # 触发
        resp = await client.post(f"{API}/pipeline/trigger",
            json={"run_type": "full", "selected_stages": ["crawl", "dedup", "clean", "import", "graph_sync"]},
            headers=headers)
        run_id = resp.json()["run_id"]

        # 等待 60s
        for _ in range(60):
            stages = (await client.get(f"{API}/pipeline/stages", headers=headers)).json()["stages"]
            if all(s["status"] in ("completed", "failed", "skipped") for s in stages):
                break
            await asyncio.sleep(1)

        # 验证
        stages = {s["name"]: s for s in (await client.get(f"{API}/pipeline/stages", headers=headers)).json()["stages"]}
        for name in ["crawl", "dedup", "clean", "import"]:
            assert name in stages, f"{name} missing"
            # 不能是 pending (如果实际有数据)
            assert stages[name]["status"] in ("completed", "failed"), \
                f"{name} stuck in {stages[name]['status']}"
            assert stages[name]["progress"] is not None, \
                f"{name} has no progress"


@pytest.mark.asyncio
async def test_cancel_mid_pipeline_preserves_upstream():
    """中途 cancel → 上游保留 records，下游 skipped/cancelled"""
    # 类似上面，触发后 5s cancel
    # 验证 upstream completed records 保留
    pass


@pytest.mark.asyncio
async def test_retry_single_stage():
    """retry 单个 failed stage 不重跑上游"""
    pass


@pytest.mark.asyncio
async def test_sse_reconnect_within_30s():
    """模拟 token 失效 → SSE 应在 30s 内自动 reconnect"""
    pass
```

## Task 2: Audit 已知 backend 问题 [backend]

### Issue A: SSE 持续断连无自动恢复

**症状:** 用户截图显示 "SSE 连不上" / "实时推送已断开" 长期存在

**调查:**
- 检查 `backend/app/core/dashboard/sse_broadcaster.py` 的 heartbeat 机制
- 确认 token refresh 后是否触发 SSE reconnect

**修复:** (若缺失)
- 加 Redis pub/sub 失败重试 + 自动 fallback 到 polling
- 加 ElMessage.success("实时推送已恢复") on reconnect (前端)
- 加 reconnect_attempts 指标

### Issue B: success_rate 计算含 cancelled

**症状:** success_rate 31% 看起来很低（但实际不包含 cancelled）

**调查:**
- `backend/app/core/pipeline/status_aggregator.py:51-72`
- 当前公式: `completed / (completed + failed)`

**修复:**
- 公式改为: `completed / (completed + failed)` 不变，但 UI 显示 tooltip 解释
- 增加 "近 24h" 视图（区分 7 天和 24h）

### Issue C: 数字口径不一致 (移到 Plan 16-02 — 前端文案)

**症状:** KPI 110 vs DAG 146 vs crawl 描述 171

**修复:** 见 Plan 16-02 Issue G (前端文案修改)

### Issue D: 错误消息用户友好化（已完成）

executor.py:740 已实现错误分类翻译

## Task 3: 自动化测试 [backend]

- 所有 Task 1 的 e2e 测试纳入 CI
- 失败时报告哪个 stage 停了 + 当前进度
- 添加 fixture 自动清理测试产生的 pipeline_runs

## Task 4: 索引迁移修正 [backend] (Fix M2 from review)

**文件:** `backend/alembic/versions/025_add_pipeline_indexes.py`

```python
"""Phase 16-01: 添加 pipeline_runs 索引 (使用 IF NOT EXISTS 避免冲突)"""
def upgrade():
    op.execute("CREATE INDEX IF NOT EXISTS idx_pipeline_runs_status_started ON pipeline_runs(status, started_at)")
    op.execute("CREATE INDEX IF NOT EXISTS idx_data_source_metrics_source_started ON data_source_metrics(source_id, started_at)")

def downgrade():
    op.execute("DROP INDEX IF EXISTS idx_pipeline_runs_status_started")
    op.execute("DROP INDEX IF EXISTS idx_data_source_metrics_source_started")
```

## 验收 (调整后)

- [ ] 5/5 e2e 测试通过
- [ ] 触发 → 完成时间 < 90s
- [ ] SSE reconnect < 30s
- [ ] cancel 后 5s 内 UI 反映 cancelled
- [ ] 错误消息全部人类可读
- [ ] 索引迁移成功应用 (IF NOT EXISTS 避免冲突)
- **预算: 3 天**

---

# Plan 16-02: Frontend 状态延迟 + 渲染审计

## 目标

测量前端状态延迟（触发 → UI 反映），发现渲染 bug。

## Task 1: 延迟测量脚本 [frontend]

**文件:** `frontend/scripts/measure_latency.py`

```python
"""测量 pipeline 状态从前端 API 反映延迟"""
import asyncio
import httpx
import time

API = "http://localhost:8000/api/v1"

async def measure_trigger_to_ui():
    """从触发到 /stages 反映的延迟"""
    async with httpx.AsyncClient() as client:
        # 登录
        ...

        # 触发 + 立即开始轮询 stages
        t0 = time.monotonic()
        await client.post(f"{API}/pipeline/trigger", ...)
        for i in range(120):  # poll 120s
            stages = (await client.get(f"{API}/pipeline/stages")).json()["stages"]
            # 检查是否出现 crawl=running
            if any(s["status"] == "running" for s in stages):
                t_running = time.monotonic()
                print(f"Trigger → UI shows running: {t_running - t0:.2f}s")
                break
            await asyncio.sleep(0.5)
```

## Task 2: 已知前端问题审计 [frontend]

### Issue E: SSE reconnect 无 toast 反馈

**症状:** 后端修好了 SSE 自动重连，但前端无 toast 提示

**修复:** `frontend/src/composables/useSSE.ts`
- `silentRefreshForSSE` 成功后调用 `ElMessage.success("实时推送已恢复")`

### Issue F: stage card 0% on completed

**症状:** 截图显示 crawl "已完成 0%"

**根因:** 之前 zombie run progress=None；已修

**额外修复:** `PipelineStageCard.vue`
- 加 fallback: 如果 `progress === null/undefined` 且 status=completed → 显示 100% (而非 0%)
- 加 fallback: 如果 status=skipped → 隐藏进度条

### Issue G: 数据源 0 records 解释不清

**症状:** UI 显示 7 个数据源，但大部分 0 条

**修复:** `frontend/src/components/DataSourceManager.vue`
- 加 tooltip 区分 "从未爬取" vs "本周期无新数据"
- 显示最近一次爬取时间 (last_crawl_at)

### Issue H: 自动爬虫 KPI 卡 "7 个" 但 DAG 显示不同

**症状:** KPI 卡 "7 个可用数据源" vs 数据源管理页表格

**修复:** 统一字段

## Task 3: 渲染审计 [frontend]

### 检查清单

- [ ] 所有 el-tag/数字有 unit test (Vue Test Utils)
- [ ] 进度条 v-if 条件不会导致 0% 显示在 completed
- [ ] SSE onmessage 不会重复 dispatch
- [ ] timelineStages 在 stages 为空时不卡死
- [ ] 多源聚合去重: 一个 source data 既出现在 KPI 又出现在 DAG 不重复

## 验收

- [ ] 延迟测量: Trigger → UI shows running < 2s (P50)
- [ ] SSE 重连有 toast
- [ ] 数据源 0 records 有 tooltip 解释
- [ ] 渲染单元测试 100% 通过

---

# Plan 16-03: 跨端一致性 + 性能瓶颈

## 目标

抽样验证 API / Page / DB 三端数据 100% 一致，找出性能瓶颈。

## Task 1: 跨端一致性抽样 [backend + frontend]

```python
@pytest.mark.parametrize("i", range(20))
async def test_three_tier_consistency(i):
    """抽样验证 API / DB 一致性 (前端用浏览器 E2E)"""
    async with httpx.AsyncClient() as client:
        # 登录
        ...

        # 1. 获取 API 返回的 stages
        api_stages = (await client.get(f"{API}/pipeline/stages")).json()["stages"]

        # 2. 直接查 DB
        from sqlalchemy import select
        from app.models.pipeline_models import PipelineRun
        ...
        db_run = (await session.execute(select(PipelineRun)...)).scalar_one()
        db_stages = db_run.stages

        # 3. 对比每个字段
        for api_s, db_s in zip(api_stages, db_stages):
            assert api_s["name"] == db_s["name"]
            assert api_s["status"] == db_s["status"]
            assert api_s["records_processed"] == db_s["records_processed"]
            assert abs(api_s["progress"] - (db_s.get("progress") or 0)) < 0.01
```

## Task 2: 性能瓶颈分析 [backend]

### 测量目标

- `/pipeline/stages` P50/P95/P99 响应时间
- DB query plan 分析（EXPLAIN ANALYZE）
- SSE publish → Redis 延迟

### 改进方向

- 加 DB 索引（如 `idx_pipeline_runs_status_started_at`）
- 加 Redis 缓存 (status 5s TTL)
- 加 N+1 query 检测

## Task 3: 报告与建议

最终输出：
- 一致性报告 (N=20 抽样)
- 性能瓶颈清单
- 后续 Phase 建议

## 验收

- [ ] 20/20 抽样 100% 一致
- [ ] `/pipeline/stages` P95 < 100ms
- [ ] 性能改进建议列表 (后续 Phase)