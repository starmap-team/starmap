---
phase: 08-backend-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - .env.example
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
must_haves:
  truths:
    - "后端启动时检测 LLM key，至少一个非空否则输出 WARNING 日志（不阻止启动）"
    - "后端启动时检测 DB 密码占位值，开发环境 WARNING 生产环境 RuntimeError"
    - ".env.example 包含 MIMO_API_KEY、DEEPSEEK_API_KEY、PROXY_LIST 字段"
    - ".env.example 包含降级链优先级注释 MiMo->DeepSeek->Xunfei->Ollama"
    - "ruff check + mypy app 通过 (per D-11)"
  artifacts:
    - path: "backend/app/config.py"
      provides: "LLM key 启动校验逻辑"
      contains: "mimo_api_key"
    - path: ".env.example"
      provides: "完整的环境变量模板"
      contains: "MIMO_API_KEY"
  key_links:
    - from: "backend/app/config.py"
      to: "backend/app/core/extraction/llm_client.py"
      via: "settings.mimo_api_key / settings.deepseek_api_key 被 llm 降级链消费"
      pattern: "settings\\.(mimo|deepseek|xunfei)_api_key"
    - from: ".env.example"
      to: "backend/app/config.py"
      via: "环境变量名对应 Settings 字段"
      pattern: "MIMO_API_KEY|mimo_api_key"
---

<objective>
在 config.py 的 model_validator 中添加 LLM key 启动校验（WARNING 不阻止），确认 DB 密码校验覆盖完整，完善 .env.example 模板。

Purpose: D-04 LLM key 校验仅 warning 不阻止启动（Ollama 本地可降级）；D-07 .env.example 补全 MIMO/DEEPSEEK/PROXY_LIST + 降级链注释；D-08 启动时输出可验证的 WARNING 日志。
Output: 扩展的 config.py model_validator、完善的 .env.example。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 LLM key 启动校验 + 确认 DB 密码校验 (CFG-01, CFG-02 per D-04, D-08)</name>
  <files>backend/app/config.py, backend/tests/unit/test_config.py</files>
  <read_first>
    - backend/app/config.py (完整阅读：Settings 类字段 L12-119，model_validator L124-169，_UNCONFIGURED 常量 L9，LLM 字段 L58-71)
    - backend/app/core/extraction/llm_client.py (阅读 L1-40，了解降级链如何消费 mimo_api_key/deepseek_api_key/xunfei_api_key)
    - backend/tests/unit/test_config.py (若存在，阅读现有测试 pattern；若不存在则新建)
  </read_first>
  <behavior>
    - Test 1: Settings 所有 LLM key 为空时，实例化后 logger 输出 WARNING 含 "MIMO_API_KEY" 和 "DEEPSEEK_API_KEY"
    - Test 2: Settings 仅 mimo_api_key 非空时，不输出 LLM WARNING（DB 密码校验仍可能 WARNING）
    - Test 3: Settings 所有 DB 密码为 CHANGE_ME_IN_ENV 且 app_env=development 时，输出 WARNING
    - Test 4: Settings 所有 DB 密码为 CHANGE_ME_IN_ENV 且 app_env=production 时，raise RuntimeError
  </behavior>
  <action>
    在 config.py 的 `_resolve_postgres_uri_and_warn` model_validator 方法中，在现有 DB 密码校验逻辑之后（L167 `return self` 之前）添加 LLM key 校验块（per D-04, D-08）：

    1. 定义 LLM key 字段集合（复用现有 sensitive_fields pattern）：
       `llm_keys = {"MIMO_API_KEY": self.mimo_api_key, "DEEPSEEK_API_KEY": self.deepseek_api_key, "XUNFEI_API_KEY": self.xunfei_api_key}`
    2. 过滤出空值（非空字符串视为已配置）：
       `missing_llm = [name for name, value in llm_keys.items() if not value]`
    3. 若全部为空（len(missing_llm) == 3），输出 WARNING：
       `logger.warning("⚠️  以下 LLM 供应商未配置 API key：{}。将降级使用本地 Ollama（质量较低）。如需高质量抽取，请在 .env 中配置至少一个云端 LLM key。", ", ".join(missing_llm))`
    4. 注意：不阻止启动（不 raise），开发和生产都仅 warning（per D-04：Ollama 本地模型始终可用作降级）

    CFG-02 确认：现有 L134-138 的 sensitive_fields 已包含 secret_key、neo4j_password、postgres_password 三项。无需修改，仅确认覆盖完整。在代码中无操作。

    测试文件：若 backend/tests/unit/test_config.py 不存在则创建。使用 pytest + caplog 或 unittest.mock.patch 验证 logger.warning 调用。测试应构造 Settings 实例（通过 monkeypatch 环境变量或直接实例化 Settings 子类）验证 warning 输出。
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_config.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - config.py 的 model_validator 方法含 `mimo_api_key` 和 `deepseek_api_key` 和 `xunfei_api_key` 三个字段引用
    - config.py 不含针对 LLM key 的 `raise RuntimeError`（仅 DB 密码在生产环境 raise）
    - config.py 含 `降级使用本地 Ollama` 字符串（warning 文案）
    - config.py 的 sensitive_fields 仍包含 secret_key、neo4j_password、postgres_password 三项（CFG-02 覆盖确认）
    - test_config.py 存在且包含至少 4 个测试用例
    - pytest test_config.py 全部通过
  </acceptance_criteria>
  <done>config.py model_validator 含 LLM key warning 逻辑；DB 密码校验确认覆盖完整；test_config.py 通过</done>
</task>

<task type="auto">
  <name>Task 2: 完善 .env.example 模板 (CFG-03 per D-07)</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (完整阅读：现有 LLM 区域 L33-41，前端区域 L52-54)
    - backend/app/config.py L58-71 (LLM 字段定义：mimo_api_key, deepseek_api_key, xunfei_api_key, qwen_model_path)
    - backend/app/core/extraction/llm_client.py (降级链顺序确认)
  </read_first>
  <action>
    在 .env.example 的 LLM 区域（L33-41 之间）执行以下修改（per D-07）：

    1. 在 XUNFEI 三字段之前添加降级链优先级注释：
       `# LLM 降级链优先级：MiMo(主用) -> DeepSeek -> Xunfei -> Ollama(本地兜底)`
       `# 至少配置一个云端 LLM key 以获得高质量抽取；未配置时自动降级到本地 Ollama`

    2. 在 XUNFEI 三字段之后添加 MiMo 和 DeepSeek 字段：
       ```
       # 小米 MiMo（主用 LLM，OpenAI 兼容端点，推理模型）
       MIMO_API_KEY=
       # DeepSeek（备选 LLM）
       DEEPSEEK_API_KEY=
       ```

    3. 在 LLM 区域之后（QWEN_MODEL_PATH 之后、应用区域之前）添加代理配置：
       ```
       # ---------- 爬虫代理 ----------
       # 代理列表（逗号分隔，boss 爬虫轮换使用；留空则直连）
       # 格式：http://ip:port 或 http://user:pass@ip:port
       PROXY_LIST=
       ```

    保留现有 XUNFEI 三字段和 QWEN_MODEL_PATH 不变。
  </action>
  <verify>
    <automated>grep -c "MIMO_API_KEY\|DEEPSEEK_API_KEY\|PROXY_LIST\|降级链" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example 含 `MIMO_API_KEY=` 行
    - .env.example 含 `DEEPSEEK_API_KEY=` 行
    - .env.example 含 `PROXY_LIST=` 行
    - .env.example 含 `降级链优先级` 字符串
    - .env.example 含 `MiMo` 和 `DeepSeek` 和 `Xunfei` 和 `Ollama` 降级链顺序
    - .env.example 仍含 XUNFEI_API_KEY、XUNFEI_API_SECRET、XUNFEI_APP_ID（原有字段未删除）
    - .env.example 仍含 QWEN_MODEL_PATH（原有字段未删除）
  </acceptance_criteria>
  <done>.env.example 含 MIMO_API_KEY、DEEPSEEK_API_KEY、PROXY_LIST 字段及降级链注释</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars -> Settings | Environment variables cross into app config at startup |
| config -> runtime | Settings validated once at startup via model_validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | config.py model_validator | mitigate | LLM key warning logs only provider names (MIMO_API_KEY), not key values |
| T-08-04 | Spoofing | .env.example template | accept | Template contains empty values only; no real secrets in committed file |
| T-08-SC | Tampering | no package installs | accept | No npm/pip/cargo installs; config.py and .env.example are existing files |
</threat_model>

<verification>
1. `cd backend && python -c "from app.config import Settings; s = Settings(); print('config OK')"` 不报错（model_validator 不阻止启动）
2. `cd backend && python -m pytest tests/unit/test_config.py -x -q` 全部通过
3. `grep -c "MIMO_API_KEY" .env.example` 输出 >= 1
4. `grep -c "DEEPSEEK_API_KEY" .env.example` 输出 >= 1
5. `grep -c "PROXY_LIST" .env.example` 输出 >= 1
6. `cd backend && poetry run ruff check app/config.py` 通过 (per D-11)
7. `cd backend && poetry run mypy app/config.py` 通过 (per D-11)
</verification>

<success_criteria>
- config.py model_validator 含 LLM key warning（不 raise）
- DB 密码校验确认覆盖 secret_key/neo4j_password/postgres_password
- .env.example 含 MIMO_API_KEY、DEEPSEEK_API_KEY、PROXY_LIST + 降级链注释
- ruff check + mypy 通过 (per D-10, D-11)
</success_criteria>

<output>
Create `.planning/phases/08-backend-cleanup/08-02-SUMMARY.md` when done
</output>
