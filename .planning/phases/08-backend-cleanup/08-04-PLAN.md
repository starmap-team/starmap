---
phase: 08-backend-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/composables/useAdminReset.ts
  - frontend/src/stores/datasource.ts
  - frontend/src/api/schema.ts
  - frontend/src/pages/Admin.vue
autonomous: true
requirements: [DEMO-02]
must_haves:
  truths:
    - "useAdminReset.ts 文件已删除"
    - "datasource.ts 不含 resetToDemo 函数定义和导出"
    - "schema.ts 不含 resetDemoData 操作和 /admin/seed/reset 路径"
    - "Admin.vue 不含重置演示数据按钮和 useAdminReset import"
    - "vue-tsc --noEmit 通过（无断引用）"
    - "eslint 通过（无未使用变量）"
  artifacts:
    - path: "frontend/src/stores/datasource.ts"
      provides: "无 resetToDemo 的 datasource store"
      contains: "NOT resetToDemo"
    - path: "frontend/src/api/schema.ts"
      provides: "无 resetDemoData 的 API schema"
      contains: "NOT resetDemoData"
    - path: "frontend/src/pages/Admin.vue"
      provides: "无重置按钮的 Admin 页面"
      contains: "NOT useAdminReset"
  key_links:
    - from: "frontend/src/pages/Admin.vue"
      to: "frontend/src/stores/datasource.ts"
      via: "store 不再暴露 resetToDemo"
      pattern: "resetToDemo|handleReset"
    - from: "frontend/src/api/schema.ts"
      to: "starmap-contracts/openapi.yaml"
      via: "gen:api 生成（Plan 08-01 已移除 openapi 路径）"
      pattern: "resetDemoData|seed/reset"
---

<objective>
删除前端 reset-demo 相关代码：useAdminReset.ts、datasource.ts resetToDemo、schema.ts resetDemoData、Admin.vue 按钮。

Purpose: D-03 前端删除"重置演示数据"按钮、useAdminReset.ts composable、datasource.ts resetToDemo() action、schema.ts resetDemoData 类型。与 Plan 08-01 后端删端点对应协调。
Output: 清洁的前端代码，无 reset-demo 引用，vue-tsc + eslint 通过。

注意：不要运行 `npm run gen:api`。Plan 08-01 正在并行修改 openapi.yaml，此时 gen:api 会读到未更新的契约导致冲突。手动删除 schema.ts 中的 resetDemoData 条目即可。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 reset-demo 前端代码 (DEMO-02 frontend per D-03)</name>
  <files>frontend/src/composables/useAdminReset.ts, frontend/src/stores/datasource.ts, frontend/src/api/schema.ts, frontend/src/pages/Admin.vue</files>
  <read_first>
    - frontend/src/composables/useAdminReset.ts (完整阅读：29 行，含 handleReset 函数)
    - frontend/src/stores/datasource.ts (阅读 L205-207 resetToDemo 函数定义，L233 resetToDemo 导出，L209-234 return 块)
    - frontend/src/api/schema.ts (阅读 L163-169 /admin/seed/reset 路径接口，L1222-1233 resetDemoData 操作类型)
    - frontend/src/pages/Admin.vue (阅读 L1-19 imports 含 L17 useAdminReset import，L87-88 handleReset 解构，L467-481 演示数据管理 tab + 按钮)
  </read_first>
  <action>
    1. 删除文件 `frontend/src/composables/useAdminReset.ts`（整个文件删除）

    2. 在 frontend/src/stores/datasource.ts 中：
       - 删除 `resetToDemo` 函数定义（L205-207：`async function resetToDemo() { await request.post('/admin/seed/reset') }`）
       - 删除 return 块中的 `resetToDemo,` 导出行（L233）
       - 确认删除后 return 块最后一个元素 `rejectGraphNode,` 后无尾随逗号问题（ESLint 会自动处理或手动删除尾随逗号）

    3. 在 frontend/src/api/schema.ts 中：
       - 删除 `/admin/seed/reset` 路径接口块（L163-169，从 `"/admin/seed/reset": {` 到对应的 `};` 闭合，约 7 行）
       - 删除 `resetDemoData` 操作类型块（L1222-1233，从 `/** 重置演示数据 */` 注释到 `};` 闭合，约 12 行）
       - 注意：这是自动生成的文件，手动删除需精确匹配大括号层级。删除后确保前后块之间无语法错误。

    4. 在 frontend/src/pages/Admin.vue 中：
       - 删除 import：`import { useAdminReset } from '@/composables/useAdminReset'`（L17）
       - 删除 composable 解构：`const { handleReset } = useAdminReset(admin)`（L87-88，含注释 `// 重置数据 (Phase 7 D round 12)`）
       - 删除"演示数据管理" tab-pane（L465-481 区域：`<el-tab-pane label="演示数据管理">` 到对应 `</el-tab-pane>`，含 el-card、demo-desc、el-button）
       - 若 el-tabs 内仅剩一个 tab-pane，检查是否需要调整 el-tabs 结构（保留 el-tabs wrapper，仅删除该 tab-pane）

    不要修改 `frontend/src/stores/admin.ts`（admin store 本身不直接调用 resetToDemo，是通过 datasource store 间接调用）。
    不要运行 `npm run gen:api`（与 Plan 08-01 并行执行，openapi.yaml 可能尚未更新）。
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | tail -5; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/composables/useAdminReset.ts 文件不存在
    - frontend/src/stores/datasource.ts 不含字符串 `resetToDemo`
    - frontend/src/api/schema.ts 不含字符串 `resetDemoData`
    - frontend/src/api/schema.ts 不含字符串 `seed/reset`
    - frontend/src/pages/Admin.vue 不含字符串 `useAdminReset`
    - frontend/src/pages/Admin.vue 不含字符串 `handleReset`
    - frontend/src/pages/Admin.vue 不含字符串 `演示数据管理`（tab label）
    - frontend/src/pages/Admin.vue 不含字符串 `重置为演示数据`（按钮文本）
    - vue-tsc --noEmit 退出码 0（无类型错误）
  </acceptance_criteria>
  <done>useAdminReset.ts 已删除；datasource.ts 无 resetToDemo；schema.ts 无 resetDemoData；Admin.vue 无按钮和 import；vue-tsc 通过</done>
</task>

<task type="auto">
  <name>Task 2: eslint 验证 + 残留引用检查 (per D-10, D-11)</name>
  <files>frontend/src/stores/datasource.ts, frontend/src/api/schema.ts, frontend/src/pages/Admin.vue</files>
  <read_first>
    - frontend/src/stores/datasource.ts (确认 Task 1 修改后无尾随逗号或未使用变量)
    - frontend/src/pages/Admin.vue (确认删除后 import 区无断引用)
  </read_first>
  <action>
    1. 运行 eslint 检查修改过的文件：
       `cd frontend && npx eslint src/stores/datasource.ts src/api/schema.ts src/pages/Admin.vue --fix`
       若 eslint --fix 自动修复了格式问题（如尾随逗号），接受修复结果。

    2. 全项目搜索残留引用：
       `cd frontend && grep -rn "useAdminReset\|resetToDemo\|resetDemoData\|seed/reset\|reset-demo" src/`
       若有任何匹配，说明有遗漏引用需清理。预期输出为空（退出码 1）。

    3. 检查 Admin.vue 中是否有因删除 useAdminReset 而产生的未使用 import（如 Delete icon 仅用于重置按钮）：
       - 查看 Admin.vue L9 `import { Search, Delete, Plus, Edit } from '@element-plus/icons-vue'`
       - 若 Delete icon 仅用于已删除的重置按钮，从 import 中移除 `Delete`
       - 若 Delete icon 还用于其他按钮（如删除图谱节点），保留

    4. 运行 vue-tsc 最终验证：
       `cd frontend && npx vue-tsc --noEmit`
       退出码必须为 0。
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/stores/datasource.ts src/api/schema.ts src/pages/Admin.vue && npx vue-tsc --noEmit; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - eslint 对 3 个修改文件无 error（warning 可接受）
    - `grep -rn "useAdminReset\|resetToDemo\|resetDemoData\|seed/reset\|reset-demo" frontend/src/` 退出码 1（无匹配）
    - vue-tsc --noEmit 退出码 0
    - Admin.vue 的 @element-plus/icons-vue import 不含未使用的 icon（若 Delete 仅用于重置按钮则已移除）
  </acceptance_criteria>
  <done>eslint 无 error；无残留 reset-demo 引用；vue-tsc 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> API | Removing reset-demo button eliminates one client->API call path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | Admin.vue script changes | mitigate | vue-tsc + eslint verification ensures no broken imports or type errors |
| T-08-10 | Information Disclosure | schema.ts cleanup | accept | Removing resetDemoData type reduces information surface; no secrets in type definitions |
| T-08-SC | Tampering | no package installs | accept | No npm installs; all changes are file deletion and code removal |
</threat_model>

<verification>
1. `cd frontend && npx vue-tsc --noEmit` 退出码 0 (per D-11 前端)
2. `cd frontend && npx eslint src/stores/datasource.ts src/api/schema.ts src/pages/Admin.vue` 无 error
3. `cd frontend && grep -rn "useAdminReset\|resetToDemo\|resetDemoData\|seed/reset\|reset-demo" src/` 退出码 1（无残留）
4. `test ! -f frontend/src/composables/useAdminReset.ts` 退出码 0（文件已删除）
5. `cd frontend && npm run test 2>/dev/null` 通过（若有前端测试引用 Admin 页面，确认不回归 per D-10）
</verification>

<success_criteria>
- useAdminReset.ts 已删除
- datasource.ts 无 resetToDemo
- schema.ts 无 resetDemoData / seed/reset
- Admin.vue 无重置按钮 / useAdminReset import
- vue-tsc + eslint 通过 (per D-10, D-11)
- 无残留 reset-demo 引用
</success_criteria>

<output>
Create `.planning/phases/08-backend-cleanup/08-04-SUMMARY.md` when done
</output>
