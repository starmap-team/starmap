---
phase: 08-backend-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/admin.py
  - backend/app/api/v1/quality.py
  - backend/scripts/expand_graph.py
  - backend/scripts/seed_pipeline_runs_demo.py
  - backend/scripts/seed_expansion_data_demo.py
  - backend/scripts/seed_datasources_demo.py
  - backend/scripts/seed_cross_domain_demo.py
  - scripts/seed_demo_data.py
  - scripts/seed_jd_data.py
  - scripts/seed_position_skill_records.py
  - scripts/seed_skill_timeseries.py
  - scripts/seed_hardcoded_profiles.py
  - starmap-contracts/openapi.yaml
  - backend/tests/unit/test_admin_endpoints.py
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04]
must_haves:
  truths:
    - "admin.py 中不存在 _DEMO_REVIEW_SEED 常量和 auto-seed 逻辑"
    - "review_queue 空表时返回空列表，不自动插入 demo 数据"
    - "/admin/seed/reset 和 /reset-demo 端点已删除，ResetDemoResponse 模型已删除"
    - "quality.py 不再推荐运行 seed_expansion_data_demo.py"
    - "expand_graph.py 不再推荐运行 seed_expansion_data_demo.py"
    - "所有 demo seed 脚本文件头含 ARCHIVE 注释"
    - "openapi.yaml 不含 /admin/seed/reset 路径"
    - "pytest 全部通过（admin 测试同步更新不回归）"
  artifacts:
    - path: "backend/app/api/v1/admin.py"
      provides: "无 demo 数据生成逻辑的 admin API"
      contains: "NOT _DEMO_REVIEW_SEED"
    - path: "backend/app/api/v1/quality.py"
      provides: "不推荐 demo 脚本的质量报告"
    - path: "starmap-contracts/openapi.yaml"
      provides: "无 reset-demo 端点的 API 契约"
  key_links:
    - from: "backend/app/api/v1/admin.py"
      to: "backend/tests/unit/test_admin_endpoints.py"
      via: "test 覆盖 admin 端点行为"
      pattern: "test_review_queue|TestAdminResetDemo"
    - from: "starmap-contracts/openapi.yaml"
      to: "frontend/src/api/schema.ts"
      via: "gen:api 生成"
      pattern: "resetDemoData|seed/reset"
---

<objective>
移除后端所有 demo/auto-seed 数据生成逻辑，清理 seed 脚本引用，归档 demo 脚本，同步 API 契约。

Purpose: D-01 完全移除 _DEMO_REVIEW_SEED 和 auto-seed；D-03 删 reset-demo 端点 + 契约；D-06 脚本原地归档。确保后端不再生成或推荐假数据（DEMO-01~04）。
Output: 清洁的 admin.py、quality.py、expand_graph.py；归档注释的 demo 脚本；同步的 openapi.yaml；更新的测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 auto-seed 逻辑和 reset-demo 端点 (DEMO-01, DEMO-02 per D-01, D-03)</name>
  <files>backend/app/api/v1/admin.py, backend/tests/unit/test_admin_endpoints.py</files>
  <read_first>
    - backend/app/api/v1/admin.py (完整阅读：_DEMO_REVIEW_SEED L78-83, ResetDemoResponse L73-75, auto-seed L190-194, reset_demo_seed L308-327)
    - backend/tests/unit/test_admin_endpoints.py (阅读 test_review_queue_auto_seeds_when_empty L255-263, TestAdminResetDemo L352-372, FakeAsyncSession L67-89)
  </read_first>
  <action>
    在 admin.py 中执行以下删除（per D-01, D-03）：

    1. 删除 `ResetDemoResponse` 类定义（L73-75，class ResetDemoResponse(BaseModel) 及其两个字段 ok/review_items）
    2. 删除 `_DEMO_REVIEW_SEED` 常量列表（L78-83，含 4 个 demo 审核项字典）
    3. 在 `get_review_queue` 函数中删除 auto-seed 逻辑：移除 L183-194 的 `total_count` 查询和 `if total_count == 0:` 块（含 for 循环 session.add 和 await session.commit）。保留 L196-217 的 pending 查询和返回逻辑。更新函数 docstring 从 "Return pending review items from DB; auto-seed if table is empty." 改为 "Return pending review items from DB; returns empty list when table is empty."
    4. 删除 `reset_demo_seed` 函数及其两个路由装饰器（L308-327：@router.post("/seed/reset")、@router.post("/reset-demo")、async def reset_demo_seed 及其函数体）

    在 test_admin_endpoints.py 中执行以下更新：

    5. 删除 `TestAdminResetDemo` 类（L352-372，含 test_reset_demo_returns_200、test_reset_demo_alias、test_reset_demo_requires_admin 三个测试方法）
    6. 将 `test_review_queue_auto_seeds_when_empty`（L255-263）重命名为 `test_review_queue_returns_empty_when_table_empty`，更新断言：删除 `assert len(session._added) == 4`，改为断言 `resp.json()["items"]` 为空列表 `[]`。FakeAsyncSession 传入参数从 `[FakeResult(0), FakeResult([])]` 改为 `[FakeResult([])]`（不再需要 total_count 查询，只需 pending 查询返回空）
    7. 更新测试文件头注释中的端点计数（如 "Covers all 26 endpoints" 改为 "Covers all 24 endpoints"，减少 2 个已删除端点）

    不要删除 data_sources 表数据或相关代码（per D-02：5 站点记录视为真实配置保留）。
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_admin_endpoints.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - admin.py 不含字符串 `_DEMO_REVIEW_SEED`（grep -c 为 0）
    - admin.py 不含字符串 `ResetDemoResponse`
    - admin.py 不含字符串 `reset_demo_seed`
    - admin.py 不含字符串 `/seed/reset`
    - admin.py 不含字符串 `/reset-demo`
    - admin.py 的 get_review_queue docstring 含 "returns empty list when table is empty"
    - test_admin_endpoints.py 不含 `TestAdminResetDemo` 类
    - test_admin_endpoints.py 不含 `auto_seeds` 字符串
    - test_admin_endpoints.py 含 `test_review_queue_returns_empty_when_table_empty` 且断言 items 为空列表
    - pytest tests/unit/test_admin_endpoints.py 全部通过
  </acceptance_criteria>
  <done>admin.py 中无 _DEMO_REVIEW_SEED、ResetDemoResponse、reset-demo 端点；空表返回空列表；测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 清理 seed 脚本引用文案 (DEMO-03 per D-03 处置)</name>
  <files>backend/app/api/v1/quality.py, backend/scripts/expand_graph.py</files>
  <read_first>
    - backend/app/api/v1/quality.py L545-559（recommendations.append 区域，L557 是目标行）
    - backend/scripts/expand_graph.py L712-723（targets_missed 区域，L719 是目标行）
  </read_first>
  <action>
    1. 在 quality.py L557 将推荐文案从：
       `recommendations.append(f"图谱技能数偏少 ({dashboard.total_skills}), 建议运行 seed_expansion_data_demo.py 扩充")`
       改为：
       `recommendations.append(f"图谱技能数偏少 ({dashboard.total_skills}), 建议触发 pipeline run 采集真实数据")`

    2. 在 expand_graph.py L719 将 print 文案从：
       `print("  (Run seed_expansion_data_demo.py to fill remaining gaps)")`
       改为：
       `print("  (Trigger a pipeline run to fill remaining gaps)")`

    两处修改仅替换文案，不改变控制流或变量引用。
  </action>
  <verify>
    <automated>cd backend && grep -n "seed_expansion_data_demo" app/api/v1/quality.py scripts/expand_graph.py; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - quality.py 不含字符串 `seed_expansion_data_demo`
    - expand_graph.py 不含字符串 `seed_expansion_data_demo`
    - quality.py L557 区域含字符串 `建议触发 pipeline run 采集真实数据`
    - expand_graph.py L719 区域含字符串 `Trigger a pipeline run`
    - grep 退出码为 1（无匹配 = 清理完成）
  </acceptance_criteria>
  <done>quality.py 和 expand_graph.py 均不再推荐运行 seed_expansion_data_demo.py</done>
</task>

<task type="auto">
  <name>Task 3: 归档 demo 脚本 + 同步 openapi 契约 (DEMO-04, DEMO-02 契约 per D-06, D-03)</name>
  <files>backend/scripts/seed_pipeline_runs_demo.py, backend/scripts/seed_expansion_data_demo.py, backend/scripts/seed_datasources_demo.py, backend/scripts/seed_cross_domain_demo.py, scripts/seed_demo_data.py, scripts/seed_jd_data.py, scripts/seed_position_skill_records.py, scripts/seed_skill_timeseries.py, scripts/seed_hardcoded_profiles.py, starmap-contracts/openapi.yaml</files>
  <read_first>
    - backend/scripts/seed_pipeline_runs_demo.py (文件头 1-10 行，确认 docstring 位置)
    - backend/scripts/seed_expansion_data_demo.py (文件头 1-10 行)
    - backend/scripts/seed_datasources_demo.py (文件头 1-10 行)
    - backend/scripts/seed_cross_domain_demo.py (文件头 1-10 行)
    - scripts/seed_demo_data.py (文件头 1-10 行)
    - scripts/seed_jd_data.py (文件头 1-10 行)
    - scripts/seed_position_skill_records.py (文件头 1-10 行)
    - scripts/seed_skill_timeseries.py (文件头 1-10 行)
    - scripts/seed_hardcoded_profiles.py (文件头 1-10 行)
    - starmap-contracts/openapi.yaml L1280-1300 (/admin/seed/reset 路径定义)
  </read_first>
  <action>
    1. 在以下 9 个 demo 脚本的文件 docstring 之后（module docstring 结束后的第一行）插入归档注释：
       `# ARCHIVE: 非生产用，仅开发演示。v2.1 真实数据切换后不再推荐运行。`

       目标文件：
       - backend/scripts/seed_pipeline_runs_demo.py
       - backend/scripts/seed_expansion_data_demo.py
       - backend/scripts/seed_datasources_demo.py
       - backend/scripts/seed_cross_domain_demo.py
       - scripts/seed_demo_data.py
       - scripts/seed_jd_data.py
       - scripts/seed_position_skill_records.py
       - scripts/seed_skill_timeseries.py
       - scripts/seed_hardcoded_profiles.py

       注释位置：在文件 module docstring（三引号块）结束后的第一行。若文件无 docstring，则在文件第一行之后插入。

       不要归档以下脚本（服务真实功能）：
       - backend/scripts/seed_chroma.py（向量库初始化）
       - backend/scripts/seed_changelog.py（变更日志）

    2. 在 starmap-contracts/openapi.yaml 中删除 `/admin/seed/reset` 路径定义（L1280-1298，从 `  /admin/seed/reset:` 到其 responses 块结束的空行，约 19 行）。删除后下一个路径 `/admin/graph/nodes` 应紧跟前一个路径。

    不要运行 `npm run gen:api`（前端 schema.ts 由 Plan 08-04 手动同步，两 plan 并行执行时 gen:api 会读到未更新的 openapi 导致冲突）。
  </action>
  <verify>
    <automated>cd backend && grep -rl "ARCHIVE: 非生产用" scripts/ ../scripts/ 2>/dev/null | wc -l && grep -c "seed/reset" ../starmap-contracts/openapi.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 9 个 demo 脚本文件均含字符串 `# ARCHIVE: 非生产用，仅开发演示`
    - backend/scripts/seed_chroma.py 不含 ARCHIVE 注释
    - backend/scripts/seed_changelog.py 不含 ARCHIVE 注释
    - openapi.yaml 不含字符串 `/admin/seed/reset`
    - openapi.yaml 不含字符串 `resetDemoData`
    - openapi.yaml 中 /admin/graph/nodes 路径紧随 /admin/audit/{item_id}/reject 路径之后（无 seed/reset 隔断）
  </acceptance_criteria>
  <done>9 个 demo 脚本含 ARCHIVE 注释；openapi.yaml 不含 /admin/seed/reset 路径；seed_chroma.py 和 seed_changelog.py 未被归档</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API route -> DB | admin.py routes access review_queue table; removing auto-seed changes write path |
| openapi.yaml -> frontend gen | Contract changes propagate to frontend via gen:api |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | admin.py router dependencies | accept | require_admin dependency on router unchanged; only demo code removed, auth surface same |
| T-08-02 | Information Disclosure | openapi.yaml | accept | Removing endpoint path reduces attack surface; no sensitive data in contract |
| T-08-SC | Tampering | no package installs | accept | No npm/pip/cargo installs in this plan; all changes are code deletion and comments |
</threat_model>

<verification>
1. `cd backend && python -m pytest tests/unit/test_admin_endpoints.py -x -q` 全部通过
2. `cd backend && grep -rn "_DEMO_REVIEW_SEED\|ResetDemoResponse\|reset_demo_seed\|/seed/reset\|/reset-demo" app/` 退出码 1（无匹配）
3. `cd backend && grep -rn "seed_expansion_data_demo" app/ scripts/` 退出码 1（无匹配）
4. `grep -c "seed/reset" starmap-contracts/openapi.yaml` 输出 0
5. `grep -rl "ARCHIVE: 非生产用" backend/scripts/ scripts/` 返回 9 个文件
6. `cd backend && poetry run ruff check app/api/v1/admin.py app/api/v1/quality.py scripts/expand_graph.py` 通过 (per D-11)
7. `cd backend && poetry run mypy app/api/v1/admin.py app/api/v1/quality.py` 通过 (per D-11)
</verification>

<success_criteria>
- admin.py 无 _DEMO_REVIEW_SEED、无 ResetDemoResponse、无 reset-demo 端点
- review_queue 空表返回空列表
- quality.py 和 expand_graph.py 不推荐 demo 脚本
- 9 个 demo 脚本含 ARCHIVE 注释
- openapi.yaml 不含 /admin/seed/reset
- pytest test_admin_endpoints.py 通过
- ruff check + mypy 通过 (per D-10, D-11)
</success_criteria>

<output>
Create `.planning/phases/08-backend-cleanup/08-01-SUMMARY.md` when done
</output>
