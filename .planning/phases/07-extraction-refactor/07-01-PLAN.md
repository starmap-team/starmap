---
phase: 07-extraction-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/extraction/normalize.py
  - backend/app/core/extraction/jd_extract.py
  - backend/app/core/extraction/llm_client.py
  - backend/app/core/extraction/anti_hallucination.py
  - backend/tests/unit/test_extraction_normalize.py
  - backend/tests/unit/test_extraction_anti_hallucination.py
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03]
user_setup: []
must_haves:
  truths:
    - "normalize.py 有模块级可变状态 SKILL_ALIAS / _REVERSE_INDEX"
    - "jd_extract.py 包含反幻觉检查逻辑，可以提取为独立模块"
    - "LLM 客户端接口当前是函数调用，可以契约化"
  artifacts:
    - normalize.py 重构为类/工厂函数封装
    - 反幻觉检查器独立模块
    - 测试覆盖
    - 所有现有测试通过
---

<objective>
重构技能抽取核心模块，消除模块级可变状态，契约化接口，提取反幻觉检查器。

**Purpose:** normalize.py 的模块级可变状态（SKILL_ALIAS/REVERSE_INDEX）在 import 时被修改，导致测试间污染和并发问题。反幻觉检查器嵌入在 jd_extract.py 中无法单独测试。

**Output:**
- normalize.py 封装为类/工厂模式
- 反幻觉检查器独立为 anti_hallucination.py
- 新增测试覆盖
- 所有测试通过
</objective>

<execution_context>
@backend/app/core/extraction/normalize.py
@backend/app/core/extraction/jd_extract.py
@backend/app/core/extraction/resume_extract.py
</execution_context>

<tasks>

<task type="tracer">
  <name>Task 1: Extract anti-hallucination checker from jd_extract.py</name>
  <files>backend/app/core/extraction/anti_hallucination.py, backend/tests/unit/test_extraction_anti_hallucination.py</files>
  <action>
    Read backend/app/core/extraction/jd_extract.py. Find the anti-hallucination logic (skill confidence scoring, weird skill filtering, grammar checks). Extract into a new module `backend/app/core/extraction/anti_hallucination.py`.

    The new module should export a class or function that:
    - Takes an extracted skill name + confidence score
    - Returns a boolean (is_valid) + adjusted confidence score
    - Has no module-level mutable state

    Pattern:
    ```python
    class AntiHallucinationChecker:
        def check_skill(self, skill_name: str, raw_name: str, confidence: float) -> tuple[bool, float]:
            ...
    ```

    Then update jd_extract.py to import from anti_hallucination instead of inline logic.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.core.extraction.anti_hallucination import AntiHallucinationChecker; c = AntiHallucinationChecker(); print('PASS')"</automated>
  </verify>
  <done>- 反幻觉检查器独立模块创建完成，jd_extract.py 引用更新</done>
  <reversibility rating="reversible">可逆，git revert 安全</reversibility>
</task>

<task type="auto">
  <name>Task 2: Refactor normalize.py to encapsulate module-level mutable state</name>
  <files>backend/app/core/extraction/normalize.py</files>
  <action>
    Read backend/app/core/extraction/normalize.py. The module has:
    - SKILL_ALIAS: dict (191 groups) — module-level mutable state
    - _REVERSE_INDEX: dict — built from SKILL_ALIAS at import time

    Refactor strategy:
    - Wrap SKILL_ALIAS and _REVERSE_INDEX in a class `SkillNormalizer`
    - Keep the module-level functions as thin wrappers for backward compatibility
    - Add a `get_normalizer()` factory function

    ```python
    class SkillNormalizer:
        def __init__(self, skill_alias_path: str | None = None):
            self._aliases: dict[str, list[str]] = {}
            self._reverse_index: dict[str, str] = {}
            self._load_aliases(skill_alias_path)

        def normalize(self, skill_name: str) -> str:
            ...

        def get_aliases(self, skill_name: str) -> list[str]:
            ...
    ```

    This is a refactoring — keep the existing public API signatures working.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.core.extraction.normalize import normalize_skill; print('PASS:', normalize_skill('Python'))"</automated>
  </verify>
  <done>- normalize.py 封装完成，向后兼容</done>
  <reversibility rating="reversible">可逆，但需注意 YAML 配置路径</reversibility>
</task>

<task type="auto">
  <name>Task 3: Write tests for encapsulated modules</name>
  <files>backend/tests/unit/test_extraction_normalize.py</files>
  <action>
    Create tests for the newly encapsulated SkillNormalizer class:
    1. test_skill_normalizer_init — verify construction
    2. test_skill_normalizer_normalize — verify normalization works
    3. test_skill_normalizer_get_aliases — verify alias retrieval
    4. test_skill_normalizer_unknown_skill — verify unknown skill handling
    5. test_skill_normalizer_reverse_index — verify reverse index lookup

    Also create tests for anti_hallucination module:
    1. test_anti_hallucination_check_valid — valid skill passes
    2. test_anti_hallucination_check_invalid — invalid skill filtered
    3. test_anti_hallucination_confidence_adjustment — confidence scoring
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_extraction_normalize.py --tb=short --no-cov -q 2>&1 | tail -5