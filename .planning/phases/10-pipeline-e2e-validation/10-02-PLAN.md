---
phase: 10-pipeline-e2e-validation
plan: 02
type: execute
wave: 1
files_modified:
  - crawler/middleware/__init__.py
  - crawler/middleware/proxy_middleware.py
  - crawler/spiders/boss.py
  - backend/tests/unit/test_proxy_breaker.py
autonomous: true
requirements: [PIPE-02]
---

# 10-02: PROXY_LIST 逐项试用 + 失败熔断中间件

**Goal:** (D-02) 新增 `crawler/middleware/proxy_middleware.py`，实现模块级熔断字典（5 分钟 ≥3 次连接失败 → 5 分钟冷却），逐项试用 `PROXY_LIST` 中的代理；未设置时 WARN + 直连；与 `crawler/compliance.py:_load_proxies()` 简单 random.choice 行为解耦。

## Tasks

<task id="T1" title="创建 crawler/middleware/__init__.py 与 proxy_middleware.py">
<read_first>
- crawler/compliance.py:96-130（现有 `_PROXY_LIST` 模块级 + `_load_proxies()` + `get_proxy()` 实现 — 沿用解析逻辑）
- crawler/spiders/boss.py:106-114（当前 proxy URL 解析 — `http://user:pass@host:port` 拆分）
- crawler/stealth.py（确认 `StealthConfig.proxy` 是字符串字段）
</read_first>
<action>
1. 新建 `crawler/middleware/__init__.py` 空文件
2. 新建 `crawler/middleware/proxy_middleware.py`：

```python
"""PIPE-02: PROXY_LIST 代理池 + 失败熔断中间件 (D-02)

行为契约：
1. 解析 PROXY_LIST (逗号分隔 http://[user:pass@]host:port)
2. 每次请求前从池中按"轮询 + 熔断状态"选一个代理
3. 失败计数：最近 5 分钟内同代理累计 ≥3 次连接失败 → 进入 5 分钟冷却
4. 冷却结束后重新接受该代理
5. PROXY_LIST 未设置 / 全部代理冷却中 → 警告日志 + 直连（不阻断）

模块级 dict 存储熔断状态（_BREAKER_STATE），单进程足够；
cluster 部署推 v2.2 改 Redis (deferred)。
"""
from __future__ import annotations

import os
import time
from dataclasses import dataclass
from urllib.parse import urlparse

import logging

log = logging.getLogger(__name__)


@dataclass
class ProxyEntry:
    raw: str           # 原始 URL
    scheme: str        # http / https / socks5
    host: str
    port: int
    user: str | None
    password: str | None

    def to_playwright(self) -> str:
        """转 Playwright proxy server URL: http://[user:pass@]host:port"""
        auth = ""
        if self.user and self.password:
            auth = f"{self.user}:{self.password}@"
        return f"{self.scheme}://{auth}{self.host}:{self.port}"


@dataclass
class _Breaker:
    fail_window_start: float  # 窗口起点（monotonic time）
    fail_count: int
    cooldown_until: float     # 0 = 不在冷却

    def is_open(self, now: float) -> bool:
        return self.cooldown_until > now


_BREAKER_STATE: dict[str, _Breaker] = {}
_PROXY_ENTRIES: list[ProxyEntry] = []
_LAST_PROXY_INDEX: int = 0  # 轮询游标


WINDOW_SECONDS = 5 * 60      # 5 分钟窗口
COOLDOWN_SECONDS = 5 * 60    # 5 分钟冷却
FAIL_THRESHOLD = 3           # 窗口内 ≥3 失败 → 冷却


def _parse_proxy(raw: str) -> ProxyEntry | None:
    """解析 http://[user:pass@]host:port / socks5://host:port"""
    raw = raw.strip()
    if not raw:
        return None
    try:
        p = urlparse(raw)
        if not p.hostname or not p.port:
            return None
        return ProxyEntry(
            raw=raw,
            scheme=p.scheme or "http",
            host=p.hostname,
            port=p.port,
            user=p.username,
            password=p.password,
        )
    except Exception:
        return None


def load_proxies() -> list[ProxyEntry]:
    """读 PROXY_LIST 环境变量并缓存到模块级变量。"""
    global _PROXY_ENTRIES
    raw = os.getenv("PROXY_LIST", "")
    out: list[ProxyEntry] = []
    for item in raw.split(","):
        entry = _parse_proxy(item)
        if entry:
            out.append(entry)
    _PROXY_ENTRIES = out
    return out


def pick_proxy() -> str | None:
    """轮询选择 — 跳过冷却中的代理；池空 / 全部冷却 → 返回 None（直连）。

    返回的是原始 URL（proxy_user / proxy_pass 解析由调用方做，与现有
    boss.py 行为一致）。
    """
    if not _PROXY_ENTRIES:
        load_proxies()
    if not _PROXY_ENTRIES:
        return None
    now = time.monotonic()
    # 至少轮询一圈找到第一个非冷却的代理
    for _ in range(len(_PROXY_ENTRIES)):
        global _LAST_PROXY_INDEX
        _LAST_PROXY_INDEX = (_LAST_PROXY_INDEX + 1) % len(_PROXY_ENTRIES)
        entry = _PROXY_ENTRIES[_LAST_PROXY_INDEX]
        breaker = _BREAKER_STATE.get(entry.raw)
        if breaker is None or not breaker.is_open(now):
            return entry.raw
    log.warning("All proxies in cooldown; falling back to direct connection")
    return None


def record_proxy_failure(proxy_raw: str) -> None:
    """记录一次失败。同一代理 5 分钟内累计 ≥3 → 触发 5 分钟冷却。"""
    breaker = _BREAKER_STATE.get(proxy_raw)
    now = time.monotonic()
    if breaker is None:
        _BREAKER_STATE[proxy_raw] = _Breaker(
            fail_window_start=now, fail_count=1, cooldown_until=0,
        )
        return
    # 窗口滑出（已超 5 分钟）→ 重置
    if now - breaker.fail_window_start > WINDOW_SECONDS:
        breaker.fail_window_start = now
        breaker.fail_count = 1
        return
    breaker.fail_count += 1
    if breaker.fail_count >= FAIL_THRESHOLD and breaker.cooldown_until <= now:
        breaker.cooldown_until = now + COOLDOWN_SECONDS
        log.warning(
            "Proxy {} hit {} failures in {}s, cooling down for {}s",
            proxy_raw, breaker.fail_count, WINDOW_SECONDS, COOLDOWN_SECONDS,
        )


def record_proxy_success(proxy_raw: str) -> None:
    """成功调用 — 重置该代理的失败计数（但保留 cooldown_until 不变直到自然过期）。"""
    breaker = _BREAKER_STATE.get(proxy_raw)
    if breaker is not None:
        breaker.fail_count = 0
        breaker.fail_window_start = time.monotonic()


def reset_for_tests() -> None:
    """仅测试用 — 清空模块级熔断状态。"""
    global _BREAKER_STATE, _PROXY_ENTRIES, _LAST_PROXY_INDEX
    _BREAKER_STATE = {}
    _PROXY_ENTRIES = []
    _LAST_PROXY_INDEX = 0


__all__ = [
    "ProxyEntry",
    "load_proxies",
    "pick_proxy",
    "record_proxy_failure",
    "record_proxy_success",
    "reset_for_tests",
]
```

3. **不要修改** `crawler/compliance.py:_load_proxies()` / `get_proxy()` — 保留行为，10-03 阶段在 boss.py 内可选切换（如要切换则通过 PROXY_LIST 配置天然共存）
</action>
<acceptance_criteria>
- `cat crawler/middleware/proxy_middleware.py` 包含 `class ProxyEntry` 和 `class _Breaker`
- 包含常量 `WINDOW_SECONDS = 5 * 60`, `COOLDOWN_SECONDS = 5 * 60`, `FAIL_THRESHOLD = 3`
- 包含函数 `load_proxies / pick_proxy / record_proxy_failure / record_proxy_success / reset_for_tests`
- 模块顶部 docstring 引用 "PIPE-02" 与 "D-02"
- `python -c "from crawler.middleware.proxy_middleware import ProxyEntry, pick_proxy, record_proxy_failure, record_proxy_success, load_proxies, reset_for_tests"` exit 0
</acceptance_criteria>
</task>

<task id="T2" title="crawler/spiders/boss.py 接失败计数钩子">
<read_first>
- crawler/spiders/boss.py:38-92（fetch_one — 当前未上报失败次数给 proxy breaker）
- crawler/middleware/proxy_middleware.py（T1 产出 — pick_proxy / record_proxy_failure）
</read_first>
<action>
1. 编辑 `crawler/spiders/boss.py`：
   - 在 `fetch_one()` 内，与 `stealth_log_request` 同一处（line ~55 附近 status != 200 的分支），调用 `record_proxy_failure(cfg.proxy)` 当 cfg.proxy 非 None
   - 在 `fetch_one()` 末尾（line ~69 已成功爬取后），调用 `record_proxy_success(cfg.proxy)` 当 cfg.proxy 非 None
   - 在 `fetch_one()` 的 `except Exception` 分支（line 67），调用 `record_proxy_failure(cfg.proxy)`
   - 添加 `from crawler.middleware.proxy_middleware import pick_proxy, record_proxy_failure, record_proxy_success`
   - 将 `cfg = StealthConfig(proxy=proxy or get_proxy(), ...)` 改为 `cfg = StealthConfig(proxy=proxy or pick_proxy() or get_proxy(), ...)` — pick_proxy 优先（支持 PROXY_LIST + breaker），未设置时降级给 compliance.get_proxy()（向后兼容）
2. 不修改其他爬虫（lagou / 51job / job51） — v2.1 仅 boss 是 PIPE-02 目标
</action>
<acceptance_criteria>
- `grep -n "pick_proxy" crawler/spiders/boss.py` 至少 2 行匹配（import + 调用）
- `grep -n "record_proxy_failure" crawler/spiders/boss.py` 至少 2 行匹配（import + 调用）
- `python -c "from crawler.spiders.boss import fetch_one, run_boss"` exit 0（语法/导入无误）
</acceptance_criteria>
</task>

<task id="T3" title="单元测试：熔断行为契约">
<read_first>
- crawler/middleware/proxy_middleware.py（T1 产出）
- pytest 配置：`pyproject.toml` 或 `pytest.ini`/`setup.cfg` 看现有测试目录约定（backend/tests 是 pytest 主目录）
</read_first>
<action>
1. 新建 `backend/tests/unit/test_proxy_breaker.py`：
   ```python
   """PIPE-02 D-02 验收：熔断行为契约。"""
   import time
   from crawler.middleware.proxy_middleware import (
       load_proxies, pick_proxy, record_proxy_failure,
       record_proxy_success, reset_for_tests, _BREAKER_STATE,
       FAIL_THRESHOLD, _parse_proxy,
   )


   def test_parse_proxy_basic():
       e = _parse_proxy("http://1.2.3.4:8080")
       assert e.host == "1.2.3.4" and e.port == 8080 and e.scheme == "http"


   def test_parse_proxy_with_auth():
       e = _parse_proxy("http://user:pass@5.6.7.8:3128")
       assert e.host == "5.6.7.8" and e.user == "user" and e.password == "pass"


   def test_pick_proxy_cycles(monkeypatch):
       reset_for_tests()
       monkeypatch.setenv("PROXY_LIST", "http://p1:8080,http://p2:8080,http://p3:8080")
       seen = set()
       for _ in range(6):
           p = pick_proxy()
           seen.add(p)
       assert seen == {"http://p1:8080", "http://p2:8080", "http://p3:8080"}


   def test_breaker_opens_after_threshold(monkeypatch):
       reset_for_tests()
       monkeypatch.setenv("PROXY_LIST", "http://only:8080")
       proxy = pick_proxy()
       assert proxy == "http://only:8080"
       # 触发 3 次失败
       for _ in range(FAIL_THRESHOLD):
           record_proxy_failure(proxy)
       # 此时应进入冷却，pick_proxy 返回 None
       assert pick_proxy() is None


   def test_success_resets_failure_count(monkeypatch):
       reset_for_tests()
       monkeypatch.setenv("PROXY_LIST", "http://x:8080")
       proxy = pick_proxy()
       # 2 次失败（未达阈值）
       record_proxy_failure(proxy)
       record_proxy_failure(proxy)
       record_proxy_success(proxy)
       # 计数已重置 → 再失败 1 次不应触发冷却
       record_proxy_failure(proxy)
       assert pick_proxy() == proxy


   def test_no_env_returns_none(monkeypatch):
       reset_for_tests()
       monkeypatch.delenv("PROXY_LIST", raising=False)
       assert load_proxies() == []
       assert pick_proxy() is None
   ```
2. 如 `backend/tests/unit/` 不存在，新建目录
</action>
<acceptance_criteria>
- `pytest backend/tests/unit/test_proxy_breaker.py -v` 全部 6 个测试通过
- 退出码 0
</acceptance_criteria>
</task>

<task id="T4" title="ruff + mypy 通过">
<read_first>
- crawler/middleware/proxy_middleware.py（T1 产出）
- crawler/spiders/boss.py（T2 产出）
- backend/tests/unit/test_proxy_breaker.py（T3 产出）
</read_first>
<action>
1. 运行 `cd backend && ruff check crawler/middleware/ crawler/spiders/boss.py backend/tests/unit/test_proxy_breaker.py 2>&1`（如 ruff 安装在 backend）
   - 如 backend 不直接 ruff，运行仓库根 `ruff check crawler/middleware/ crawler/spiders/boss.py backend/tests/unit/test_proxy_breaker.py`
2. 修复任何 ruff 报告（多余 import / 未使用变量等）
3. **非阻塞**：mypy 严格度可能抱怨 ProxyEntry Optional user/port — 不强制
</action>
<acceptance_criteria>
- ruff check 退出码 0
- 无 F/E 类错误（warning 可接受）
</acceptance_criteria>
</task>

## Verification

- `crawler/middleware/proxy_middleware.py` 提供 5 个公开函数
- `crawler/spiders/boss.py` 调 `pick_proxy` / `record_proxy_failure`
- 6 个单元测试全部 pass
- ruff 0 错误

## must_haves

truths:
  - PROXY_LIST 解析为 ProxyEntry 列表（含 user/pass split）(PIPE-02)
  - 5 分钟 ≥3 次失败 → 该代理进入 5 分钟冷却 (D-02: 熔断窗口)
  - 池空或全冷却时返回 None（直连 + WARN 日志）(D-02: 直连 fallback)
  - boss 爬虫的失败成功都通过 record_* 写入熔断字典
  - 单元测试覆盖：basic parse / auth parse / cycle / breaker open / success reset / no-env

## Artifacts this phase produces

- `crawler/middleware/__init__.py` (new)
- `crawler/middleware/proxy_middleware.py` (new — ProxyEntry, _Breaker, 5 functions)
- `crawler/spiders/boss.py` (modified — pick_proxy + record_* calls)
- `backend/tests/unit/test_proxy_breaker.py` (new — 6 unit tests)
