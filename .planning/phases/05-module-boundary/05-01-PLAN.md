---
phase: 05-module-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/match_service.py
  - backend/app/services/recommendation_service.py
  - backend/tests/unit/test_layer_boundary.py
autonomous: true
requirements: [MAT-01, MAT-02, MAT-03, PIPE-05, ARCH-03]
user_setup: []
must_haves:
  truths:
    - "Phase 3 已覆盖 PIPE-04（SSE Pipeline 契约冻结 + 测试）"
    - "match_service.py 已直接使用 core/matching/ 组件，无需大规模去包装器"
    - "recommendation_service.py 是薄层，需要类型定义明确化"
  artifacts:
    - match_service.py 边界测试（验证路由不直接调用 core 层）
    - recommendation_service.py 输入输出类型注释
    - Pipeline E2E 集成测试 skeleton
    - 匹配缓存策略文档
---

<objective>
明确 core/ 与 services/ 的模块边界，冻结 SSE Pipeline 契约，建立 Pipeline E2E 测试框架。

**Purpose:** 模块边界模糊是当前技术债务之一。match_service.py 和 recommendation_service.py 的职责边界需要明确化，确保路由层（api/v1）只调用 services/ 层，services/ 层调用 core/ 层。

**Output:**
- 模块边界测试（确保层间调用合规）
- recommendation_service.py 接口明确化
- Pipeline E2E 测试 skeleton
- 匹配缓存策略文档
</objective>

<tasks>

<task type="tracer">
  <name>Task 1: Write module boundary tests</name>
  <files>backend/tests/unit/test_layer_boundary.py</files>
  <action>
    Create a test that verifies the core/ ↔ services/ boundary:
    - API routes (api/v1/) should NOT import from core/ directly
    - API routes should only import from services/ or schemas/
    - services/ modules should import from core/ (this is expected)

    ```python
    """Layer boundary tests.

    Verifies that the module dependency rules are followed:
    - api/v1/ → services/ → core/
    - No direct api/v1/ → core/ imports
    """
    from __future__ import annotations

    import ast
    import pathlib


    def _get_imports(filepath: str) -> list[tuple[str, str]]:
        """Return (module, name) import tuples from a Python file."""
        tree = ast.parse(pathlib.Path(filepath).read_text(encoding="utf-8"))
        imports = []
        for node in ast.walk(tree):
            if isinstance(node, ast.Import):
                for alias in node.names:
                    imports.append(("", alias.name))
            elif isinstance(node, ast.ImportFrom):
                mod = node.module or ""
                for alias in node.names:
                    imports.append((mod, alias.name))
        return imports


    # API routes should not import from core/ directly
    API_ROUTE_FILES = [
        "app/api/v1/evolution.py",
        "app/api/v1/extract.py",
        "app/api/v1/judge.py",
        "app/api/v1/learning.py",
        "app/api/v1/match.py",
        "app/api/v1/position.py",
        "app/api/v1/quality.py",
        "app/api/v1/resume.py",
    ]


    def test_api_routes_do_not_import_core_directly():
        """API routes must go through services/ layer, not core/ directly."""
        violations = []
        for rel_path in API_ROUTE_FILES:
            imports = _get_imports(rel_path)
            for mod, name in imports:
                if mod.startswith("app.core") or name.startswith("app.core"):
                    violations.append(f"{rel_path}: {mod}.{name}")
        assert not violations, (
            f"API routes importing from core/ directly: {violations}"
        )


    def test_api_routes_import_from_services():
        """API routes should import from services/ for business logic."""
        for rel_path in API_ROUTE_FILES:
            imports = _get_imports(rel_path)
            service_imports = [
                (mod, name) for mod, name in imports
                if mod.startswith("app.services") or name.startswith("app.services")
            ]
            assert service_imports, (
                f"{rel_path} has no imports from services/ layer"
            )
    ```

    Note: This test is a "verification" layer boundary check. It will reveal
    which routes violate the rule — we document the findings, not fix every
    violation in this phase (that's an ongoing effort).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_layer_boundary.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>- Layer boundary test created and passes</done>
  <reversibility rating="reversible">新增测试，纯增量</reversibility>
</task>

<task type="auto">
  <name>Task 2: Document matching cache strategy</name>
  <files>backend/app/core/matching/cache.py</files>
  <action>
    Read backend/app/core/matching/cache.py. Add docstrings and type annotations to document:
    - Cache key format
    - TTL values
    - Invalidation strategy
    - Cache backend type

    This is a documentation-only task — no behavior changes.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.core.matching.cache import *; print('PASS')"</automated>
  </verify>
  <done>- Cache strategy documented in cache.py</done>
  <reversibility rating="reversible">纯文档，无行为变更</reversibility>
</task>

<task type="auto">
  <name>Task 3: recommendation_service.py interface contract</name>
  <files>backend/app/services/recommendation_service.py</files>
  <action>
    Read backend/app/services/recommendation_service.py. Add type annotations to all public functions and methods. Ensure every function has:
    - Complete type hints for all parameters
    - Return type annotation
    - Docstring describing input/output contract
  </action>
  <verify>
    <automated>cd backend && python -c "from app.services.recommendation_service import *; print('PASS')"</automated>
  </verify>
  <done>- recommendation_service.py 接口契约化完成</done>
  <reversibility rating="reversible">类型注解 + 文档，安全</reversibility>
</task>

<task type="auto">
  <name>Task 4: Pipeline E2E integration test skeleton</name>
  <files>backend/tests/integration/test_pipeline_e2e.py</files>
  <action>
    Create a Pipeline E2E integration test skeleton that covers crawl→import→graph_sync:

    ```python
    """Pipeline E2E integration test skeleton.

    Tests the full pipeline flow: crawl → import → graph_sync.
    Uses mocked external services (Neo4j, PostgreSQL) to verify orchestration.
    """
    from __future__ import annotations

    import pytest
    from unittest.mock import AsyncMock, MagicMock, patch


    class TestPipelineE2E:
        """Crawl → import → graph_sync full pipeline."""

        @pytest.mark.asyncio
        async def test_crawl_stage_emits_records(self):
            """Crawl stage should produce records for import stage."""
            # TODO: Implement with real crawl mock
            pass

        @pytest.mark.asyncio
        async def test_import_stage_creates_models(self):
            """Import stage should create database records."""
            # TODO: Implement with real DB session
            pass

        @pytest.mark.asyncio
        async def test_graph_sync_stage_projects_to_neo4j(self):
            """Graph sync should push changes to Neo4j."""
            # TODO: Implement with mock Neo4j driver
            pass

        @pytest.mark.asyncio
        async def test_full_pipeline_orchestrates_all_stages(self):
            """Pipeline orchestrator should run all stages in order."""
            # TODO: Implement with full pipeline mock
            pass
    ```

    This is a skeleton for Phase 9 to fill in when integration test infrastructure is available.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_pipeline_e2e.py --tb=short --no-cov -q 2>&1 | tail -5</automated>
  </verify>
  <done>- Pipeline E2E test skeleton created</done>
  <reversibility rating="reversible">纯增量</reversibility>
</task>

</tasks>

<verification>
1. cd backend && python -m pytest tests/unit/test_layer_boundary.py --no-cov -q
2. cd backend && python -c "from app.services.recommendation_service import *"
3. cd backend && python -m pytest tests/integration/test_pipeline_e2e.py --no-cov -q
</verification>

<success_criteria>
- [ ] Layer boundary test passes (documents violations if any)
- [ ] Cache strategy documented
- [ ] recommendation_service.py fully typed
- [ ] Pipeline E2E test skeleton created
</success_criteria>
</output>