# Phase 8-03: 辅助页面冒烟测试 + 验证（9 页）

## Goal

为剩余的 9 个辅助/管理页面添加冒烟测试，并执行最终验证。

## Success Criteria

1. 以下 9 个页面各有至少一个冒烟测试文件（渲染 + 基本交互）
2. 所有 18 个页面测试通过
3. 覆盖率配置正确，覆盖率报告反映源码实际覆盖
4. 所有现有 232 个测试保持通过
5. vue-tsc 0 errors
6. 密钥安全验证确认

## Pages

### 3.1 登录页 — Login.spec.ts
- 渲染登录表单
- 用户名/密码输入交互
- 登录按钮交互
- 表单验证（空字段、格式错误）
- 登录失败错误提示
- 加载状态

### 3.2 修改密码 — ChangePassword.spec.ts
- 渲染修改密码表单
- 输入交互
- 表单验证
- 提交成功/失败状态
- 错误提示

### 3.3 管理后台 — Admin.spec.ts
- 渲染管理后台
- 管理功能展示
- 加载状态
- 无权限提示（非管理员访问）
- 错误状态

### 3.4 数据大屏 — DataDashboard.spec.ts
- 渲染数据大屏
- 数据指标展示
- 图表挂载
- 加载状态
- 错误状态

### 3.5 学习中心 — LearningCenter.spec.ts
- 渲染学习中心
- 学习路径展示
- 加载状态
- 空学习路径状态
- 错误状态

### 3.6 闭环演示 — LoopDemo.spec.ts
- 渲染闭环演示页面
- 循环流程展示
- 加载状态
- 错误状态

### 3.7 用户管理 — UserManagement.spec.ts
- 渲染用户管理列表
- 用户列表展示
- 加载状态
- 空列表状态
- 错误状态

### 3.8 审计日志 — AuditLog.spec.ts
- 渲染审计日志列表
- 日志条目展示
- 加载状态
- 空日志状态
- 错误状态

### 3.9 全景图谱 — (Home.vue 已在 8-01 覆盖，此项为路由/重定向测试)
- 验证 `/` 路由重定向到 Home

## 密钥安全验证（ARCH-04）

确认 Phase 1 完成的密钥安全工作：
- [ ] `secrets/` 在 `.gitignore` 中
- [ ] 已追踪的密钥文件已 `git rm --cached`
- [ ] 无 `.pem` / `.key` / `.pfx` 在 Git 追踪中
- [ ] `.env.example` 不含真实凭证

## 最终验证

### 测试套件全面验证
```bash
cd frontend

# 1. 所有测试通过
npx vitest run

# 2. 覆盖率报告正确（含源码）
npx vitest run --coverage

# 3. 类型检查零错误
npx vue-tsc --noEmit

# 4. 构建不报错
npm run build
```

### 覆盖率阈值调整
基于 8-01 首次覆盖率运行结果，在 `vitest.config.ts` 中设置合理的阈值：
- 预期：lines 30-40%, functions 25-35%, branches 20-30%, statements 30-40%
- 具体值根据首次运行结果调整

## Dependencies

- 依赖于 8-01（基础设施）和 8-02（核心页面测试）
- 8-01 和 8-02 的测试辅助函数和 mock 策略在此复用

## Verification

1. `npx vitest run` — 18 个页面测试 + 232 个现有测试全部通过
2. `npx vitest run --coverage` — 覆盖率报告覆盖源码
3. `npx vue-tsc --noEmit` — 0 errors
4. `npm run build` — 构建成功
5. `git ls-files secrets/` — 空（已移除）
6. `git check-ignore secrets/` — 返回路径（被 gitignore）