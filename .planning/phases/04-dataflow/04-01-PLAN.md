---
wave: 1
depends_on: []
files_modified:
  - evaluation/judge_eval.py
  - backend/app/core/extraction/prompt.py
requirements_addressed:
  - EVAL-02
autonomous: true
---

# Plan 04-01: EVAL-02 LLM Judge 真接线 + 超时降级

**Objective:** 为 `judge_eval.py` 的 LLM judge 调用添加 10 秒超时保护，确保失败时静默回退到 `compute_skill_f1`（D-10/D-11）。LLM judge prompt 和 `_call_llm_judge` 已存在，仅需补超时和降级逻辑。

## Task 1: 为 _call_llm_judge 添加 asyncio.wait_for 超时

<read_first>
- evaluation/judge_eval.py — 当前 _call_llm_judge 实现（line 96-124），evaluate_single_sample（line 127-178）
- backend/app/core/extraction/llm_client.py — call_llm_with_fallback 签名和返回格式
</read_first>

<action>
在 `evaluation/judge_eval.py` 中：

1. 在 `_call_llm_judge` 函数内，将 `response = await call_llm_with_fallback(prompt)` 替换为 `response = await asyncio.wait_for(call_llm_with_fallback(prompt), timeout=10.0)`
2. 捕获 `asyncio.TimeoutError` 异常，返回 `(None, "LLM judge timed out after 10s")`
3. 在模块顶部添加 `import asyncio`（如果尚未存在——当前 line 1 已有）
4. 在 `evaluate_single_sample` 中，当 `use_llm_judge=True` 且 `llm_score is None` 时，不设置 `eval_result.llm_score`（保持 None），日志记录降级：`logger.info("LLM judge unavailable for sample {}, using F1 only", sid)`
</action>

<acceptance_criteria>
- `evaluation/judge_eval.py` 包含 `asyncio.wait_for(call_llm_with_fallback(prompt), timeout=10.0)`
- `evaluation/judge_eval.py` 包含 `asyncio.TimeoutError` 异常捕获
- `evaluation/judge_eval.py` 中 `evaluate_single_sample` 在 `llm_score is None` 时记录降级日志
- `cd backend && poetry run ruff check ../evaluation/judge_eval.py` 无错误
</acceptance_criteria>

## Task 2: 验证 LLM judge 降级路径

<read_first>
- evaluation/judge_eval.py — 修改后的实现
- evaluation/golden_set.jsonl — 现有 Golden Set（10 条）
</read_first>

<action>
在 `evaluation/judge_eval.py` 末尾添加 `__main__` 自检块：
```python
if __name__ == "__main__":
    # Self-check: verify F1 fallback works when LLM unavailable
    ...
```
该块用 2 条硬编码 golden/system 样本调用 `evaluate_single_sample(use_llm_judge=True)`，验证 `llm_score is None` 时不崩溃且 `f1 > 0`。不引入 pytest，仅 `python evaluation/judge_eval.py` 可运行。
</action>

<acceptance_criteria>
- `python evaluation/judge_eval.py` 退出码 0
- 输出包含 "F1 fallback OK" 或类似确认
</acceptance_criteria>

## Verification

```bash
cd backend && poetry run ruff check ../evaluation/judge_eval.py
python evaluation/judge_eval.py
```

## must_haves

- truths:
  - LLM judge 失败/超时时静默回退到 compute_skill_f1，不阻塞主流程
  - 超时上限 10 秒（D-11）
  - 不缓存 LLM judge 结果（D-12）
