---
phase: 09-frontend-mock-off
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/composables/useDashboardCharts.ts
  - frontend/src/pages/DataDashboard.vue
autonomous: true
requirements: [MSW-02]
---

# 09-02: Placeholder 删除 + 空状态展示

**Goal:** 删除 getPlaceholder* 函数，后端无数据时显示 custom-empty 空状态组件而非半透明占位图。

## Tasks

<task id="T1" title="删除 useDashboardCharts.ts 中 getPlaceholder* 函数">
<read_first>
- frontend/src/composables/useDashboardCharts.ts
- frontend/src/stores/dashboard.ts (了解 store 数据类型)
</read_first>
<action>
1. 删除 `getPlaceholderPie()` 函数定义（约 L66-76）
2. 删除 `getPlaceholderTreemap()` 函数定义（约 L136-155）
3. 删除 `getPlaceholderTrend()` 函数定义（约 L254-283）
4. 删除 `getPlaceholderRadar()` 函数定义（约 L345-367）
5. 修改 4 个 computed 中的空数据检测，从 `return getPlaceholder*()` 改为 `return null`：
   - `darkPieOption`: L21 `return getPlaceholderPie()` → `return null`
   - `treemapOption`: L82 `return getPlaceholderTreemap()` → `return null`
   - `trendOption`: L160 `return getPlaceholderTrend()` → `return null`
   - `radarOption`: L288 `return getPlaceholderRadar()` → `return null`
</action>
<acceptance_criteria>
- `frontend/src/composables/useDashboardCharts.ts` 不包含 `getPlaceholder` 字符串
- `frontend/src/composables/useDashboardCharts.ts` 中 4 个 computed 的空数据分支返回 `null`
- `useDashboardCharts` 仍导出 `{ darkPieOption, treemapOption, trendOption, radarOption }`
</acceptance_criteria>
</task>

<task id="T2" title="DataDashboard.vue 添加空状态展示">
<read_first>
- frontend/src/pages/DataDashboard.vue
- frontend/src/components/CareerPathGraph.vue (参考 custom-empty 模式)
- frontend/src/components/CompetitivenessChart.vue (参考 custom-empty 模式)
- frontend/src/App.vue (参考 custom-empty CSS 定义)
</read_first>
<action>
1. 在 4 个 `<VChart>` 元素上添加 `v-if` 条件，当 option 为 null 时不渲染
2. 在每个 `<VChart>` 后添加 `v-else` 的 chart-empty 空状态 div

改造模式（4 处相同结构）：
- `<VChart v-if="xxxOption" :option="xxxOption" autoresize :style="{ height: '100%' }" />`
- `<div v-else class="chart-empty"><span class="chart-empty-icon">📊</span><p class="chart-empty-text">暂无数据</p><p class="chart-empty-hint">数据加载中或暂无记录</p></div>`

3. 具体位置：
   - L113 VChart (darkPieOption) → 添加 v-if + v-else
   - L129 VChart (treemapOption) → 添加 v-if + v-else
   - L143 VChart (trendOption) → 添加 v-if + v-else
   - L251 VChart (radarOption) → 添加 v-if + v-else
</action>
<acceptance_criteria>
- `DataDashboard.vue` 包含 4 处 `v-if="xxxOption"` 条件渲染
- `DataDashboard.vue` 包含 4 处 `v-else` + `class="chart-empty"` 空状态
- 每个 chart-empty 包含 icon + text + hint 三层结构
- VChart 组件仅在 option 非 null 时渲染
</acceptance_criteria>
</task>

<task id="T3" title="添加 chart-empty CSS 样式（暗色主题适配）">
<read_first>
- frontend/src/pages/DataDashboard.vue (现有 CSS)
- frontend/src/App.vue:530-540 (custom-empty CSS 参考)
</read_first>
<action>
1. 在 `DataDashboard.vue` 的 `<style scoped>` 中添加 `.chart-empty` 系列样式
2. 使用 DataDashboard 的 `--dash-*` CSS 变量而非 `--foreground`/`--muted-foreground`（暗色主题适配）

样式规则：
- `.chart-empty` — flex column center, height 100%, gap 8px
- `.chart-empty-icon` — font-size 28px, opacity 0.3
- `.chart-empty-text` — font-size 13px, font-weight 600, color var(--dash-text-50), margin 0
- `.chart-empty-hint` — font-size 11px, color var(--dash-text-30), margin 0
</action>
<acceptance_criteria>
- `DataDashboard.vue` scoped style 包含 `.chart-empty` 类
- `.chart-empty-text` 使用 `var(--dash-text-50)` 颜色
- `.chart-empty-hint` 使用 `var(--dash-text-30)` 颜色
</acceptance_criteria>
</task>

<task id="T4" title="验证构建通过">
<read_first>
- frontend/src/pages/DataDashboard.vue
- frontend/src/composables/useDashboardCharts.ts
</read_first>
<action>
1. 运行 `cd frontend && npx vue-tsc --noEmit` 确认无类型错误
2. 运行 `cd frontend && npx eslint src/ --ext .ts,.vue` 确认无 lint 错误
</action>
<acceptance_criteria>
- `cd frontend && npx vue-tsc --noEmit` 退出码为 0
- `cd frontend && npx eslint src/ --ext .ts,.vue` 退出码为 0
- `useDashboardCharts.ts` 中无 `getPlaceholder` 字符串（D-06: 0 placeholder 图表）
</acceptance_criteria>
</task>

## Verification

- `useDashboardCharts.ts` 中 4 个 `getPlaceholder*` 函数已删除
- 4 个 computed 空数据时返回 `null`
- DataDashboard.vue 中 4 个图表有 `v-if`/`v-else` 条件渲染
- 空状态使用 `--dash-*` 暗色主题变量
- `vue-tsc --noEmit` 和 `eslint` 通过

## must_haves

truths:
  - useDashboardCharts.ts 无 getPlaceholder 函数（D-06: 0 placeholder 图表）
  - 后端无数据时显示空状态而非空白 canvas（D-02: custom-empty 空状态组件）
  - vue-tsc 和 eslint 通过（D-09: 构建验证）

## Artifacts this phase produces

- `frontend/src/composables/useDashboardCharts.ts` (modified: 4 getPlaceholder functions removed, computed returns null)
- `frontend/src/pages/DataDashboard.vue` (modified: v-if/v-else conditional rendering + chart-empty CSS)
