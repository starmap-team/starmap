---
phase: 09-frontend-mock-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/main.ts
  - frontend/src/env.d.ts
  - frontend/.env.development
  - frontend/src/mock/ (delete)
  - frontend/public/mockServiceWorker.js (delete)
autonomous: true
requirements: [MSW-01, MSW-03, MSW-04]
---

# 09-01: MSW 关闭 + Mock 文件清理 + 环境变量配置

**Goal:** 关闭 MSW Mock 拦截，删除 mock 文件，配置环境变量确保前端默认走真实后端 API。

## Tasks

<task id="T1" title="删除 main.ts 中 MSW 调用">
<read_first>
- frontend/src/main.ts
- frontend/src/mock/msw-browser.ts (确认 enableMocking 的签名和调用方式)
</read_first>
<action>
1. 删除 `frontend/src/main.ts:15` 的 `import { enableMocking } from './mock/msw-browser'` 行
2. 删除 `frontend/src/main.ts:22` 的 `await enableMocking()` 行
3. 保留 `bootstrap()` 函数的 async 结构（未来可能需要其他异步初始化）
4. 删除后 bootstrap() 函数体直接从 `const app = createApp(App)` 开始
</action>
<acceptance_criteria>
- `frontend/src/main.ts` 不包含 `enableMocking` 字符串
- `frontend/src/main.ts` 不包含 `from './mock/msw-browser'` 字符串
- `frontend/src/main.ts` 仍包含 `async function bootstrap()` 定义
- `frontend/src/main.ts` 仍包含 `app.mount('#app')` 调用
</acceptance_criteria>
</task>

<task id="T2" title="删除 mock 目录和 mockServiceWorker.js">
<read_first>
- frontend/src/mock/msw-browser.ts (确认无外部消费者)
- frontend/src/mock/handlers.ts (确认 MOCK_RESUME_RESULT export 无引用)
- frontend/public/mockServiceWorker.js (确认存在)
</read_first>
<action>
1. 删除整个 `frontend/src/mock/` 目录（含 handlers.ts 和 msw-browser.ts）
2. 删除 `frontend/public/mockServiceWorker.js` 文件
3. 不修改 `frontend/package.json` — 保留 msw 在 devDependencies 中（D-04）
</action>
<acceptance_criteria>
- `frontend/src/mock/` 目录不存在
- `frontend/public/mockServiceWorker.js` 文件不存在
- `frontend/package.json` 仍包含 `"msw": "^2.2.0"` 在 devDependencies 中
</acceptance_criteria>
</task>

<task id="T3" title="创建 .env.development 文件">
<read_first>
- frontend/vite.config.ts (确认 proxy 配置和 VITE_API_BASE_URL 使用)
- docker-compose.dev.yml (确认 VITE_USE_MSW=false 已设置)
</read_first>
<action>
1. 创建 `frontend/.env.development` 文件，内容为：
   ```
   # 本地开发环境变量 — 默认关闭 MSW Mock，走真实后端 API
   VITE_USE_MSW=false
   VITE_API_BASE_URL=http://localhost:8000
   ```
2. 确保 `.gitignore` 不忽略 `.env.development`（Vite 项目中 .env.development 应提交到版本控制）
</action>
<acceptance_criteria>
- `frontend/.env.development` 文件存在
- `frontend/.env.development` 包含 `VITE_USE_MSW=false`
- `frontend/.env.development` 包含 `VITE_API_BASE_URL=http://localhost:8000`
</acceptance_criteria>
</task>

<task id="T4" title="补全 env.d.ts 中 VITE_USE_MSW 声明">
<read_first>
- frontend/src/env.d.ts
</read_first>
<action>
1. 在 `frontend/src/env.d.ts` 的 `ImportMetaEnv` 接口中添加 `readonly VITE_USE_MSW: string` 字段
2. 放在 `readonly VITE_API_BASE_URL: string` 之后
</action>
<acceptance_criteria>
- `frontend/src/env.d.ts` 的 `ImportMetaEnv` 接口包含 `readonly VITE_USE_MSW: string`
- `frontend/src/env.d.ts` 的 `ImportMetaEnv` 接口仍包含 `readonly VITE_API_BASE_URL: string`
</acceptance_criteria>
</task>

<task id="T5" title="验证构建通过">
<read_first>
- frontend/tsconfig.json
- frontend/tsconfig.app.json
</read_first>
<action>
1. 运行 `cd frontend && npx vue-tsc --noEmit` 确认无类型错误
2. 运行 `cd frontend && npx eslint src/ --ext .ts,.vue` 确认无 lint 错误
3. 如有错误，修复（可能是 mock/ 删除后的残留引用）
</action>
<acceptance_criteria>
- `cd frontend && npx vue-tsc --noEmit` 退出码为 0
- `cd frontend && npx eslint src/ --ext .ts,.vue` 退出码为 0
</acceptance_criteria>
</task>

## Verification

- `main.ts` 中无 `enableMocking` 调用和 mock import
- `frontend/src/mock/` 目录不存在
- `frontend/public/mockServiceWorker.js` 不存在
- `frontend/.env.development` 存在且包含 `VITE_USE_MSW=false`
- `env.d.ts` 包含 `VITE_USE_MSW` 声明
- `vue-tsc --noEmit` 和 `eslint` 通过

## must_haves

truths:
  - main.ts 无 enableMocking 调用（D-05: 0 MSW 拦截）
  - mock/ 目录和 mockServiceWorker.js 已删除（D-08: 无 mock 目录）
  - .env.development 固化 VITE_USE_MSW=false（D-03 环境变量）
  - vite.config.ts proxy 配置已存在（D-07: Vite proxy 到后端）
  - vue-tsc 和 eslint 通过（D-09: 构建验证）

## Artifacts this phase produces

- `frontend/.env.development` (new file)
- `frontend/src/env.d.ts` (modified: ImportMetaEnv + VITE_USE_MSW field)
- `frontend/src/main.ts` (modified: enableMocking import/call removed)
- `frontend/src/mock/` (deleted directory)
- `frontend/public/mockServiceWorker.js` (deleted file)
