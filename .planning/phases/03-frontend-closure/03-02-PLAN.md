---
phase: 03-frontend-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Graph3D.vue
  - frontend/src/pages/Home.vue
  - frontend/src/pages/EvolutionDashboard.vue
  - frontend/src/stores/graph.ts
autonomous: true
requirements:
  - EVOLVE-FE-01
  - EVOLVE-FE-02
  - EVOLVE-FE-03
  - EVOLVE-FE-04

must_haves:
  truths:
    - "图谱 3D 视图中演化图层可开关，开启后显示 EVOLVES_TO 关系边"
    - "EVOLVES_TO 边着色：rising=绿、stable=灰、declining=红"
    - "点击 EVOLVES_TO 边弹出演化详情 el-drawer"
    - "演化图层聚焦当前岗位，仅渲染选中岗位的演化上下游（per D-02）"
    - "演化图层仅在 3D 视图生效（per D-03）"
    - "EvolutionDashboard 时间线滑块控制快照时间点"
  artifacts:
    - path: "frontend/src/components/Graph3D.vue"
      provides: "3D 演化边渲染 + 点击检测"
      contains: "EVOLVES_TO"
    - path: "frontend/src/pages/Home.vue"
      provides: "演化图层开关 + focusedPositionId + evolutionPaths"
      contains: "evolutionPaths"
    - path: "frontend/src/pages/EvolutionDashboard.vue"
      provides: "时间线滑块控制快照"
      contains: "el-slider"
    - path: "frontend/src/stores/graph.ts"
      provides: "evolutionPaths + focusedPositionId 状态"
      contains: "focusedPositionId"
  key_links:
    - from: "frontend/src/pages/Home.vue"
      to: "Graph3D.vue"
      via: "props: showEvolution, evolutionPaths"
      pattern: "showEvolution|evolutionPaths"
    - from: "frontend/src/stores/graph.ts"
      to: "/evolution/paths/{position}"
      via: "fetchEvolutionPathsForPosition"
      pattern: "fetchEvolutionPathsForPosition"
    - from: "frontend/src/pages/EvolutionDashboard.vue"
      to: "/evolution/snapshots"
      via: "fetchSnapshots + el-slider"
      pattern: "snapshots"
---

<objective>
演化视图功能闭环 — 在 3D 图谱中渲染 EVOLVES_TO 关系边，在 EvolutionDashboard 添加时间线滑块。

Purpose: EVOLVE-FE-01~04 实现图谱 3D 视图的演化图层渲染（独立图层可叠加 per D-01）、边着色（D-05）、点击详情（D-11 el-drawer）、聚焦当前岗位（D-02）、仅 3D 生效（D-03）；EvolutionDashboard 添加快照时间线滑块（D-10）。

Output: Graph3D.vue 渲染 EVOLVES_TO 边，Home.vue 演化图层与聚焦岗位联动，EvolutionDashboard 时间线滑块控制快照切换。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-closure/03-CONTEXT.md
@frontend/src/components/Graph3D.vue
@frontend/src/pages/Home.vue
@frontend/src/pages/EvolutionDashboard.vue
@frontend/src/stores/graph.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 3D 演化图层渲染 + 交互 (EVOLVE-FE-01/02/03)</name>
  <files>frontend/src/components/Graph3D.vue, frontend/src/pages/Home.vue, frontend/src/stores/graph.ts</files>
  <read_first>
    - frontend/src/components/Graph3D.vue
    - frontend/src/pages/Home.vue
    - frontend/src/stores/graph.ts
  </read_first>
  <action>
EVOLVE-FE-01 + D-01/D-02/D-03: Graph3D.vue 添加演化边渲染。

当前状态: Home.vue 已有 showEvolution ref（第 53 行）和 toggleEvolution 函数（第 154 行），但 Graph3D.vue 不接收 showEvolution prop，也不渲染演化边。graphStore 已有 evolutionEdges 和 fetchEvolutionEdges。

修改 graph.ts:
1. 添加 `focusedPositionId` ref<string | null>(null) — D-02 聚焦当前岗位
2. 添加 `evolutionPaths` ref — 存储聚焦岗位的演化路径（从 /evolution/paths/{position} 获取）
3. 添加 `fetchEvolutionPathsForPosition(positionName: string)` — 调 GET /evolution/paths/{position}，仅获取当前岗位的上下游演化关系（per D-02）
4. 当 focusedPositionId 变化时（position layer 中选中岗位），自动调用 fetchEvolutionPathsForPosition
5. 导出 focusedPositionId, evolutionPaths, fetchEvolutionPathsForPosition

修改 Home.vue:
1. 将 showEvolution 和 evolutionPaths 传递给 Graph3D 组件（当前只传给 Graph2D）
2. 当 showEvolution 开启且 currentLayer === 'position' 时，如果有 selectedNode 且 labels 含 Position，设 focusedPositionId
3. 当演化图层开启但未选岗位时，显示提示文案"点击岗位查看演化路径"（per specifics）
4. 将 graphStore.evolutionPaths 中的边传递给 Graph3D 作为额外 link 数据

修改 Graph3D.vue:
1. 添加 props: showEvolution (boolean), evolutionPaths (GraphEdge[])
2. 当 showEvolution 为 true 且有 evolutionPaths 数据时，将演化边合并到 3d-force-graph 的 link 数据中
3. 演化边 type='EVOLVES_TO'，与普通边区分渲染
4. D-03: 演化边仅在 3D 视图渲染（Graph3D 本身就是 3D 组件，此约束自然满足）
5. D-04: 演化边仅显示当前知识领域内的关系 — 过滤 evolutionPaths，只保留 target 在当前 expandedKAId 下的 positions

EVOLVE-FE-02 + D-05: 演化边着色。
1. 在 Graph3D.vue 的 linkColor 回调中，如果 link.type === 'EVOLVES_TO'，根据 link.properties.trend 着色: rising='#22c55e'(绿), stable='#94a3b8'(灰), declining='#ef4444'(红)
2. trust_score 用边透明度微调：opacity = 0.3 + trust_score * 0.7（trust_score 0~1 映射到 opacity 0.3~1.0）
3. 演化边用虚线样式或更细的宽度与普通 REQUIRES 边区分

EVOLVE-FE-03 + D-11: 点击 EVOLVES_TO 边弹出演化详情。
1. Graph3D.vue 添加 linkClick handler — 3d-force-graph 的 .onLinkClick(callback)
2. 点击 EVOLVES_TO 边时，emit('evolution-edge-click', edge) 事件
3. Home.vue 接收事件，弹出 el-drawer（per D-12），显示: 源岗位 → 目标岗位、趋势方向、技能重叠列表、关键差距、证据数、trust_score
4. el-drawer 标题为"演化路径详情"

具体实现注意事项:
- 3d-force-graph 的 link 数据需要 source/target 为 id 字符串或对象引用
- 演化边的 source_id/target_id 可能不在当前 visibleNodes 中（跨领域），需要过滤掉或作为虚拟节点添加（D-04 决定不渲染跨领域，所以过滤）
- NodeTooltip3D 已有 tooltip 机制，点击边时可以复用 el-drawer
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"; grep -q 'EVOLVES_TO' src/components/Graph3D.vue && echo "EVOLVE_RENDER_OK" || echo "EVOLVE_RENDER_MISSING"; grep -q 'el-slider' src/pages/EvolutionDashboard.vue && echo "SLIDER_OK" || echo "SLIDER_MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - Graph3D.vue 接收 showEvolution 和 evolutionPaths props
    - Graph3D.vue 中存在 EVOLVES_TO 类型边的渲染逻辑
    - 演化边着色: rising=绿(#22c55e)、stable=灰(#94a3b8)、declining=红(#ef4444)
    - Graph3D.vue 存在 .onLinkClick handler 或 emit('evolution-edge-click')
    - Home.vue 中存在演化详情 el-drawer
    - graph.ts 包含 focusedPositionId ref 和 fetchEvolutionPathsForPosition 函数
    - 演化边仅在当前领域内渲染（跨领域过滤）
  </acceptance_criteria>
  <done>
    - 3D 视图中演化图层可开关，开启后渲染 EVOLVES_TO 边（EVOLVE-FE-01, D-01/D-03）
    - 演化边按趋势着色：绿/灰/红，trust_score 调透明度（EVOLVE-FE-02, D-05）
    - 点击演化边弹出 el-drawer 详情（EVOLVE-FE-03, D-11）
    - 演化图层聚焦选中岗位，仅渲染当前领域内关系（D-02/D-04）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EvolutionDashboard 时间线滑块 (EVOLVE-FE-04)</name>
  <files>frontend/src/pages/EvolutionDashboard.vue</files>
  <read_first>
    - frontend/src/pages/EvolutionDashboard.vue
  </read_first>
  <action>
EVOLVE-FE-04 + D-10: 演化看板添加时间线滑块，控制快照时间点。

当前 EvolutionDashboard.vue（839 行）已有 CII 时序曲线、新兴技能卡片、技能对比、趋势概览表、演化详情 el-drawer。但没有时间线滑块来控制快照时间点。

实现步骤:
1. 添加 `fetchSnapshots` 函数 — 调 GET /evolution/snapshots 获取快照列表（后端已有此端点，见 CONTEXT.md canonical_refs）。快照列表包含 snapshot_date 和 summary 信息。
2. 在页面顶部（KPI 区域下方、CII 仪表盘上方）添加 el-slider 组件，marks 为快照日期列表。用户拖动滑块选择时间点。
3. 添加 `selectedSnapshotDate` ref，默认为最新快照。
4. 当滑块值变化时，更新 selectedSnapshotDate，并重新请求 /evolution/trends?since={date} 或过滤当前数据到该时间点。
5. 在快照时间点无数据时显示降级提示（Claude's Discretion: 简洁文案如"该时间点暂无快照数据"）。
6. 滑块区域设计：水平布局，左侧"快照"标签，中间 el-slider，右侧显示当前选中的日期。使用 design tokens 保持风格一致。
7. 如果 /evolution/snapshots 返回空数组，隐藏滑块区域，显示"暂无快照数据"提示。

具体 UI 布局:
- 在 page-header 下方、kpi-row 上方插入一个新的 el-card
- card 内: el-slider + 日期标签
- marks 对象: { timestamp: 'YYYY-MM-DD' } 格式
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"; grep -q 'EVOLVES_TO' src/components/Graph3D.vue && echo "EVOLVE_RENDER_OK" || echo "EVOLVE_RENDER_MISSING"; grep -q 'el-slider' src/pages/EvolutionDashboard.vue && echo "SLIDER_OK" || echo "SLIDER_MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - EvolutionDashboard.vue 包含 el-slider 组件
    - 存在 fetchSnapshots 函数调 GET /evolution/snapshots
    - 存在 selectedSnapshotDate 响应式变量
    - 滑块值变化时触发数据更新或过滤
    - 无快照数据时隐藏滑块或显示提示
  </acceptance_criteria>
  <done>
    - EvolutionDashboard 时间线滑块控制快照时间点（EVOLVE-FE-04, D-10）
    - 拖动滑块切换时间点，CII 曲线和趋势表随之更新
    - 无快照时显示降级提示
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vue-tsc --noEmit` — TypeScript 编译无错误
2. `cd frontend && npx eslint src/components/Graph3D.vue src/pages/Home.vue src/pages/EvolutionDashboard.vue src/stores/graph.ts` — ESLint 无新增 error
3. 浏览器验证：3D 视图开启演化图层 → 显示绿色/灰色/红色边 → 点击边弹出 el-drawer 详情
4. 浏览器验证：EvolutionDashboard 拖动时间线滑块 → 日期切换 → 数据更新
</verification>

<success_criteria>
1. 3D 视图演化图层可开关，开启后渲染 EVOLVES_TO 边
2. 演化边着色正确（绿/灰/红）
3. 点击演化边弹出 el-drawer 详情
4. 演化图层聚焦选中岗位（D-02）
5. EvolutionDashboard 时间线滑块功能可用
6. 0 个死按钮
</success_criteria>

<output>
Create `.planning/phases/03-frontend-closure/03-02-SUMMARY.md` when done
</output>
