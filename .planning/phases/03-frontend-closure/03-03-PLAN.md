---
phase: 03-frontend-closure
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/pages/MatchDiagnosis.vue
  - frontend/src/pages/PipelineMonitor.vue
  - frontend/src/composables/usePipelineMonitor.ts
  - frontend/src/components/PipelineStageCard.vue
  - frontend/src/pages/DataDashboard.vue
  - frontend/src/stores/dashboard.ts
autonomous: true
requirements:
  - MATCH-FE-01
  - MATCH-FE-02
  - PIPE-FE-01
  - PIPE-FE-02
  - PIPE-FE-03
  - PIPE-FE-04
  - PIPE-FE-05
  - DASH-FE-01
  - DASH-FE-02

must_haves:
  truths:
    - "匹配诊断学习路径显示为格式化时间线/卡片，非 JSON 数组"
    - "岗位详情热度列显示为进度条/星级，非原始数字"
    - "PipelineStageCard failed 时红色边框高亮"
    - "重试按钮点击后显示 loading spinner"
    - "配置保存后 toast 提示已更新"
    - "调度列表显示 last_run_at 和 next_run_at"
    - "支持立即执行按钮"
    - "KPI 卡片点击跳转到对应详情页"
    - "SSE 事件驱动 KPI 实时更新"
  artifacts:
    - path: "frontend/src/pages/MatchDiagnosis.vue"
      provides: "学习路径格式化时间线/卡片 + 热度进度条"
      contains: "el-timeline"
    - path: "frontend/src/components/PipelineStageCard.vue"
      provides: "failed 红色边框 + 重试 loading"
      contains: "stage-failed"
    - path: "frontend/src/composables/usePipelineMonitor.ts"
      provides: "配置保存 toast + 立即执行"
      contains: "已更新"
    - path: "frontend/src/pages/PipelineMonitor.vue"
      provides: "调度 last_run_at/next_run_at + 立即执行按钮"
      contains: "last_run_at"
    - path: "frontend/src/pages/DataDashboard.vue"
      provides: "KPI 点击跳转 + SSE 实时更新"
      contains: "router.push"
  key_links:
    - from: "frontend/src/pages/MatchDiagnosis.vue"
      to: "learningPaths computed"
      via: "el-timeline/el-card 格式化渲染"
      pattern: "el-timeline"
    - from: "frontend/src/pages/PipelineMonitor.vue"
      to: "pipeline schedules"
      via: "last_run_at/next_run_at + 立即执行"
      pattern: "last_run_at"
    - from: "frontend/src/pages/DataDashboard.vue"
      to: "router"
      via: "KPI card click → router.push"
      pattern: "router.push"
---

<objective>
匹配诊断增强 + Pipeline 前端闭环 + DataDashboard 增强 — 三个页面的交互细节闭环。

Purpose: MATCH-FE-01/02 将学习路径从 JSON 数组转为格式化时间线、热度从数字转为进度条；PIPE-FE-01~05 补全 Pipeline 监控的交互反馈（failed 高亮、重试 loading、配置 toast、调度时间、立即执行）；DASH-FE-01/02 实现 KPI 点击跳转和 SSE 实时更新。

Output: MatchDiagnosis 学习路径为 el-timeline 卡片、热度为进度条；PipelineMonitor 全交互闭环；DataDashboard KPI 可点击跳转且 SSE 驱动更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-closure/03-CONTEXT.md
@frontend/src/pages/MatchDiagnosis.vue
@frontend/src/pages/PipelineMonitor.vue
@frontend/src/composables/usePipelineMonitor.ts
@frontend/src/components/PipelineStageCard.vue
@frontend/src/pages/DataDashboard.vue
@frontend/src/stores/dashboard.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 匹配诊断增强 (MATCH-FE-01/02)</name>
  <files>frontend/src/pages/MatchDiagnosis.vue</files>
  <read_first>
    - frontend/src/pages/MatchDiagnosis.vue
  </read_first>
  <action>
MATCH-FE-01: 学习路径从 JSON 数组 → 格式化时间线/卡片组件。

当前 MatchDiagnosis.vue 第 253-264 行的 learningPaths computed 将 gapSkills 映射为 { skill, importance, gapLevel, path, pathArray }，其中 path 是 join(' → ') 字符串。Step 4 的模板中（需确认行号）学习路径可能直接显示 JSON 或简单文本。

修改:
1. 找到 Step 4 模板中学习路径的渲染区域
2. 将学习路径列表改为 el-timeline 组件，每个 gap skill 为一个 el-timeline-item
3. 每个 timeline item 包含: 技能名称（加粗）、重要性标签（el-tag: required=danger, bonus=info）、差距等级、学习路径步骤（如果 pathArray 有内容，用 el-steps 或步骤列表展示，每个步骤一个节点）
4. 时间线 item 的 timestamp 显示预计学时（如果有）
5. 如果学习路径为空，显示"—"而非 JSON 数组

MATCH-FE-02: 岗位详情"热度"列从原始数字 → 进度条/星级显示。

在 MatchDiagnosis.vue 的 Step 3 差距分析报告中，如果有岗位热度数据（matched_skills 的匹配分数或 match_score），将原始数字替换为:
1. el-progress 组件（百分比进度条），值 = match_score * 100
2. 或者用 el-rate 星级评分，值 = Math.round(match_score * 5)
3. 选择 el-progress 因为更直观且与 Admin.vue 的 authority_score 进度条风格一致

具体修改位置需确认模板中热度/匹配分数的渲染位置。
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - MatchDiagnosis.vue Step 4 模板中包含 el-timeline 组件用于学习路径
    - 学习路径不直接显示 JSON 数组（无 JSON.stringify 或原始数组渲染）
    - 热度/匹配分数使用 el-progress 或 el-rate 而非原始数字
  </acceptance_criteria>
  <done>
    - 学习路径显示为格式化 el-timeline 卡片（MATCH-FE-01）
    - 热度/匹配分数显示为进度条（MATCH-FE-02）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pipeline 前端闭环 + DataDashboard 增强 (PIPE-FE-01~05, DASH-FE-01/02)</name>
  <files>frontend/src/components/PipelineStageCard.vue, frontend/src/composables/usePipelineMonitor.ts, frontend/src/pages/PipelineMonitor.vue, frontend/src/pages/DataDashboard.vue, frontend/src/stores/dashboard.ts</files>
  <read_first>
    - frontend/src/components/PipelineStageCard.vue
    - frontend/src/composables/usePipelineMonitor.ts
    - frontend/src/pages/PipelineMonitor.vue
    - frontend/src/pages/DataDashboard.vue
    - frontend/src/stores/dashboard.ts
  </read_first>
  <action>
PIPE-FE-01: PipelineStageCard failed 时红色边框高亮。
当前 PipelineStageCard.vue 第 173-176 行已有 `.stage-failed` 样式: `border: 2px solid var(--destructive)` + 脉冲动画。PIPE-FE-01 已实现。确认样式正确且醒目。

PIPE-FE-02: 重试按钮点击后 loading spinner。
当前 PipelineStageCard.vue 第 108-117 行的重试按钮已有 `:loading="retrying"` prop，usePipelineMonitor.ts 的 handleRetryStage 在重试期间将 stageName 加入 retryingStages Set。PIPE-FE-02 已实现。确认 loading 状态正确传递。

PIPE-FE-03: 配置保存后 toast 提示"已更新，下一个 run 生效"。
当前 usePipelineMonitor.ts 第 266-275 行的 handleSaveConfig 成功后显示 `ElMessage.success('配置已更新，下一个 run 生效')`。PIPE-FE-03 已实现。确认文案符合 D-14 风格（简洁统一）。调整为 '保存成功' 以符合 D-14 统一文案规则，但保留"下一个 run 生效"作为副提示（ElMessage.success('保存成功，下一个 run 生效')）。

PIPE-FE-04: 调度列表显示 last_run_at 和 next_run_at。
当前 PipelineMonitor.vue 第 284-295 行的调度表格已有"上次运行"和"下次运行"列，显示 `row.last_run_at` 和 `row.next_run_at` 格式化时间。PIPE-FE-04 已实现。确认格式化正确。

PIPE-FE-05: 支持"立即执行"按钮。
当前 PipelineMonitor.vue 第 301-306 行的调度操作列已有"立即执行"按钮，调用 `handleTriggerSchedule(row)`。usePipelineMonitor.ts 的 handleTriggerSchedule 调用 `pipeline.triggerSchedule(schedule.id)`。PIPE-FE-05 已实现。确认按钮存在且调 API。

PIPE-FE-01~05 总结: 大部分已实现。需要微调:
1. 确认 PIPE-FE-01 的 failed 红色边框样式足够醒目（当前已有 + 脉冲动画）
2. 确认 PIPE-FE-02 的 retrying loading 状态正确
3. 微调 PIPE-FE-03 的 toast 文案为 '保存成功，下一个 run 生效'
4. 确认 PIPE-FE-04 的 last_run_at/next_run_at 格式化
5. 确认 PIPE-FE-05 的"立即执行"按钮存在且工作

DASH-FE-01: KPI 卡片点击跳转到对应详情页。
当前 DataDashboard.vue 的 KPI 卡片（第 49-100 行）有 route 属性但模板中可能未绑定点击事件。需要:
1. 在 KPI 卡片模板中添加 @click 事件，调用 `router.push(card.route)`
2. 添加 cursor: pointer 样式
3. 确认每个 KPI 卡片的 route 值正确: 总节点→/, 总关系→/, 技能域→/learning, 岗位数→/positions, 技能数→/quality, 等等

DASH-FE-02: SSE 事件驱动 KPI 实时更新。
当前 DataDashboard.vue 使用 useSSE composable，但需要确认 SSE 事件是否正确更新 dashboard store 的 overview 数据。检查:
1. useSSE 的 storeHandlers 是否包含 dashboard 相关事件
2. SSE 事件触发时是否更新 overview 的 KPI 值（total_nodes, total_edges 等）
3. 如果 SSE 未连接到 dashboard 事件，需要添加: 在 useSSE 配置中添加 dashboard_update handler，收到事件时调用 dashboardStore.fetchOverview() 刷新数据
4. 在 DataDashboard.vue 的 onMounted 中启动 SSE 连接（如果尚未启动）
5. onUnmounted 中断开 SSE
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - PipelineStageCard.vue 包含 .stage-failed 样式（border: 2px solid var(--destructive)）
    - PipelineStageCard.vue 重试按钮有 :loading="retrying"
    - usePipelineMonitor.ts handleSaveConfig 成功后 ElMessage 包含 '保存成功'
    - PipelineMonitor.vue 调度表格包含 last_run_at 和 next_run_at 列
    - PipelineMonitor.vue 调度操作列包含"立即执行"按钮
    - DataDashboard.vue KPI 卡片有 @click 事件调用 router.push
    - DataDashboard.vue SSE 事件更新 dashboard store 数据
  </acceptance_criteria>
  <done>
    - PipelineStageCard failed 红色边框高亮（PIPE-FE-01）
    - 重试按钮 loading spinner（PIPE-FE-02）
    - 配置保存 toast '保存成功，下一个 run 生效'（PIPE-FE-03）
    - 调度列表显示 last_run_at/next_run_at（PIPE-FE-04）
    - "立即执行"按钮存在且调 API（PIPE-FE-05）
    - KPI 卡片点击跳转详情页（DASH-FE-01）
    - SSE 事件驱动 KPI 实时更新（DASH-FE-02）
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vue-tsc --noEmit` — TypeScript 编译无错误
2. `cd frontend && npx eslint src/pages/MatchDiagnosis.vue src/components/PipelineStageCard.vue src/composables/usePipelineMonitor.ts src/pages/PipelineMonitor.vue src/pages/DataDashboard.vue` — ESLint 无新增 error
3. 浏览器验证：匹配诊断 Step 4 学习路径为 el-timeline 格式
4. 浏览器验证：PipelineMonitor 重试按钮 loading、配置保存 toast、调度时间列、立即执行按钮
5. 浏览器验证：DataDashboard KPI 卡片可点击跳转
</verification>

<success_criteria>
1. MatchDiagnosis 学习路径为 el-timeline 格式化卡片
2. 热度显示为进度条
3. PipelineStageCard failed 红色边框
4. 重试按钮有 loading spinner
5. 配置保存 toast 提示
6. 调度列表有 last_run_at/next_run_at
7. "立即执行"按钮存在
8. KPI 卡片可点击跳转
9. SSE 驱动 KPI 更新
</success_criteria>

<output>
Create `.planning/phases/03-frontend-closure/03-03-SUMMARY.md` when done
</output>
