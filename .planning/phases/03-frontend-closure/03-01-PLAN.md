---
phase: 03-frontend-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Admin.vue
  - frontend/src/pages/LearningCenter.vue
  - frontend/src/stores/learning.ts
  - frontend/src/stores/admin.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - LEARN-FE-01
  - LEARN-FE-02
  - LEARN-FE-03
  - LEARN-FE-04

must_haves:
  truths:
    - "Admin 编辑数据源保存后 API 被调用且列表刷新"
    - "Admin 审核队列编辑按钮弹出 el-drawer 并可保存"
    - "LearningCenter 加入计划按钮调用 POST /learning/plan"
    - "LearningCenter 从 API 加载数据，无硬编码 demo"
    - "LearningCenter 显示学习进度（从 GET /learning/plan/{plan_id}）"
    - "LearningCenter 无计划时显示空状态引导"
  artifacts:
    - path: "frontend/src/pages/Admin.vue"
      provides: "handleSaveSource 调 API + 审核编辑 el-drawer"
      contains: "el-drawer"
    - path: "frontend/src/pages/LearningCenter.vue"
      provides: "加入计划 handler + localStorage plan_id + 空状态"
      contains: "localStorage"
    - path: "frontend/src/stores/learning.ts"
      provides: "createPlanFromMatch + localStorage 读写"
      contains: "localStorage"
  key_links:
    - from: "frontend/src/pages/LearningCenter.vue"
      to: "POST /learning/plan"
      via: "learningStore.createPlan"
      pattern: "createPlan"
    - from: "frontend/src/pages/Admin.vue"
      to: "PUT /datasources/:id"
      via: "admin.updateSource"
      pattern: "updateSource"
---

<objective>
Admin + LearningCenter 功能闭环 — 把两个页面的"看起来有按钮但不工作"变成真正调 API、有反馈、有数据的闭环。

Purpose: ADMIN-01~03 修复 Admin 页面保存不调 API、审核队列无编辑弹窗；LEARN-FE-01~04 实现学习计划创建/进度/空状态的完整闭环，用 localStorage 暂存 plan_id（per D-06/07/08）。

Output: Admin.vue 的 handleSaveSource 真正调 API 且刷新列表、审核编辑用 el-drawer；LearningCenter.vue "加入计划"调 POST、进度从 GET 读取、空状态有引导文案。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-closure/03-CONTEXT.md
@frontend/src/pages/Admin.vue
@frontend/src/pages/LearningCenter.vue
@frontend/src/stores/learning.ts
@frontend/src/stores/admin.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Admin 功能闭环 (ADMIN-01/02/03)</name>
  <files>frontend/src/pages/Admin.vue, frontend/src/stores/admin.ts</files>
  <read_first>
    - frontend/src/pages/Admin.vue
    - frontend/src/stores/admin.ts
  </read_first>
  <action>
ADMIN-01: 验证 handleSaveSource 当前实现。Admin.vue 第 171-188 行的 handleSaveSource 已经调用 `admin.updateSource(editingSource.value.id, payload)` 并在成功后 `await admin.fetchSources()` 刷新列表——ADMIN-01 和 ADMIN-03 已完成。确认 `admin.updateSource` 确实调用 `request.put(/datasources/:id, payload)`（admin.ts 第 42 行已确认）。若此逻辑已正确工作，只需确认无 bug 即可。

ADMIN-02: 审核队列"编辑"按钮实现 el-drawer 编辑弹窗。在 Admin.vue 的审核队列 Tab（ReviewQueuePanel 组件），需检查 ReviewQueuePanel.vue 是否已有编辑功能。如果 ReviewQueuePanel 组件本身没有编辑按钮，需在 Admin.vue 的审核队列 Tab 中添加编辑交互：点击编辑按钮 → 打开 el-drawer（per D-12），包含 name 和 trust 字段的表单，保存时调用 `admin.updateAuditItem(id, data)`，成功后刷新列表。

具体步骤:
1. 读取 ReviewQueuePanel.vue 确认是否已有编辑按钮。若无，需在 ReviewQueuePanel 组件中添加编辑按钮和 el-drawer。
2. 若 ReviewQueuePanel 已有自己的编辑逻辑，检查是否用 el-drawer 形态（D-12 决定统一用 el-drawer）。如果不是 el-drawer，改为 el-drawer。
3. 编辑保存时调用 `admin.updateAuditItem(id, { name, trust })`，成功后调 `admin.fetchAuditQueue()` 刷新列表，并显示 ElMessage.success('保存成功')（per D-13/D-14）。
4. 如果编辑弹窗需新增在 Admin.vue 而非 ReviewQueuePanel 中，则在 Admin.vue 添加 auditDrawerVisible + editingAuditItem 响应式状态，以及 handleEditAudit / handleSaveAudit 函数。

ADMIN-03: 数据源编辑保存后刷新列表 — 已在 handleSaveSource 的 `await admin.fetchSources()` 中实现。确认 ElMessage 文案为 '保存成功'（per D-14 统一文案风格）。
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - Admin.vue 的 handleSaveSource 函数体包含 `admin.updateSource` 和 `admin.fetchSources`
    - Admin.vue 或 ReviewQueuePanel.vue 中存在 el-drawer 组件用于审核项编辑
    - 编辑保存后调用 admin.updateAuditItem 并刷新 fetchAuditQueue
    - ElMessage.success 文案为 '保存成功'
  </acceptance_criteria>
  <done>
    - handleSaveSource 调用 API 保存并刷新列表（ADMIN-01/ADMIN-03）
    - 审核队列编辑按钮弹出 el-drawer，保存后调 API + 刷新（ADMIN-02）
    - 所有保存成功提示统一为 '保存成功'（D-14）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LearningCenter 功能闭环 (LEARN-FE-01/02/03/04)</name>
  <files>frontend/src/pages/LearningCenter.vue, frontend/src/stores/learning.ts</files>
  <read_first>
    - frontend/src/pages/LearningCenter.vue
    - frontend/src/stores/learning.ts
  </read_first>
  <action>
LEARN-FE-01: "加入计划"按钮绑定 handler 调用 POST /learning/plan。当前 LearningCenter.vue 的"加入计划"按钮（第 430 行）已有 handler `handleAddToPlan(rec)`，但它调的是 `learningStore.addSkillToPlan`（给已有计划加技能），不是创建计划。需要:
1. 在推荐区域的"加入计划"按钮保持当前逻辑（给已有计划加技能），这是正确的。
2. 检查空状态下是否有"创建学习计划"的按钮和 handler。当前空状态有"从匹配诊断生成"按钮（跳转 /match），但缺少直接从推荐技能创建计划的入口。
3. 需要一个 "加入计划" handler：如果 currentPlan 为 null，先调用 `learningStore.createPlan({ position: rec.skill, skills: [rec.skill] })` 创建计划，然后 localStorage 存 plan_id（per D-06/D-08/D-09）。如果已有计划，用 el-message-box confirm 覆盖前确认（per D-08），确认后创建新计划覆盖。

LEARN-FE-02: 移除硬编码 demo 数据，从 API 加载。当前 LearningCenter.vue 的 onMounted 调用 `learningStore.fetchPlans()` 和 `learningStore.fetchRecommendations()`——已从 API 加载。检查 store 中是否有硬编码 fallback 数据。learning.ts 的 fetchPlans/fetchRecommendations 在 catch 时设置空数组，无硬编码数据。确认满足 LEARN-FE-02。

LEARN-FE-03: 学习进度展示 → 调用 GET /learning/progress/{plan_id}。当前 learningStore.fetchPlan 已实现此功能。需要增加 localStorage 集成（D-06/D-07）:
1. 在 learning.ts store 中添加 `LOCAL_STORAGE_KEY = 'starmap_learning_plan_id'`。
2. 在 fetchPlans 成功后，检查 localStorage 中是否有 plan_id。如果有，验证其有效性（调 fetchPlan），无效则清除。
3. 在 createPlan 成功后，将 plan_id 写入 localStorage。
4. 页面刷新后自动恢复 currentPlan。

LEARN-FE-04: 空状态引导。当前 LearningCenter.vue 已有空状态 UI（第 240-260 行），显示"暂无学习计划"和"前往匹配诊断生成"按钮。需要添加更具体的引导文案（Claude's Discretion 区域），如"在推荐区域选择技能加入计划"或"前往匹配诊断，基于差距分析生成个性化学习计划"。

具体修改 learning.ts:
- 添加 LOCAL_STORAGE_KEY 常量
- 在 fetchPlans 成功后调用 `_restorePlanFromLocalStorage()`：读取 localStorage plan_id → fetchPlan → 失败则清除
- 在 createPlan 成功后写入 localStorage
- 导出 restorePlanFromLocalStorage 供页面 onMounted 调用

具体修改 LearningCenter.vue:
- onMounted 中先调用 learningStore.fetchPlans()，它会自动恢复 localStorage plan_id
- 空状态引导文案更新
- handleAddToPlan 中：如果无 currentPlan，调用 createPlan 创建；已有计划时覆盖前确认
  </action>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - learning.ts 包含 LOCAL_STORAGE_KEY = 'starmap_learning_plan_id'
    - learning.ts createPlan 成功后写 localStorage
    - learning.ts fetchPlans 后从 localStorage 恢复 plan_id，无效则清除
    - LearningCenter.vue "加入计划"按钮在无计划时创建新计划（调 createPlan）
    - LearningCenter.vue 空状态有引导文案
    - 无硬编码 demo 数据
  </acceptance_criteria>
  <done>
    - "加入计划"按钮调 POST /learning/plan，localStorage 暂存 plan_id（LEARN-FE-01, D-06/08/09）
    - 数据全部从 API 加载，无硬编码（LEARN-FE-02）
    - 学习进度从 GET /learning/plan/{plan_id} 读取，页面刷新后自动恢复（LEARN-FE-03, D-07）
    - 空状态有引导文案（LEARN-FE-04）
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vue-tsc --noEmit` — TypeScript 编译无错误
2. `cd frontend && npx eslint src/pages/Admin.vue src/pages/LearningCenter.vue src/stores/learning.ts src/stores/admin.ts` — ESLint 无新增 error
3. 浏览器验证：Admin 编辑数据源 → 保存 → toast → 列表刷新；审核编辑 → el-drawer → 保存 → 刷新
4. 浏览器验证：LearningCenter 加入计划 → 创建 → localStorage 有 plan_id → 刷新页面 → 进度自动恢复
</verification>

<success_criteria>
1. Admin handleSaveSource 调 API + 刷新列表 + toast
2. Admin 审核队列编辑用 el-drawer + 保存调 API
3. LearningCenter "加入计划"调 createPlan + localStorage plan_id
4. 刷新后学习计划自动恢复
5. 0 个死按钮（"加入计划"和"编辑"都有 handler 且调 API）
</success_criteria>

<output>
Create `.planning/phases/03-frontend-closure/03-01-SUMMARY.md` when done
</output>
