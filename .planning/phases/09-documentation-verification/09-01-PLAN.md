---
phase: 09-documentation-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
  - AGENTS.md
autonomous: true
requirements: [INFRA-04]
user_setup: []
must_haves:
  truths:
    - "Phase 1-8 已完成，所有重构和测试已落地"
    - "AGENTS.md 需要与实际代码保持同步"
    - "docs/README.md 是项目文档入口"
  artifacts:
    - AGENTS.md 与实际代码同步确认
    - 最终覆盖率报告
    - 全链路验证结果
---

<objective>
同步文档，执行全链路验证，确认重构质量，完成 v4.0 milestone。

**Purpose:** 所有 8 个 Phase 已完成，最后一步是文档同步和验证确认。

**Output:**
- AGENTS.md 与实际代码一致
- 全链路验证通过
- 覆盖率确认
- STATE.md 更新为 v4.0 complete
</objective>

<tasks>

<task type="tracer">
  <name>Task 1: Full-stack verification — run backend + frontend tests</name>
  <files>N/A</files>
  <action>
    Run full test suite to verify no regressions:
    1. cd backend && python -m pytest tests/unit/ tests/integration/ --tb=short -q 2>&1 | tail -5
    2. cd frontend && npx vitest run 2>&1 | tail -8
    3. cd frontend && npx vue-tsc --noEmit 2>&1
  </action>
  <verify>
    <automated>Backend: pytest passes; Frontend: vitest + vue-tsc passes</automated>
  </verify>
  <done>- Full-stack verification complete</done>
  <reversibility rating="reversible">验证任务，无代码变更</reversibility>
</task>

<task type="auto">
  <name>Task 2: Coverage confirmation</name>
  <files>N/A</files>
  <action>
    Run coverage reports and document final numbers:
    1. cd backend && python -m pytest tests/ --cov=app --cov-report=term -q 2>&1 | grep "TOTAL"
    2. cd frontend && npx vitest run --coverage 2>&1 | grep "All files"
  </action>
  <verify>
    <automated>Coverage documented</automated>
  </verify>
  <done>- Coverage confirmed and documented</done>
  <reversibility rating="reversible">验证任务</reversibility>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md to mark v4.0 complete</name>
  <files>.planning/STATE.md</files>
  <action>
    Update STATE.md to reflect:
    - All 9 phases complete
    - v4.0 milestone 100%
    - Final metrics
  </action>
  <verify>
    <automated>STATE.md updated</automated>
  </verify>
  <done>- v4.0 marked complete</done>
  <reversibility rating="reversible">元数据更新</reversibility>
</task>

</tasks>

<verification>
1. Backend pytest passes
2. Frontend vitest passes
3. vue-tsc 0 errors
4. Coverage documented
</verification>

<success_criteria>
- [ ] Backend tests pass
- [ ] Frontend tests pass (255+)
- [ ] vue-tsc 0 errors
- [ ] Coverage >= 70% (backend), >= 60% (frontend)
- [ ] STATE.md updated to v4.0 complete
</success_criteria>
</output>