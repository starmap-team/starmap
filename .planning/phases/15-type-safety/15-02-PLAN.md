---
wave: 2
depends_on: [PLAN-01]
files_modified:
  - frontend/src/api/client.ts (as any removal)
  - .pre-commit-config.yaml (new)
  - frontend/eslint.config.js (strict rules)
autonomous: true
requirements: [QUALITY-02, QUALITY-03, QUALITY-04]
---

# PLAN-02: Frontend Type Safety + Pre-commit (Wave 2)

## Tasks

### Task 2.1: Eliminate `as any` in production code
Fix 2 `as any` in client.ts with proper types.

### Task 2.2: Add pre-commit hooks
Create `.pre-commit-config.yaml` with ruff, eslint, vue-tsc gates.

### Task 2.3: Enable eslint strict
Set `@typescript-eslint/no-explicit-any` to warn.

## Acceptance Criteria
- Production code `as any` = 0
- `.pre-commit-config.yaml` exists and runs
- `cd frontend && npm run lint && npm run typecheck` — 0 errors
