---
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml (mypy config)
  - backend/app/api/v1/extract.py (sa.text removal)
  - backend/app/api/v1/quality.py (sa.text removal)
  - backend/app/repositories/ (new - SQL sink)
autonomous: true
requirements: [QUALITY-01, QUALITY-05]
---

# PLAN-01: Backend Type Safety + SQL Sink (Wave 1)

## Tasks

### Task 1.1: Unlock mypy for core modules
Remove `ignore_errors = true` from pyproject.toml mypy section for matching, graph_service, match_service, celery_app. Fix type errors.

### Task 1.2: Sink raw SQL to repository layer
Move `sa.text()` calls from extract.py and quality.py into new `app/repositories/` module. API layer calls repository methods.

## Acceptance Criteria
- mypy core 4 modules: 0 errors
- API routes have 0 `sa.text()` calls
- `cd backend && poetry run pytest -q` — 0 new failures
