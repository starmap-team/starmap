# scripts/offline/ — 离线 Fixture 数据生成（CI / 演示专用）

## 用途定位

本目录下的脚本用于**离线环境**生成确定性 JD 抽取数据：

- CI 流水线（无外网、无 LLM 凭据）
- 本地评审演示（v2ex 爬虫被限流时）
- 单元测试 fixture 数据源

**严格禁止**：将这些脚本的数据混入正式生产 pipeline。它们生成的数据**不是真实爬取结果**，是内置 fixture JDs + 确定性归一化（无 LLM 调用）。