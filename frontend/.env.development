# 本地开发环境变量 — 默认关闭 MSW Mock，走真实后端 API
#
# 注意：不要在这里设置 VITE_API_BASE_URL 为绝对 URL（如 http://localhost:8000），
# 否则 axios 会绕开 vite proxy 直接打后端端口。
# 默认让 axios baseURL 走 fallback '/api/v1'（相对路径），由 vite.config.ts 的
# proxy 转发到正确的后端（默认 http://localhost:8000）。
VITE_USE_MSW=false
